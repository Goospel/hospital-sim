---
tags:
  - type/meta
---

# plan — 앞으로 할 일 (살아있는 계획)

> 범례: ✅완료 · 🔜다음 · ⬜예정 · ⏸의도적 보류(본선/v2) · ⚠️리스크·전제 · 🔴기각된 답(이력 보존)
> 관련 문서: [changeLog.md](changeLog.md)(완료 상세·역순) · [troubleshooting.md](troubleshooting.md) · [docs/concept/game-concept.md](../docs/concept/game-concept.md) · **[submission-plan.md](../docs/submission/submission-plan.md)**(25일 제출 로드맵 상세)
>
> 📏 **이 문서의 규율**: plan은 **"앞으로 할 일 + 살아있는 제약"**만 담는다. 구현이 끝나 changeLog에 기록된 완료 서사는 여기 쌓지 않는다 — 완료 마일스톤은 "무엇이 끝났나" **한 줄(지도)**로만, 상세는 changeLog·PR·git이 가진다. (2026-07-18 정리: 완료 서사 130줄을 걷어냈고, F1~F3의 📌 설계 근거는 [spec §13](superpowers/specs/2026-07-17-essential-care-economics-devices-design.md)으로 이관. 이 규율 자체가 "문서가 뚱뚱해진다"의 처방 — 읽기 패턴이 통독인 plan에 조회용 완료 기록이 섞이면 안 된다.)

## 목표

NAN 2026 (NHN Game × AI 해커톤) **사전 과제 제출**. 마감: **2026-08-10**. 본선(9/4~9/6)은 이 프로토타입을 확장.

**제출물 정의·진행 상태는 [docs/submission/requirements.md](../docs/submission/requirements.md)가 단일 출처다**(요강 5종 표 · 솔로라 1~4번 · 미확인 항목). 여기 목록을 다시 적지 않는다 — 두 곳에 적으면 한쪽이 낡는다.

전략: **최소 제출선 먼저** — LLM 없이 결정론 폴백으로 한 판 플레이+배포를 먼저 확보하고, 실LLM은 그 위에 **비파괴 가산 레이어**로. 핵심 원칙: **판정 = 결정론 코드 / 대사 = LLM**(2콜 분리). 게임 상태 전이는 절대 LLM이 정하지 않는다.

**AI 심사축(2026-07-16 결정)**: 심사 북극성 = **'디렉팅'**(AI를 어떻게 지휘해 게임을 만들었나, B축). 대화 LLM(게임 속 AI 기능, A축)은 B축의 대체재가 아니라 **최강 물증**이다 — 2콜 권한 경계(`adjudicate`에 설득 파라미터 부재 = 타입 수준 하드월)가 "AI를 어디까지 믿고 어디서 끊는가"의 디렉팅 그 자체. → 극소판 라이브 유지, 풀 2콜(X8)은 발전 가능성으로 후순위. ④ 문서는 B축으로 리드하고 A축을 물증으로 → [ai-usage-doc.md](../docs/submission/ai-usage-doc.md).

---

## ✅ 완료 — 지도만 (상세·회고는 changeLog가 단일 출처)

- **M0·M1 — 셋업 + 순수 시뮬 코어**: Next+TS+Tailwind·배포 파이프라인·작업추적 3종 / 결정론 전원 판정(4게이트)·골든타임·상태기계·**병상0 하드락 불변식**. PR #1~#3
- **U1~U5 — 최소 제출선**: 한 판 UI 척추·거절 누적·설득 입력(하드락 유지)·인과 디브리핑(차가운 영수증)·병원 장부(흑자인데 필수과 채용 0)·Vercel 라이브. PR #6~#16
- **R3.5~R3.8 — 리서치·문서 정합 계열**: 거절 사유 배후진료 중심 4종 재설계 / game-concept를 코드·리서치 현재 상태에 lint 동기화 / AI활용 규칙 감사 + 훅 2개 승격 / troubleshooting 항목 1건=파일 1개 분할. PR #10·#11·#36·#42·#43
- **E — 경영 확장(2막)**: 위저드→콜큐→막간→응급→결말 플레이어블 / 7일 루프+달력(분기 폐기) / 하루 진료 자리 제한(과부하) / 랜딩 타이틀 카드. PR #17~#35 ⚠️ **아래 「응급 재설계 A」가 2막(주말 응급·뺑뺑이 미니게임)을 은퇴시켜 supersede**
- **응급 재설계 A — 2막 은퇴·받는 쪽 통합**: 주말 STEMI 클라이맥스·골든타임 뺑뺑이 미니게임 은퇴(12파일 삭제), 7일차 DAY_END→WEEK_SUMMARY 직행, 결산=돌려보낸 응급 수·에필로그=주간 신문+장부. 플레이어는 항상 받는 벽. 설계 [spec](../docs/superpowers/specs/2026-07-19-received-side-emergency-redesign-design.md)
- **응급 재설계 B — 응급 CallKind 다양화**: 분만(산부)·뇌출혈(신경외과)·중증외상(외과)을 받는 콜로 신설 — 4종이 hardlockReason 한 case를 공유(adjudicate 제네릭 재사용, 판정 무변경). DAY_PLANS 4종 분산 재구성, 하드락 대사·신문 헤드라인이 과를 정확히 따라감, 결산에 받은/돌려보낸 응급 병치. 재정중립 산부 −16 상쇄가 장부에서 체감. 233 vitest green + tsc 0 + 브라우저 라이브 검증
- **의사 개인 유닛 — 담당 수·피로도 표시 레이어**: 채용 인원수를 이름 붙은 개인 유닛으로(개원 시 결정론 명명), RECEIVING에 각 의사의 담당 환자 수(실시간)·피로 막대. 시스템 부하가 개인의 몸에 남기는 자국을 가시화(경영자 vantage). 순수 표시(판정·경제 0 침습), `doctor.ts`(런타임 임포트 0)+`Hospital.roster?`+`SessionState.fatigue`(주 간 누적). 268 vitest green + tsc 0 + 브라우저 검증. ⏸ 후속: ~~승격 문(피로→판정 먹이기)~~ → ✅ 2026-07-25 「피로 승격」으로 실현 · 흉부외과는 라우팅 콜이 없어 항상 담당 0(현 콜 종류 구성상 정직 — 피로 승격에서도 보류 재확정, F4 절단 근거 유효)
- **시간 기반 하루 루프 — 전문의 점유 벽**: 하루를 콜 5통 고정에서 9시~마감 시계로, 진료 소요시간(seed 결정론 `daysim.ts`)이 배후과 의사를 점유해 응급이 자유 전문의 없으면 벽(`NO_FREE_SPECIALIST`). 능동 거절 제거(응급 자동·선택진료만 결정), 병상 벽 제거, boarding=의사 점유 이월, dayProgress 시계 기반. 배후과 예약(SPECIALIST_ELECTIVE) 흑자원+점유 경쟁원(라벨 DEPARTMENTS 파생). 289 vitest green + tsc 0 + next build + 브라우저 실측(콘솔 0). 최종 리뷰 Ready(I8 구조 방어 — 흑자원 인원캡3·콜델타 큐고정). 설계 [spec](../docs/superpowers/specs/2026-07-20-time-based-day-loop-design.md)·[플랜](../docs/superpowers/plans/2026-07-20-time-based-day-loop.md)·[리서치](../docs/research/physician-workflow-and-backup-occupancy.md). → ✅ 아래 「내과 진료과 추가」로 실현(GENERAL_EMERGENCY 설계A 폐기)
- **내과 진료과 추가 + 복통 응급 세분**: 내과(INTERNAL_MEDICINE) 신설(저수가 박리다매 −5·essential·소송 미구현). `GENERAL_EMERGENCY`를 급성복증(`ABDOMINAL_EMERGENCY`, 외과)·고열감염(`MEDICAL_EMERGENCY`, 내과)으로 세분해 그 과로 라우팅·점유 경쟁. `isCriticalEmergency`를 `requiresBackupCare`(배후·신문)/`carriesLawsuitRisk`(소송) 술어 2개로 분리 → 무게 비대칭(급성복증=소송+신문 / 고열감염=신문만). 설계 A(GENERAL 무점유) 폐기→B(세분 응급 점유). 302 vitest green + tsc 0 + 브라우저 7일 완주(I8 +47억·콘솔 0). 설계 [spec](../docs/superpowers/specs/2026-07-20-internal-medicine-department-design.md)·[플랜](../docs/superpowers/plans/2026-07-20-internal-medicine-department.md)·[리서치](../docs/research/internal-medicine-emergency-grounding.md). ⏸ 후속: 내과 법적 리스크(소송) 구현·회색지대 노이즈·내과↔외과 오분류
- **병원 성장 시스템(재투자 루프)**: 주 사이 GROWTH 페이즈 — 금고(누적 순이익)로 배후과 채용·병상 증설. ①인력 제로섬(전국 유한 풀 `system.ts`·소진=못 뽑음·배경 감소) + ②금고 재정 지속불가 + ③병상 증설→콜 볼륨↑(3→5·5→7·7→9)로 커질수록 배후 없으면 더 뺑뺑이. 에필로그 전국·지방 병치(강원 40.3%·풀 소진). SDD 11태스크·**337 vitest**·tsc 0·next build·I8 결정론 계산(최대 353·수익과 3캡)·브라우저 e2e(돌려보냄 20→33). 설계 [spec](../docs/superpowers/specs/2026-07-20-hospital-growth-system-design.md)·[플랜](../docs/superpowers/plans/2026-07-20-hospital-growth-system.md)·[리서치](../docs/research/hospital-growth-dilemma-grounding.md)·[T-056](troubleshooting/T-056.md). ⏸ 후속: 상급종합 지위·③ 공간화·개원 채용 풀 차감(설계 확인)
- **병원 맵 — 의사를 숫자에서 아바타로**: RECEIVING 화면에 탑다운 픽셀 병원 맵을 얹었다. `deriveMapScene(receiving, atMin)` 순수 파생 하나가 방(8칸, 안 뽑은 과도 빈 방으로 존재)·의사 위치·`busyUntil` 파생 병상 환자·조명 3단을 전부 계산한다(새 게임 상태 0개, `src/game/*.ts` 그 외 무변경). `compareDeptKeys`로 맵 방 순서와 `DoctorRoster` 명단 순서를 단일 출처로 묶었다. inline SVG 자체 제작 스프라이트(`PixelSprite.tsx`) + 아바타 절대 좌표 한 레이어(`HospitalMap.tsx`)로 이동이 CSS transition으로 미끄러진다. 콜 사이 빨리감기(`useFastForwardClock` — 캡 1500ms·12스텝·reduced-motion 즉시 점프)와 맵 클릭 스킵을 얹어 맵 주도로 `ReceivingPhase` 레이아웃을 재편했다. SDD 5태스크·**367 vitest**(+30)·tsc 0·next build·브라우저 7일 완주 실측(8칸 빈 방·수용 시 아바타 입실+침대 점유·진료 종료 시 퇴실+침대 비움·17시 이후 🌙 전환·1인 배후과(외과) 야간 소등 vs 2인 배후과(순환기) 유지·빨리감기 애니메이션+스킵·모바일 375px 가로 스크롤 없음, 콘솔 에러 0). 설계 [spec](../docs/superpowers/specs/2026-07-23-hospital-map-design.md)·[플랜](../docs/superpowers/plans/2026-07-23-hospital-map.md) ⚠️ **아래 「연속 시간 흐름 — 콜 사이가 흐른다」가 빨리감기(`useFastForwardClock` — 캡 1500ms·12스텝)를 은퇴시켜 supersede**
- **연속 시간 흐름 — 콜 사이가 흐른다**: RECEIVING이 "정지가 기본이고 움직임이 예외"인 슬라이드쇼였다. 시계를 게임 1분 = 50ms로 늦추고 캡을 없애(콜 사이 0.48초 → 6초, `useHospitalClock`) 콜과 콜 사이가 끊기지 않고 흐른다. 시계만 늦추면 자유 의사가 복도에 서 있어 "느려진 정지 화면"이 되므로, 유휴 배회(`wanderTiming` — 아바타 안쪽 `transform`)와 배경 보행자(`ambientWalkers` — 조명 파생 주간 5/석양 2/야간 0, MapScene 밖 별도 레이어)를 **게임 시계와 분리된 CSS 층**으로 세웠다 — 결정 대기로 시계가 멈춰도 병원은 계속 돈다. 콜 카드는 도착해야 뜨고(`CallCard` 분리 — 마감 흐름에선 `queue[index]`가 undefined), 하루의 끝은 `session.ts`가 이미 계산해 내일 아침으로 넘기던 마감 초과 점유(`dayEndMin`)를 읽어 19시를 넘겨 재생한다(새 숫자 0개). 게임 로직(receiving·adjudicate·daysim·session) 0줄. 설계 [spec](../docs/superpowers/specs/2026-07-23-continuous-clock-design.md)·[플랜](../docs/superpowers/plans/2026-07-23-continuous-clock.md)
- **PR 게이트 CI — 머지 전 검사기**: `npm test`가 도는 곳이 배포 워크플로(`on: push: main`) 하나뿐이라 **머지된 뒤에야** 돌았고, 머지 전 실질 게이트는 "돌렸다고 말하는 것"뿐이었다. `on: pull_request`로 lint → test → build를 도는 `.github/workflows/ci.yml` 신설(빌드는 타입 검사용 — vitest는 transpile-only). 문서 태그 하나가 빠지면 커밋이 거부되는데 테스트가 깨진 코드는 통과하던 비대칭을 없앴다. ⏸ **LLM 리뷰 봇은 의도적 보류** — 결정론 검사가 비어 있는 상태에서 확률적 리뷰를 얹으면 오탐 노이즈가 먼저 쌓이고, 추가 비용을 들일 만큼 리뷰가 부족하지 않다는 판단(2026-07-30). 검사할 축이 생기면 LLM이 아니라 테스트·검사기로 옮긴다. [T-110](troubleshooting/T-110.md)

### 🔜 다음

> ⚠️ **방향 전환(2026-07-27 · 사용자 결정)** — *게임이 재미없다*는 판정으로 **림월드형 전면 재설계(배수진)**에 들어갔다. 새 게임 정의의 단일 출처는 [림월드형 재설계 spec](../docs/superpowers/specs/2026-07-27-rimworld-like-redesign-design.md)이다. 이 문서 위쪽의 콜 카드 루프 항목들·아래 크리티컬 패스는 이 전환으로 **재검토 대상**이며(메시지·2콜 원칙·경제/피로/사직 수학은 계승), 문서 전반 정합(game-concept 배너·README·submission 문서)은 2주차 문서 부채로 처리한다.

- ✅ **림월드형 재설계 1주차 — 「돌아가는 병원」 슬라이스** (2026-07-27 완료, brainstorming→writing-plans→SDD 7태스크·2단 리뷰): `src/sim/` 결정론 코어(타일 48×32·방 건설·A*·폰 틱·환자 흐름·경로 무효화) + `/sim` 렌더(드래그 건설·일시정지/배속). 기존 게임 무변경 공존. **693 vitest**·tsc 0·build+Pages export·브라우저 실측. 상세 changeLog·[플랜](../docs/superpowers/plans/2026-07-27-rimworld-week1-living-hospital.md).
- ✅ **림월드형 2주차 — 「림월드가 되는 주」** (2026-07-28 **PR A~D 전부 완주** · 우선순위 결정 2026-07-27은 "시간 부족 시 PR C 절단·PR D 사수"였는데 C도 D도 살렸다. LLM은 계획대로 폴백 우선):
  - ✅ **PR A — 하루·주 리듬** (2026-07-27 완료): 19시 정산(`settleDay` — 진행 진료 완료 인정·잔여 이탈 집계)→`DAY_END` 동결→[다음 날], 7일차 직행 `WEEK_END`→주간 결산(의사 고정비 상수·`insolvencyStreak` 폐업 음수 2주)→[다음 주]. 도착 시드에 week 폴딩(재방송 차단·T-087 회귀 수정 포함). 오버레이 2종+HUD `W주 D일`. **737 vitest**·tsc 0·build+Pages export·브라우저 3주 완주 실측(폐업 분기 포함). 상세 changeLog·[플랜](../docs/superpowers/plans/2026-07-27-rimworld-week2-day-week-rhythm.md)
  - ✅ **PR B — 과·경제·응급·피로 이식** (2026-07-28 완료): 과 4개 카탈로그(`dept.ts`)·환자 과 라우팅(삼중 일치)·**응급 배후과 하드락**(`NO_SPECIALIST`/`NO_BED` — 108구성 전수 불변은 **리뷰 프로브** 실측·커밋 테스트는 대표 케이스)·피로 이식(기존 곡선 임포트·감속·밤 회복)·과별 결산+**부호 불변식 I-B1**(40시드 위반 0은 **리뷰 프로브** — 커밋 회귀망은 4시드 — 계획 수치가 부호 정반대였던 것을 실측이 잡아 카탈로그 재조정, 현행 값 단일 출처는 `dept.ts`·`emergency.ts`)·UI 일괄(과 선택 건설·채용 패널·회차 토스트·피로 막대·결산 과별 표). **857 vitest**·build+Pages export. 상세 changeLog·[플랜](../docs/superpowers/plans/2026-07-28-rimworld-week2-dept-economy.md). **이월**: 주간 결산 사유별 내역(DayRecord가 사유 미보유) · 과 없는 의사 계약 통일(패널 은폐 vs 결산 폭발) · 사직 후 `−0만원` 고정비 표시 · HirePanel 포커스 트랩 · **밸런스 판단 필요 — 4과 완비 병원이 주 −3,700~−4,800으로 약 10주 폐업(유일 흑자 = 미용 단독). 부호는 의도이나 "혼합 생존선"의 존재 여부는 게임 디자인 결정**(고정비 10:1 비율이 유일한 조정 후보)
  - ✅ **PR C — 욕구·우선순위·사직** (2026-07-28 완료 · 절단 후보였으나 완주): **욕구 2종** — 휴식(LOUNGE 의자로 걸어가 60분 · 피로 −15 · 밤 회복 20보다 반드시 약함이 계약)·허기(CAFETERIA · 300분 뒤 배고픔 · 식사 20분 · 굶은 채 시작한 작업 **1.15배** 감속, 식사로만 풀림). 두 갈래가 `BREAKS` 표 한 기계를 공유하고 **표 순서가 곧 우선순위**(휴식 먼저 — 되돌릴 수 없는 쪽이 급하다). ⏸ **스트레스는 절단**: 행동 규칙이 피로와 동형(임계→행동 중단→회복)이라 셋째 축이 새 결정을 안 낳는다 — 개성 상호작용이 붙어야 뜻이 생기므로 **PR D(스토리텔러) 재료**로 넘긴다. **우선순위 토글 0~3**(진료·응급·휴식 세 축 · 기본 2 · 화면 순환 2→3→1→0). 응급 하드락 `NO_SPECIALIST` 정의가 "그 과 의사 ≥1"에서 **"응급을 켠 그 과 의사 ≥1"**로 확장됐다(토글이 실제로 벽을 세운다). **사직·전국 풀** — 포화(피로 100)로 마감한 날이 **리셋 없이** 쌓여 4일에 닿으면 주간 결산에서 통지하고 **다음 주에 집행**, 전국 풀(미용 8·내과 5·외과 3·순환기 2)로 **돌아오지 않는다**. 리팩터 둘: `spots.ts` **leaf 추출**(needs ⇄ patientFlow 값 임포트 순환 제거 — [T-093](troubleshooting/T-093.md))·`workDurationMin` 통합(피로×허기 감속의 단일 출처). **958 vitest**·tsc 0·eslint 0(신규 `no-restricted-imports` 포함)·build+Pages export·브라우저 실측(채용→건설→나흘 포화→휴게실·식당 관측→토글 4단 순환→1주차 결산 사직 통지→다음 주 소멸·잔여 미반환). 함정 4건 등재([T-091](troubleshooting/T-091.md)~[T-094](troubleshooting/T-094.md)) + [T-086](troubleshooting/T-086.md) 보강. 상세 changeLog·[플랜](../docs/superpowers/plans/2026-07-28-rimworld-week2-needs-resignation.md)
    - ✅ **PR A 이월 해소 — "고정비 = 결산 시점 의사 수" 재검토**: **유지로 결론**. 사직 집행이 `startNextWeek`에서만 일어나 주 중 인원 변동이 없고, 그래서 고정비는 자동으로 **사직 전** 명단으로 청구된다(그 주를 일한 사람의 주급은 나간다 — 의도).
    - ✅ **최종 브랜치 리뷰 READY**(2026-07-28) — 게이트 재확인(958 green·tsc 0·eslint 0·양 빌드·`src/game` 0줄) + **다주 종주 25조합**(5구성 × 5시드 × 최대 12주)에서 불변식 위반·크래시 0 + **부호 불변식 I-B1 12시드 전부 통과**(미용 +40~+700 / 순환기 −1,500~−4,700 — PR B가 세운 논지가 욕구·사직이 얹힌 뒤에도 유지된다) + 돌연변이 헤드라인 8종 전원 사살 + 브라우저 핵심 여정 완주(콘솔 0·375px 넘침 0). **아래 밸런스 수치는 전부 「(최종 리뷰 프로브)」 — 커밋된 회귀망이 아니라 리뷰가 따로 돌린 측정이다**(커버리지로 읽으면 안 된다 — [T-089](troubleshooting/T-089.md)의 문서판).
    - ⚠️ **이월(밸런스) — 최종 리뷰 프로브 실측 반영**: ⓐ **포화의 88%가 응급이다**(응급분 중앙값 · 최종 리뷰 프로브). 근인이 산수로 특정됐다 — 응급 1건이 90분 × 강도 2.0 = **180**인데 하루 상한이 160이라 **1건만 받아도 초과**다. "1건은 공짜, 2건부터 갈린다"를 원하면 `EMERGENCY_INTENSITY` ≤ **1.78**(`src/sim` 자유값이라 옛 게임 무영향). **대조군이 이걸 "유일 원천"으로 확정한다**(최종 리뷰 프로브 · 병동 없는 병원 3시드): 병동이 없으면 응급 수용이 0이라 최대 일부하가 **130.8~154.8**로 전부 상한(160) 아래에 머물고, 그 결과 **최대 피로 0 · 포화일 0**이다 — 성분 88%가 나머지 12%의 기여를 남기는 것과 달리, 응급을 통째로 빼면 포화가 **아예 발생하지 않는다** ⓑ **대응 창은 생각보다 넓었다**(최종 리뷰 프로브 — 앞서 적은 "~3일"을 정정): 「포화 N/4일」 표시가 **4~10일**(중앙값 6), 「이번 주말 떠남」 배지가 **1~3일** 선다. 인사 패널 표시로 코어 튜닝 없이 넓히려던 의도가 실측으로 확인된 것이다 ⓒ **순환기 2인 동시 채용은 1인보다 나쁘다**(최종 리뷰 프로브) — 둘이 같이 갈려 **동시에** 사직하고 그 과 풀이 0으로 영구 소진돼 금고까지 악화된다. "두 번의 기회"는 **순차 채용에서만** 생긴다(플레이어에게 안 보이는 함정 — 표시 후보) ⓓ **굶주림 감속(1.15배)은 포화·사직 시점을 못 앞당긴다**(최종 리뷰 프로브의 **반증** — 얹을 때의 기대와 반대다). 그래서 식당은 처리량을 2~5% 깎는 **순손실 건물**이다(후보 손잡이: `MEAL_MIN`↓ 또는 `STARVED_SLOW`↑) ⓔ **rest 토글이 응급 피로에 무력** — 토글은 외래 배정에서만 빼 주는데 피로의 주범이 응급이다(태업 조합에서 바뀐 건 외래 17→7건뿐). *이 축은 Task 4 프로브 관측이고 최종 리뷰의 5축에는 없었다 — 재실측 전까지 그 귀속으로 읽는다* ⓕ **튜닝 손잡이 소속** — `EMERGENCY_INTENSITY`·`durationMin`·`REST_BREAK_*`·`MEAL_MIN`·`STARVED_SLOW`·`dept` 카탈로그는 `src/sim` **자유값**이지만 `RESIGN_SATURATED_DAYS`·`FATIGUE_*`는 `src/game/doctor.ts` **공유**라 한쪽만 돌리면 옛 게임이 함께 움직인다(갈라야 하면 `DEFAULT_PRIORITY` 패턴 — 같은 상수를 쓰는 것이 의도된 등식임을 주석으로 못박고 필요할 때 분기)
    - 🔑 **신규 — 판 종결이 없다**(최종 리뷰 프로브가 새로 드러낸 것 · **게임 디자인 결정 필요**): ⓐ **의사 0명 좀비 상태** — 전원 사직하면 수익 0 · 고정비 0이라 금고가 **음수로 안 간다** → 폐업 조건(음수 연속 2주)이 영영 성립하지 않아 판이 안 끝나고, 주 ~430명 이탈만 무한 누적된다 ⓑ **완비 병원도 같은 끝** — 응급 2과(순환기·외과)가 1~2주 내 전멸하고 미용+내과만 남아 **무한 지속**된다. 논지("필수과부터 갈려나간다")는 정확히 성립하는데 **끝나는 자리가 없다** — 폐업이 유일한 종결 장치인데 그게 "돈을 잃는 것"만 보고 "사람을 다 잃은 것"은 안 본다. PR D(스토리텔러 결말) 또는 사용자 판단 대상
    - ⚠️ **그 외 이월**: 주중 사직 없음(의사 0 + 수익 > 0 상태는 표시만 잠기고 세계는 계속 돈다) · 태업 힌트(「쉴 자리 없음」) 발생 조건이 좁다(rest > exam **이면서** 휴게실 부재 — 각색 수용) · M-2 `resigningIds` 메모화(주간 결산 렌더마다 재파생 — 주 1회라 실측 영향 0, 주석만)
  - ✅ **PR D — AI 스토리텔러 + 판 종결 + 랜딩 스왑·제출물** (2026-07-28 완료 · 사수 대상이었고 완주): **판 종결 3종**(`settleWeek` 단일 판정 — `INSOLVENCY` > `NO_PEOPLE`(의사 0 **그리고** 전국 풀 0 · 사직 선반영) > `CAMPAIGN_END` 12주. **우선순위가 곧 주장 — 사람이 시간을 이긴다**) · **이벤트 엔진 4종**(`events.ts` leaf + `director.ts` — 대량 응급/전염병/인근 폐업/의료소송. 효과·전제·가드레일 전부 코드, 지속은 그날 하루, 폴백 디렉터는 시드 결정론) · **특성·이름**(채용 서수 파생이라 동명이인 구조적 불가 — `nextId` 인덱싱은 실제로 겹쳤다) · **스토리텔러 표면**(`narrative.ts` 한국어 폴백 원고가 **먼저** 완성 · 이벤트 카드·사직 편지·엔딩 결말문 · `formatManwon` 단위 혼재 해소) · **LLM 프록시**(`/api/storyteller` — 키는 서버에만, 경계 세 겹(프롬프트→enum→`eligible` 대조), 모든 실패가 한 경로로 폴백 강등 + HUD 배지. **실호출 레이어까지 섰고 키 등록만 대기**) · **랜딩 스왑**(`/` = 타일 병원 · 옛 판 `/classic` 코드 무변경 · `/sim` 호환 유지) · **제출 문서 ③④ 재작성**(심사자가 여는 화면과 읽는 문서가 같은 게임이 됐다). 밸런스 `EMERGENCY_INTENSITY` 1.7·`STARVED_SLOW` 1.3. **1062 vitest**·tsc 0·eslint 0·양 빌드·`src/game` 로직 0줄(주석 1줄 — salt 레지스트리 등재). 함정 3건([T-095](troubleshooting/T-095.md)~[T-097](troubleshooting/T-097.md)). 상세 changeLog·[플랜](../docs/superpowers/plans/2026-07-28-rimworld-week2-storyteller-submission.md)
    - ⏸ **jsdom UI 배선 테스트 — 이번에도 보류**(PR A부터 2회 이월). 사유: **제출 크리티컬 패스(문서·영상) 우선**. 이 저장소는 DOM 테스트가 0건이라 JSX 안의 판정은 어떤 테스트도 못 겨누고, 그래서 표시 판정을 `simHud.ts`로 내리는 우회를 계속 쓰고 있다(PR C에서 확립). 스토리텔러 UI(이벤트 카드·편지·에필로그)는 최종 게이트의 브라우저 실측이 덮는다. 도입하면 잠글 대상: `effectiveSpeed` 배선 · 결산 1회 가드 · 오버레이 `paused` 합류
    - ⏸ **사직 파동 이벤트 — 절단**(스펙 §4의 이벤트 후보). 사직은 이미 코어 규칙(포화 4일)이라 이벤트가 강제 사직을 얹으면 **「통지 = 집행」 단일 출처 계약**(`resigningSimDoctors`)이 깨진다 — 결산이 통지한 명단과 다음 주가 집행하는 명단이 갈리는 순간 그 화면은 거짓말이 된다. 본선 후보
    - ⏸ **특성의 수치 효과 — 본선**. 이번 PR의 특성은 **서사 재료**(사직 편지·연출문·PriorityPanel 라벨)뿐이다. PR C가 스트레스를 절단한 것과 같은 근거 — **효과 없는 셋째 축은 새 결정을 안 낳고**, 개성 상호작용이 붙어야 뜻이 생긴다
    - ⏸ **환자 항의문 — 본선**(스펙 §4의 폰 서사 표면 잔여). 사직 편지와 **같은 기계의 반복**이라 새 구조를 안 낳는다
    - ⏸ **식당 다음 손잡이** — `MEAL_MIN`↓ · `HUNGRY_AFTER_MIN` · 「유휴일 때만 먹는다」 규칙. 이번에 `STARVED_SLOW`를 1.3으로 올려 **식당의 가치가 처리량이 아니라 「포화 축 보호」임이 실측**됐으므로(굶은 채 시작한 응급 117분 → 부하 198.9 > 160 단독 초과), 추가 손잡이는 그 축을 봤을 때 다시 만진다
    - 🔑 **사용자 몫 — 코드로 못 하는 것**: ✅ ⓐ **Vercel 환경변수 `ANTHROPIC_API_KEY` 등록**(2026-07-28 완료) → ✅ ⓑ **실호출 실측**(2026-07-28 — 모델 3종 × 작업 3종 지연·토큰 + 프로덕션 3작업 요청↔응답 원문 + 판당 비용. ④ 문서 §6-4에 기입 완료. **이 실측이 기본 모델의 잠복 결함을 드러냈다** — `claude-opus-5`는 letter·epilogue가 9초를 넘겨 항상 폴백이었다 → 기본값 `claude-sonnet-5`) + ✅ ⓒ **Vercel Firewall rate-limit 룰**(`/api/storyteller` · 600초/IP당 10회) + ✅ ⓓ **Anthropic 콘솔 지출 상한**($10/월 · 자동충전 꺼짐) — ⚠️ **CORS는 인증이 아니다**(`curl -H Origin:` 한 줄로 위조된다). ⓒⓓ가 코드 밖에 있는 유일한 최종 방어선이다 ⬜ ⓔ **스크린샷 7장 재캡처**(본편 `/` 기준 — 현재 실린 것은 옛 판 화면이고 문서가 스스로 그렇게 표식하고 있다. [T-034](troubleshooting/T-034.md)로 사용자 로컬 화면에서)
    - ⚠️ **ai-usage-directing.html 갱신 필요** — ④ 문서의 HTML 판본이 이번 재작성(§2 「같은 경계를 두 번째로 그었다」·§6-4 실측/미실측 분리·§3-1 집계)을 아직 안 담았다
    - ⚠️ **ACAO 반사(`Access-Control-Allow-Origin`) 재검토** — 현재 응답은 허용 목록에 맞으면 그 오리진을 그대로 반사하고 `Vary: Origin`을 단다. Vercel 안에서는 배포 도메인이 고정이라 안전하지만, **호스팅을 옮기거나 프리뷰 도메인이 늘면** 반사 대상을 다시 좁혀야 한다
  - ⬜ **이월 항목**(1주차 리뷰가 남김): 스페이스바 일시정지·9~19시 운영 시간대(스펙 §1 — 1주차 계획에 미배정, 버튼·0..480분만 구현) · 의사 재홈 없음(고립 방에 갇히면 영구) · ~~철거 없음~~(→ 림월드식 PR 2에서 철거 도구 신설) · `/sim` 진입 링크 없음 · RECEPTION 수납 흐름(`PAYING`/`GONE` 예약) · 표시층 상호작용 테스트 0건 · 터치 가로 스크롤 불가 · 대각선 보간이 벽 모서리를 시각적으로 스침 · `Skills-used` 트레일러가 Task 1~2 커밋에서 과다보고(템플릿 오류 — Task 3부터 `none`으로 정정, 집계 시 참고).
- 🔜 **간호사 축 1단계 — 수납 + 간호등급** (사용자 결정 2026-07-29: 「최소+N1만 · N2는 시간 남으면 · N3는 문서로만」 · 설계 [spec](../docs/superpowers/specs/2026-07-29-nurse-payment-grade-design.md) · Fable 설계 → implementer 위임):
  - ✅ **PR ① 간호사 폰 + 수납** (2026-07-30 완료): `'NURSE'` 신설(주급 300 · 피로/사직 비합류가 계약) · `hasCashier` 단일 출처 · PAYING 도착 분 계상 · 미수 `unpaidManwon` 결산/경고 · 체크리스트 6단계. 회귀 픽스처 3차 재핀(환자 흐름 3축 불변 · revenue −12는 걷는 중). **1297 vitest**(+31) · [T-108](troubleshooting/T-108.md). 상세 changeLog
  - ✅ **PR ② 간호등급 가감산** (2026-07-30 완료): `nurseGradeOf` 3단(필요 = ceil(의사/2) · BONUS +10%/MET 0/SHORT −10% · 의사 0이면 MET 고정) · `nursing` 블록에 `grade`·`adjustManwon` · 금고 청구를 수익−순익 유도로 접음. **밸런스 창발 보존**: 감산 229 < 주급 300이라 필수과 병원은 미달 감수가 유리, 미용 병원(주 ~3,900)은 역전 — "벌은 못 버는 병원이 받는다"가 상수 조정 없이 성립. **1309 vitest**(+12) · 돌연변이 9종 전원 사살. 상세 changeLog
  - ✅ **문서 PR — game-intro 간호 축 반영 + N3(PA) 문서화** (2026-07-30 완료): §2 루프도·§3-1 접수처 행·§3-2 간호사 채용·§3-6 간호 줄·§6 근거 표 1행(각색 고지 포함)·§7 본선 항목으로 PA("기능이 아니라 톤이 이유" — 가드레일 확정 후 본선에서). N3는 이로써 닫힘
  - ✅ **PR ③ 간호사 이탈(N2 · 사직 축만)** (2026-07-30 완료 · 설계 [spec](../docs/superpowers/specs/2026-07-30-nurse-attrition-design.md)): `shortDays`(SHORT로 마감한 날 · 회복 없음) · `resigningNurses`(기존 4일/신규 2일 — 신규 가중이 "재채용으로는 안 풀린다"를 잠금) · 집행은 `startNextWeek` 같은 자리, 단 의사와 달리 **유휴로 돌아간다**(`nursesResignedTotal` 장부 · 채용은 계속 무제한 — "사람이 없다"가 아니라 "잔류가 안 된다") · 카드 배지 + 결산 사직 줄. **1346 vitest**(+37) · 돌연변이 13종 전원 사살 · 회귀 픽스처 무수정 통과. 상세 changeLog
  - ⏸ N2 잔여(간호사 피로·욕구·휴게실) — 사직 드라이버가 개인 피로가 아니라 배치 조건인 것이 메시지라 **의도적 보류**(보류 근거는 스펙 「왜 사직만인가」가 단일 출처)
- ✅ **실플레이 후속 2건 — 간호 기준 표시 + 소송 합의금 가시화** (2026-08-03 · 12주 실플레이 관측에서 · implementer 위임): ① 채용 패널 간호사 줄 「현재 N명 · **기준 M명**」(미달 시 경고색+툴팁 — 산식은 `nurseGradeOf` 결과째, 화면이 세지 않는다) — 간호등급 감산(−314만 실측)의 **사후 발견 제거** ② `event.costManwon` 이름표 + `lawsuitManwonWeek` 주간 누계 → 이벤트 카드에 합의금 액수·주간 결산에 「의료소송 합의금 **(발생 시 차감)**」 정보 줄. **netManwon에 넣지 않는 것이 계약**(이미 차감 — 넣으면 이중 차감, 테스트가 잠금). **1,606 vitest**(+10) · 돌연변이 6종 사살 · 브라우저 실측. 상세 changeLog
  - ⏸ 실플레이 관측 잔여(대기 포화 배너·이벤트 카드 스탯 줄·연속 진행/고배속·패널 높이) — 메모리 `ui-ux-playtest-observations`가 단일 출처. 마감(8/10) 대비 우선순위 낮음으로 보류
- ✅ **채용 패널을 전국 인력 시장으로 — 후보 실체화 + 카드 UI** (2026-08-03 · SDD 4태스크 + 리뷰 · 설계 [spec](../docs/superpowers/specs/2026-08-03-talent-market-design.md) · [플랜](../docs/superpowers/plans/2026-08-03-talent-market.md) · 사용자 보고 *"인력풀이 너무 적어 보여 볼품없다"*): 풀 숫자(미용 8·내과 5·외과 3·순환기 2)가 게임의 논지 셋을 지는 의도값인데 **회색 숫자 한 칸으로만 서 있어 의도된 희소성이 아니라 콘텐츠 부족으로 읽혔다.** 사용자 결정으로 **숫자는 그대로, 표현만** 바꿨다. 새 leaf 모듈 `candidate.ts`가 `(과, 슬롯) → 사람`을 **순수 함수**로 주고(RNG 0 · 이름은 전국 풀 기준 전역 분할이라 과가 달라도 안 겹친다), `SimWorld.hiredSlots`가 소비 슬롯을 들어 `hireDoctor(w, dept, slot)`이 **정확히 그 사람**을 세운다. HirePanel은 과별 4열 카드 그리드로 — **열 높이 차이 자체가 인력난 시각화다**(미용 8장 vs 순환기 2장). 새 이미지 에셋 0개, 초상 키를 이름으로 통일해 카드의 그 얼굴이 부지에도 선다. **밸런스·규칙 무변**(풀·주급·사직·지역 델타 전부 그대로, 이름·특성은 시드를 안 쓴다). **1596 vitest**(+11)·tsc 0·eslint 0 error·브라우저 실측. [T-148](troubleshooting/T-148.md)
  - **리뷰 후속 — `slot`을 필수로 올렸다**: optional 폴백이 있으면 화면이 인자를 떨어뜨려도 **tsc·테스트 1596건이 전원 통과**하면서 카드에서 고른 사람이 아닌 다른 사람이 조용히 선다(이 슬라이스의 논지가 어떤 계측기에도 안 걸리는 유일한 경로). 필수화로 그 회귀가 **컴파일 에러**가 된다(돌연변이 실측 `TS2554`). 폴백은 손세계 픽스처용 `testHelpers.hire`로 이동 — 프로덕션은 사람을 지목하고 테스트만 아무나 뽑는다
  - **초록불이 컴파일을 뜻하지 않았다** ([T-148](troubleshooting/T-148.md)) — 빈 배열 표 한 줄이 **vitest 1596건 전건 초록인 채 tsc에서만 TS2352**로 죽었다. 런타임 동작이 정확해서 테스트는 **원리적으로** 못 잡는다. CI는 build가 같은 줄에서 죽어 안 뚫리지만, 로컬에서 `npm test`만 돌리면 타입 에러가 초록 아래 누적된다
  - ⏸ **후보 리롤·재보충·스카우트** — 스펙 §5에서 명시적으로 뺐다. 새 결정을 낳지 않는 메커니즘이라 이번 범위 밖
  - ⏸ **간호사 후보 카드화** — 풀이 없는 제약(무제한 채용)을 카드로 흉내 내면 화면이 거짓말을 한다. 하단 고정 줄 그대로
- ✅ **시작 프레이밍 — 배경이 한 겹 도는 배율로 연다 (아트 디렉션 ⑤ · 5단계 완결)** (2026-08-01 · SDD 3태스크 · 설계 [spec](../docs/superpowers/specs/2026-08-01-camera-framing-design.md) · [플랜](../docs/superpowers/plans/2026-08-01-camera-framing.md)): 배경 12종을 낮으로 재대역한 직후 재 보니 **그 배경이 기본 화면의 3.8%만** 차지했다 — `zoom 1`이 정의상 *"부지 전체가 안전 영역에 맞는 배율"*이라 부지가 화면을 꽉 채우기 때문이다. 지역 선택이 이 게임의 첫 결정인데(랜딩 17곳 → 4타입) 그 보상이 기본 프레임에서 잘려 나갔다. **기제는 한 줄도 안 고쳤다** — 줌·팬·클램프·하한은 정확히 동작했고 바꾼 것은 「어디서 시작하나」라는 정책 하나다. 시작 배율을 매직 넘버가 아니라 **「부지 둘레로 배경이 2타일 보이는 배율」**로 창에서 파생시켰고, `fit`이 짧은 축에서 결정되므로 식이 접혀 **창 형상과 무관하게 [512/576, 768/832]에 갇힌다**(1을 넘을 수 없어 상한이 불필요). `⌂`도 zoom 1이 아니라 같은 프레임으로 돌아온다 — 갈리면 화면에 「기본 화면」이 두 개 생긴다. **1585 vitest**(+5)·tsc 0·eslint 0 error·브라우저 실측(1280×720 · 1440×900 두 창에서 scale이 기대와 정확히 일치 · 여백 정확히 2타일 · `⌂` 복귀). [T-147](troubleshooting/T-147.md)
  - **적용 시점을 「첫 측정 때만」으로 적었다가 고쳤다** — `insets`는 부모(SimGame)가 effect에서 재는데 **React가 자식 effect를 먼저 돌려** TileMap의 첫 측정은 언제나 인셋 0인 상태로 일어난다. 규칙을 「**플레이어가 카메라를 건드리기 전까지**」로 바꾸니 정확해졌고, 구현도 한 곳으로 접혔다 — `useCamera`가 돌려주는 `setCam`을 건드림 표시 래퍼로 감싸면 휠·줌 버튼·⌂·팬이 전부 그걸 써서 **호출부를 하나도 안 고친다**
  - **계약 하나가 성립 불가였다** — 「하한과 함께 쓰면 배경이 뷰포트를 덮는다」가 48조합 중 `600×900`+인셋`120/320`에서 실패했다. 덮는 데 zoom 3.214가 필요한데 `ZOOM_MAX`가 3이라 **어떤 배율로도 못 덮는 창**이고, `zoomFloor` 자기 주석이 이미 적어 둔 천장이다. 구현자가 예외 조건 + `measured > 40` 카운트(예외가 전수를 삼키지 못하게 하는 positive control)로 처리
  - **그 단언이 사실은 비유한값만 문다** — `999`·`−1`·`0` 전부 생존, `NaN`만 사망. `settledCamera`가 하한을 받치고 `clampCamera`가 커버리지를 강제하므로 **유한하면 무엇이든 통과**한다. 구현자가 커밋 전에 실측해 주석·제목을 실제로 무는 것에 맞췄다([T-144](troubleshooting/T-144.md)·[T-145](troubleshooting/T-145.md) 계열을 이번엔 커밋 전에)
  - ⬜ **극단 형상 창의 커버리지 천장** — `600×900`처럼 배경 비율(72×48타일)을 크게 벗어난 창은 `ZOOM_MAX`에서도 페이지 공백이 남는다. 이 브랜치가 만든 것이 아니라 원래 있던 것이고, 올리려면 `BACKDROP_MARGIN`(배경 그림 범위)을 키워야 해 **아트 작업**이다
  - ⏸ **건설 중 자동 추적·컷신 카메라·지역별 차등 배율** — 스펙 §7·§9에서 명시적으로 뺐다. 카메라가 플레이어 손을 뺏는 순간 조작 감각이 통째로 바뀐다
- ✅ **의자 변종 — 플레이어가 고르는 가구 (아트 디렉션 ②-a)** (2026-08-01 · SDD 5태스크 + 리뷰 · 설계 [spec](../docs/superpowers/specs/2026-07-31-chair-variants-design.md) · [플랜](../docs/superpowers/plans/2026-08-01-chair-variants.md) · 사용자 지시 *"림월드를 보면 의자 종류가 여러개고 사용자가 골라서 넣는거거든"*): 대기실·휴게실·식당이 전부 같은 `CHAIR` 그림이던 것을 **플레이어가 고르는 5종**(스툴·플라스틱·벤치·소파·리클라이너)으로 갈랐다. **`FurnitureKind`를 안 쪼갠 것이 이 설계의 전부다** — `kind === 'CHAIR'` 검사가 프로덕션에만 여덟 곳이라 하나만 빠뜨려도 「앉을 수 없는 소파」가 에러 없이 생기는데, 선택 필드 `variant`는 그 여덟 곳을 **수정 0곳**으로 통과한다(리뷰가 전수 확인). 도구는 8개 그대로이고 [의자]를 고르면 아래에 종류 줄이 열린다. **시뮬은 이 필드를 한 번도 안 읽는다** — 그게 「외형만 다르다」(②-a)를 규칙이 아니라 구조로 보장한다. **1580 vitest**(+12)·tsc 0·eslint 0·브라우저 실측. [T-146](troubleshooting/T-146.md)
  - **아트를 코드 한 줄 쓰기 전에 닫았다** — 팔레트 20색을 휘도·색상으로 검산하고 5종×6크기를 4개 바닥 위에 래스터했다. 그 과정에서 **시안 둘이 죽었다**: 소파 팔걸이를 몸체색으로 두면 24px에서 녹색 덩어리였고(강조색으로 → 등쿠션과 이어진 U자 천), 무채색 벤치는 스툴과의 분리를 **채도 없는 색상**에 떠맡겨 계약이 헛돌았다([T-144](troubleshooting/T-144.md) 형태 → 채도 15% 청회색). 램프도 채널 곱에서 **휘도 가산**으로 바꿨다 — 곱하면 밝은 몸체에서 강조가 사람 톤(간호사 144.8)을 넘어간다
  - **벤치는 이름이 실제가 됐다** — 타일 폭을 꽉 채우고 **좌우 잉크선을 안 그어** 나란히 놓으면 하나로 이어진다(8칸 실측: 몸체·강조·좌면 rect가 전부 `x0 w16`·`stroke` 없음, 잉크는 가로 3줄뿐)
  - **테스트가 못 겨누는 축을 브라우저가 닫았다** — 좌면 도형·`placeFurniture` 전달·두 스프라이트 prop·`seatTiles` 값, 넷 다 돌연변이를 심어도 1580건 전건 통과다. 실제로 놓아 DOM을 세니 **가구층과 좌면 오버레이의 변종별 개수가 정확히 일치**했다(리클라이너 5·스툴 5·플라스틱 5·벤치 13·소파 5) — 배선이 틀렸으면 오버레이 33개가 전부 플라스틱이었다
  - **계획서가 세 곳 틀렸고 전부 리뷰·실측이 잡았다**(플랜 §3에 실행 기록) — ⓐ 「`undefined`를 채우면 깊은 비교 회귀가 깨진다」는 **반대**였다(`toEqual`이 undefined 키를 무시한다 → `Object.keys` 단언이 유일 가드) ⓑ 「가격 무관」 테스트가 기준선을 안 물어 `placeFurniture`가 통째로 죽어도 초록이었다 ⓒ 소파 팔걸이를 `rx="1.5"`로만 고치면 `ry`가 1.5를 상속해 **그림이 실제로 바뀐다**(구현자가 옳게 벗어나 `ry="2.4"`를 함께 명시)
  - ⏸ **②-b 안락함** — 수치가 붙으면 밸런스(피로 회복·대기 이탈)와 프로브 회귀가 따라온다. 스펙 §6이 분리한 그대로. 그때 비싼 의자를 고를 이유가 생긴다
  - ⬜ **장식 오브젝트**(통행 무관·`world.furniture` 밖) — 방 종류별 자동 배치로 화면을 채우는 별개 기제. ②의 나머지
  - ⏸ **놓은 뒤 변종 바꾸기** — 철거 후 재설치로 된다. 붙이려면 `TileMap`의 React key(`${f.kind}-${f.x}-${f.y}`)에 변종을 넣어야 한다(안 넣으면 같은 key로 DOM이 재사용돼 그림이 안 바뀐다)
- ✅ **배경 12종 낮 전환 — 아트 디렉션 ③** (2026-08-01 · SDD 5태스크 + 2단 리뷰 · 설계 [spec](../docs/superpowers/specs/2026-08-01-backdrop-daylight-design.md) · [플랜](../docs/superpowers/plans/2026-08-01-backdrop-daylight.md) · 사용자 지시 *"③ 배경 12종 재대역 하자"*): **진단부터 틀려 있었다** — plan이 「보라 기미」로 적어 둔 것을 실행 화면의 캔버스 픽셀로 재니 12장면이 **전부 야경**이었다(별·불 켜진 창·가로등, 배경 평균 휘도 60~68 vs 부지 176). 사용자 결정으로 **낮 전환 + 지역별 톤 분화**. 팔레트 65색을 새 대역으로 교체(지면 63.5 → 90.1로 끌어올려 스프레드를 79 → 52로 **좁혔다** — 지금까지 세 번과 방향이 반대다), 밤 장치를 낮 장치로(창불·전조등 → 유리에 튄 햇빛, 알파 1/3 · 가로등 지면 웅덩이 제거 · 비네트 검정 50% → 중성 22% · 어두운 바닥 위 밝은 선이던 크림색 디테일을 반전), **색조를 처음으로 잠갔다**. 그리기 함수·좌표·구도·부지 천장 무변. **1557 vitest**·tsc 0·eslint 0 error. [T-144](troubleshooting/T-144.md)
  - **실측(캔버스 픽셀, 변형 2)**: 도심 보라 **74.8% → 0%** · 무채색 **0.5% → 99.3%** · 평균 휘도 67.9 → 99.7 · 발광이 하얗게 탄 자리 0%. 농어촌 초록 65.3% · 보라 0%. 래스터 면적 집계로는 도심 변형 0이 **84% 보라**였고 옥상 하나(`roofBase`)가 화면의 48%를 혼자 덮고 있었다
  - **리뷰가 잡은 가장 값진 것 — 지역 분리 단언이 옛 보라 팔레트에서도 통과했다.** 도심이 「가장 무채색」으로 나온 이유가 무채색이라서가 아니라 **무채색 키가 지역마다 1개씩인데 도심의 분모가 가장 작아서**였다(순전한 분모 사고). 서열(`>`)을 **최소 격차**로 바꿔 옛 팔레트가 5건 FAIL로 죽는 것을 확인했다([T-144](troubleshooting/T-144.md))
  - **계측기 구멍 둘도 함께 닫았다** — ⓐ 요구 간격이 리터럴이라 무계측(27.8을 20.0으로 낮춰도 1542건 전건 초록)이던 것을 **구간표 중점에서 파생**시켜 「대역과 간격은 같은 커밋에서」를 규약이 아니라 **구조**로 강제(돌연변이 실측: 파생이 **양방향**으로 문다 — 한 대역을 내리면 위 대역과의 요구 간격이 자동으로 커진다) ⓑ 대역 여유가 0.03까지 붕괴했던 것을 양쪽 2씩 되살렸다
  - **보라를 250°가 아니라 230°부터 막는다** — 지각 공간에서 순수 파랑이 264°라, HSL 250~310으로 세면 `sidewalk`(248°)·`roofEdge`·`roofVent`(240°)가 빠져나간다. 팔레트 주석이 「인도가 보라색이 된다」고 스스로 지목한 바로 그 키다. 돌연변이로 248° 값이 실제로 잡히는 것을 확인
  - ⬜ **계측기가 면적맹이다** — 테스트 스텁이 fill 색을 `Set`에 담아 **면적이 아니라 색의 종류 수**를 센다. 그래서 ⓐ 「화면의 84%가 보라」류 면적 주장을 이 파일이 재현하지 못하고 ⓑ **「신도시가 지방보다 따뜻하다」(warm 0.357 vs 0.273)는 실측이 스펙 §3의 「지방=황토」와 어긋나는데 진위를 가릴 수 없다**(신도시가 공사장을 그려 따뜻한 키 *가짓수*가 늘었을 뿐, 면적으로는 지방의 황토 논밭이 넓을 가능성이 크다). 고치려면 스텁이 `fillRect(x,y,w,h)`를 누적해 색별 면적을 세면 되고, 그러면 지역 분리 단언 7건이 전부 면적 단위로 다시 서야 한다
  - **돌연변이가 테스트 삭제를 막았다** ([T-145](troubleshooting/T-145.md)) — 가로등 지면 웅덩이를 없애자 「가로등이 방사형 그라디언트를 만든다」 테스트가 겨누던 대상이 사라져 보여 **삭제를 지시했는데**, 실행자가 먼저 승계를 실측하니 「발광 패스」 테스트는 `composites`만 단언하고 **그라디언트는 아예 안 봤다**. 지웠으면 발광 감쇠 전체가 무방비가 됐다. *두 테스트가 같은 코드 구간을 지나는 것은 겹침이 아니다 — 겹침은 단언하는 값이 정하고, 판정 수단은 돌연변이뿐이다*
  - ⏸ **비네트가 무계측** — 알파 0.22를 0.9로 바꿔도 1557건 전건 통과한다. 브라우저 실측이 유일한 게이트인 자리다
  - ⏸ 시간대별 배경 전환(아침/낮/저녁이 게임 시계를 따라감) — 스펙 §6에서 명시적으로 뺐다. 이번엔 낮 한 장면 고정
- ✅ **HUD 밝은 전환 — 아트 디렉션 ④** (2026-08-01 · SDD 3태스크 + 2단 리뷰 · 설계 [spec](../docs/superpowers/specs/2026-08-01-bright-hud-design.md) · [플랜](../docs/superpowers/plans/2026-08-01-bright-hud.md) · 사용자 지시 *"HUD도 밝게 바꾸자"*): **SimGame 루트에서 desk 토큰 6종을 `LANDING` 값으로 재정의**해 본편 UI 99곳이 **클래스 무변으로** 뒤집혔다 — `@theme inline`이 유틸리티에 `var(--desk)`를 직접 심어(리뷰가 산출 CSS로 실증) 커스텀 프로퍼티가 사용 지점에서 해석되기 때문이다. `/classic`(20파일·284곳)은 `:root`의 어두운 값 그대로고 `globals.css`는 무변. 경고색은 `alarmDeep`(#b02a30 — 카드 5.43·배경 4.71) 신설, 청록은 진행 확정 버튼 3곳(개원 준비 시작·다음 날·다음 주)에만. **1542 vitest**·tsc 0·eslint 0 error·브라우저 전 화면 검수. [T-143](troubleshooting/T-143.md)
  - **검수가 잡은 defect 1건** — 전역 `html { color-scheme: dark }`가 남아 밝은 패널의 UA 스크롤바·선택 하이라이트가 어두웠다(`getComputedStyle(main).colorScheme === "dark"` 실측). 루트 style `colorScheme: "light"` 한 줄로 해소(상속 속성이라 스크롤 컨테이너 6곳이 따라온다).
  - ⏸ **딤(`bg-desk/85`)이 밝은 안개가 된다** — 카드와의 휘도 차가 1.1~1.2로 얇다. 종이 카드의 따뜻한 색조 + 드롭섀도가 분리를 지어 실화면에서는 카드로 읽히므로 토큰 6종만 재정의한다는 원칙을 지켰다. 더 강한 모달 초점이 필요해지면 딤만 별도 어두운 값으로 뺀다
  - ⏸ 9px 지도 라벨(`TileMap:596` `text-on-desk/70`)이 3.35~4.17:1 — #178 이후 1.03에서 크게 올랐지만 소형 텍스트 4.5에는 못 미친다. `/70` 알파를 떼면 6.06~8.94. 이번 범위 밖
- ✅ **랜딩(지역 선택) 밝은 재설계 — 종이 위 잉크 지도** (2026-07-31 · SDD 3태스크 + 2단 리뷰 · 설계 [spec](../docs/superpowers/specs/2026-07-31-bright-landing-design.md) · [플랜](../docs/superpowers/plans/2026-07-31-bright-landing.md) · 사용자 지시 *"랜딩페이지 디자인 변경하자. 지금까지 변경한 것과 통일성 있게"*): 본편이 밝은 병원 톤이 됐는데 첫 화면만 어두운 desk 토큰 위에 있던 것을 뒤집었다 — 극성 반전(**어두울수록 내용**: 잉크 < 육지 < 배경 < 카드), 85% 딤 → 불투명 밝은 배경, 유채색은 수술복 청록 한 점(선택·확정만 · 간호사 코트 #4fa39e에서 흰 글자 4.5:1을 위해 어둡게 내림). 값은 `landingPalette.ts`가 단일 출처, 서열·간격·WCAG·계측기 고정점은 `landingPalette.test.ts`가 잠금. desk 토큰·레이아웃·`HIT_SPAN` 불변식 무변. **1541 vitest**·tsc 0·브라우저 실측(hover 픽셀 `#2f736e`가 계산과 일치 — sRGB 필터 전제 확정 · 키보드 포커스 잉크 아웃라인 · 확정→게임 진입). [T-142](troubleshooting/T-142.md)
  - **리뷰가 잡은 것 둘** — ⓐ `hover:brightness-110`이 테스트가 잠근 4.5:1을 런타임에 우회(→ 어둡게 하는 방향 `brightness-95`로 — [T-142](troubleshooting/T-142.md)) ⓑ 밝은 육지 위에서 청록 포커스 점(2.18)이 쉬는 잉크 점(5.62)보다 흐려 **포커스 표식이 역전** → `focus:outline-none`을 잉크 아웃라인으로 교체(선택된 점의 포커스 무표시 공백까지 닫힘)
  - ⏸ 개원 준비(채용) 다이얼로그·HUD는 여전히 어둡다 — 아트 디렉션 ④의 몫(위 항목 ⏸와 동일)
- ✅ **자유 영역 지정 — 벽·문 밀폐에서 해방** (2026-07-31 · SDD 4태스크 + 2단 리뷰 · 설계 [spec](../docs/superpowers/specs/2026-07-31-free-zone-designation-design.md) · [플랜](../docs/superpowers/plans/2026-07-31-free-zone-designation.md) · 사용자 지적 *"접수처는 대기실 바로 앞에 공개되어 있는데 여기에 문을 다는 건 어색해"*): 원천을 앵커(`designations`)에서 **칠한 타일**(`zones: ReadonlyMap<타일, ZonePaint>`)로 바꾸고, 영역은 여전히 파생하되 정의가 「벽·문 밀폐 성분」 → 「같은 (type·dept) 칠의 4방 연결 성분」이 됐다(병합·분할이 공짜). `OUTDOORS`·`Region.doors`·밀폐 판정 삭제 · `Region.type` 필수 승격 · `designateRegion` → `paintZone`/`eraseZone`(벽·문 타일은 건너뜀, 가구 타일은 칠해짐 — 가구는 영역 **안**의 것) · DESIGNATE가 클릭 도구 → **드래그 도구** + 「지정 해제」(UI 전용 `'ERASE'`가 코어에 안 새는 것을 tsc가 강제) · 밀실 경고 → **입구 도달성** 경고. **1536 vitest**·tsc 0·eslint 0 error·build 통과 · **프로브 12주 장부 무변**(`placeRoom`이 사각형 전체를 칠해도 벽·문을 건너뛰어 옛 내부 성분과 같은 집합). [T-141](troubleshooting/T-141.md)
  - **리뷰가 잡은 신규 실패 모드** — 파생 정의를 바꾸자 옛 정의가 가리던 자리가 열렸다: 행 넘김(x-wrap) 가드가 무계측(가드 제거 돌연변이가 564건 전건 생존)이었고, 「막는 가구로 덮인 영역」을 고립으로 오탐해 체크리스트의 뒤 경고를 전부 가렸다. 둘 다 옛 정의에서는 **입력 자체가 존재할 수 없어** 안 보이던 것이다([T-141](troubleshooting/T-141.md))
  - **계획서가 틀렸던 두 곳**(플랜 문서에 실행 기록으로 남김) — Task 3의 *"드래그 파이프라인은 손대지 않는다"* 전제가 틀렸다(`isDragTool`이 도구를 열거하는 구조라 DESIGNATE를 더해야 했다) · Task 2의 「죽은 방어 정리」가 지목한 sim 4파일에서 실측 **0건**(커밋 없이 종료)
  - ⬜ **한 타일 영역의 유효성** (스펙 §8 미해결) — 카운터 한 칸만 접수처로 칠하는 것이 규칙상 유효하다. 슬롯 파생은 정상 동작함을 확인했고(`cashierSlots` 3), 어색하면 최소 크기 제약을 단다. 지금 안 다는 이유: 제약을 걸면 "열린 접수처"라는 이번 설계의 존재 이유와 충돌할 수 있어 실플레이 판단이 먼저다
  - ⬜ **도달성 판정 비용** (스펙 §8 미해결) — `setupSteps`가 ENTRANCE에서 전 격자(1,536타일) flood fill을 한 번 돌고, 그게 **매 렌더 두 번** 일어난다(`SimGame`이 직접 한 번 + `alertsOf` → `setupAlert` → `setupSteps`로 한 번 — 후자는 첫 미완 하나를 얻자고 전부를 다시 계산한다). 실측 지연은 안 재 봤고 화면도 안 끊겨 이번엔 안 달았다. 처방은 정해져 있다: `computeRegions` memo와 같은 **참조 키 방식**(`walls`·`furniture`·`zones` 참조)을 `reachableTiles`에 씌우거나, `alertsOf`가 이미 `regions`·`blocked`를 나눠 쓰듯 `setupSteps` 결과도 한 번만 계산해 넘긴다
- ✅ **밝은 아트 디렉션 ① — 밝은 병원 톤 + 3/4 인물** (2026-07-31 · TDD 8태스크 · 설계 [spec](../docs/superpowers/specs/2026-07-31-bright-art-direction-design.md) · [플랜](../docs/superpowers/plans/2026-07-31-bright-art-direction.md) · 사용자 방향 지시 *"병원의 대표 컬러는 흰색, 수술복 색"*): 배경 65색을 **RGB 채널 ×2.4**로 일괄 상승(휘도식이 RGB에 선형이라 색상비 보존 · 구간표와 **요구 간격을 같은 커밋에서** 올려 가드가 1/2.4로 느슨해지는 것을 막았다) + 부지·실내를 밝은 병원 톤으로(조명 모델을 「밝히지 않고 그늘만 진다」로 — `SHADE.max` 1.2 → 1.0) + 과 색 8종을 채도 낮춘 수술복 톤으로(무채색이던 미용·검진 → 금·녹) + **인물 3/4 시점 재작성**(과 색은 가슴·소매 커프스 두 곳) + 누운 환자만 탑다운 예외 + 앉은 폰이 의자에 앉아 보이게 좌면 앞단 오버레이. **1520 vitest**·tsc 0·eslint 0 error·build 통과 · 프로브 무변(바뀐 파일이 `src/components/*`뿐이라 프로브 임포트 그래프 밖). [T-139](troubleshooting/T-139.md)·[T-140](troubleshooting/T-140.md)
  - ⬜ **`HospitalMap`의 앉은 아바타도 의자를 삼킨다** — `TileMap`에서 고친 것과 **같은 결함**이 [HospitalMap.tsx](../src/components/HospitalMap.tsx)에 남아 있다. 의자가 14/16px인데 아바타가 20/24px로 **더 커서 같은 중심에** 겹쳐, 3/4 전환 뒤 의자가 어깨 뒤 후광만 남는다. `ReceivingPhase`·`SetupWizard`가 쓰는 살아 있는 화면이다. 좌표계가 백분율이라 `TileMap`의 타일 기반 해법(좌면 앞단 오버레이 + `SEAT_LIFT`)을 그대로 못 옮겨 별도 실측이 필요하다
  - ⬜ **환자 회색조가 두 곳에 적혀 있다** — `PatientSprite` 호출 인자와 `LyingPatientSprite` 리터럴. 설계 §7이 「환자 회색의 최종 위치」를 미해결로 열어 둬 그 이동이 예정돼 있는데, 지금 옮기면 누운 환자만 조용히 뒤처진다. 상수 하나로 묶는 것이 근본 해결
  - ⏸ **청진기·명찰의 확대 조건부 렌더링** — 설계 §7 미해결. 배율을 읽는 분기가 새로 필요해 이 슬라이스 범위 밖(②에서 판단)
  - ✅ **도시 배경의 보라 기미** — 위 「배경 12종 낮 전환」(2026-08-01)로 해소. ⚠️ **진단이 좁았다**: 「보라 기미」가 아니라 12장면이 전부 **야경**이었고, 도심은 화면의 최대 84%가 보라였다
  - ✅ **어두운 HUD가 밝아진 게임 화면과 충돌** — 위 「HUD 밝은 전환 — 아트 디렉션 ④」(2026-08-01)로 해소
  - ✅ **②-a 의자 변종 구현** — 위 「의자 변종 — 플레이어가 고르는 가구」(2026-08-01)로 해소
- ✅ **그래픽 격상 — 화면이 검정에서 나온다** (2026-07-30 · 설계 [spec](../docs/superpowers/specs/2026-07-30-graphics-uplift-design.md) · 사용자 판정 *"AI를 사용한 것치곤 너무 단순하다 · 옛날 테트리스 수준"* → 시안 비교 후 **코드 상한** 경로 선택, 이미지 에셋·CC0 도입은 기각): 실제 화면을 띄워 보니 원인이 8×8 해상도가 아니었다 — 부지·배경·실내가 전부 휘도 **7~26/255**에 갇혀 지역별 배경 12종이 화면에서 검정으로만 보였다. **1단계** 팔레트 60여 색을 서열·간격 그대로 **3~3.5배** 위로(부지 20.4 → **71.3** · 실내 바닥 92~104 · 벽 126~145) + 타일마다 **조명·AO 계수 하나**(`SHADE`·`shade` — 방 중심 광원·벽 접촉 AO·체커를 한 숫자로) + **벽 두께**(캡·몸통·밑동 3단 + 남쪽 트인 벽만 낙영) + 폰 접지 그림자 · 방 줄눈 · 배경 additive 발광(창문·간판·전조등)+비네트. **2단계** 8×8 격자 → **해상도 독립 벡터 SVG**(실루엣 하나 공유·색으로만 역할 구분·이미지 파일 0개는 계승 / 그라디언트 금지 — 폰마다 인라인 SVG라 id가 수십 개 겹친다 / 평면 2~3단 램프로 입체). **1487 vitest**·tsc 0·eslint 0 error·브라우저 실측(방 3개·집기·폰 4명 + 스프라이트 8종 160px 확대 검수). [T-115](troubleshooting/T-115.md)
  - **오버레이 레이어를 안 쓴 것이 이 구현의 핵심 결정**이다 — `mix-blend-mode: multiply` 한 겹이 가장 쉬운 답이지만 그건 폰 위의 **판독용 표시**(선택 링·피로 막대·활동 글리프)까지 눌러 어둡게 만들고 z 순서 싸움이 붙는다. 계수를 색에 **미리 곱하면** DOM이 한 겹도 안 늘고 HUD는 무변경이며, "사람이 시선을 먼저 받는다"는 기존 규칙도 자동으로 지켜진다(폰은 계수를 안 받는다)
  - **알베도와 최종 픽셀이 갈라졌다** — 팔레트 리터럴은 이제 음영 전 값이라, 리터럴만 보는 검사는 "최소 계수를 맞은 방 구석이 마당보다 어두워지는" 회귀를 통과시킨다. `shade` describe를 신설해 최종 픽셀 쪽 서열·단조성·비포화를 따로 잠갔다
  - **요구 간격도 함께 올렸다**(4.0/1.5/1.5 → 14/5/6) — 대역을 3.5배 넓히고 기준을 그대로 두면 통과 기준이 ¼로 느슨해져, 다음 사람이 전 항목을 다시 눌러 담아도 초록불이 뜬다(이 파일이 세 번 경고한 「준수되면서 목적만 증발」의 테스트판)
  - ⏸ **이미지 에셋(AI 생성·CC0) — 기각**: 페인터리·일러스트 풍은 코드로 안 나오지만, 마감 11일에 배경 12종 + 스프라이트 세트를 전면 교체하면 톤 일관성 검수에만 며칠이 간다. 코드 상한이 인상 차이의 대부분을 냈으므로 본선 후보
- ✅ **배경 톤 재조정 — 부지가 "구멍"이 아니라 무대가 되도록** (2026-07-30 · TDD · 배포 화면 실측: *병원 부지가 주변 배경보다 어두워 화면에 뚫린 구멍처럼 보인다* — 설계 의도와 정반대): 부지 바닥 `OUTSIDE_FLOOR`(#0d0d11 · 상대휘도 **13.3**)가 기준인데 배경이 전부 그보다 밝았다(풀밭 16.9 · 도로 21.4 · 숲 수관 47.7 · 나무 밝은면 **56.7**) — 시선이 병원이 아니라 배경으로 먼저 갔다. ① **팔레트 단일 출처** `BACKDROP_PALETTE` 신설 — 그리기 함수 19종에 리터럴로 흩어져 있던 hex 65개를 모듈 최상위 한 곳으로 모으고 그리기 함수는 참조만 한다(색이 흩어져 있으면 톤 조정이 산탄총 수술이다) ② **카테고리별 휘도 구간**으로 재조정 — 최종값은 부지 천장을 20.4로 올린 뒤의 것이다(아래 3차): 지면 base 7~9.5 · 지면 노이즈/논둑 10~12 · 도로 10.5~12.5 · 인도/산책로 13~15 · 건물 본체 13.5~16.5 · 건물 디테일 16.5~18.5 · 풀포기/관목 13~16 · **랜드마크 16.5~19**(수관·물·천창 불빛·벤치는 지역 식별의 핵심이라 배경의 상단 · 부지 20.43 아래). **색조(hue)는 유지** — 지역 구분은 밝기가 아니라 색조·패턴이 진다 ③ **도심 3종의 바닥을 잔디 → 포장면**(`pavement` 신설 · 저채도 회색 + 미세 노이즈 + 드문 이음매) — 도심인데 건물 사이가 초록이면 도심으로 안 읽힌다(urban1은 상단 2/3가 초록이었다). 호출 좌표는 그대로, 함수만 교체 ④ **가로등 불빛을 방사형 그라디언트로** — 단색 `arc`라 회색 원반(얼룩)으로 보였다 ⑤ 횡단보도 알파 0.20 → 0.13. **좌표·시드·구조는 한 줄도 안 바뀐다** — 이번 변경은 색과 위 3·4번뿐이다. **1393 vitest**(+32)·tsc 0·eslint 0 error·build 통과 · 돌연변이 15종 사살(1차 7 + 스프레드 5 + 천장 3)
  - **휘도 식은 프로덕션이 export하고 테스트가 그걸 쓴다**(`relativeLuminance`) — 테스트가 자기 식을 따로 들면 구현과 갈려 초록불이 아무 말도 안 한다. 기준값도 리터럴 13.3이 아니라 **`OUTSIDE_FLOOR`를 TileMap에서 import**해 파생한다
  - **돌연변이가 공허한 테스트를 하나 잡아냈다** — 처음 쓴 "모든 값이 부지보다 어둡다"는 상한(11/12/13)이 이미 부지(13.29)보다 낮아 **카테고리 검사와 완전히 중복**이었다: 상한을 13→14로 올리는 돌연변이가 살아남았다(값 자체는 그대로 어두우니 안 걸린다). **상한 자체를 부지에 묶는** 단언으로 고쳐 잡았다 — 「소프트 규칙은 불이행이 아니라 준수되면서 목적만 증발한다」가 테스트에서도 같은 모양으로 재발한 사례
  - **`@/*` 별칭을 vitest에 배선**(`vitest.config.ts`) — 없으면 `@/`로 import하는 **소스 파일**을 테스트가 아예 못 불러온다(테스트가 상대경로만 써도 소용없다 — 걸리는 건 대상 모듈이다). tsconfig `paths`와 같은 계약을 복제한 것
  - ✅ **눈으로 확인 → 과교정 발견 → 스프레드 복원** (2026-07-30 · 브라우저 실측): 서열은 고쳐졌는데(부지 13.29 > 배경 평균 9.8~10.9) **배경 내부의 밝기 스프레드가 함께 압축돼 형태가 안 읽혔다** — 지면 9.22와 건물 9.5~10.5의 차이가 0.3~1.3뿐이라 건물 윤곽이 지면에 묻혔고(도심 상단 2/3가 균일한 검정 · 아파트 동 소실 · 바다 거의 안 보임), 랜드마크만 19로 홀로 튀었다. **13.29 아래 구간을 넓게 쓰는 쪽으로 재조정** — 지면 5.5~7.5(평균 8.13 → 6.25) · 도로 8~9.5 · 인도 10~11.5 · 건물 본체 9~11.5(지면과 +3.97) · 디테일 ≤13(본체와 +1.83) · 랜드마크 11~13.2(평균 16.25 → 12.07). 좌표·시드·구조는 여전히 무수정(팔레트 hex와 테스트만)
  - **상한만 보는 테스트는 "다 같이 어두워짐"을 통과시킨다** — 그게 이번 회귀의 근인이다. 계측기를 세 겹으로: ⓐ 카테고리마다 **하한도 단언** ⓑ **평균 서열**(지면 < 건물 < 랜드마크 < 부지) ⓒ **최소 간격**(건물−지면 ≥ 4.0 · 인도−도로 ≥ 1.5 · 디테일−본체 ≥ 1.5 — 3차에서 새 대역에 맞춰 올림). ⓑⓒ는 상한 표가 아니라 팔레트 값에서 직접 파생하므로 **표가 통째로 느슨해져도 살아남는다** — 표를 감시하는 단언과 표를 안 거치는 단언을 함께 두는 형태
  - ✅ **3차 — 천장을 올린다: 부지를 밝혀 배경에 대역을 준다** (2026-07-30 · 사용자 결정): 2차로 서열·스프레드는 잡혔지만 **부지 13.29 아래 5~13이 배경이 가진 전부**였고, 그 좁은 대역에서는 지면·건물·물의 층위가 인지되지 않았다(브라우저 실측: 해안(바다)·대로변 상단이 거의 균일한 검정). 스프레드를 더 벌리려 해도 천장이 없어 못 벌린 것이라 **근본 해결은 천장을 올리는 것**이다. `OUTSIDE_FLOOR` #0d0d11(13.29) → **#14141a(20.43)** — 색조는 그대로 중성 남빛 그레이 · 배경 팔레트 65개를 새 대역에 hue 유지로 재분배(지면 8.25 · 도로 11.69 · 인도 13.87 · 본체 14.98 · 디테일 17.70 · 랜드마크 17.73). **반대쪽 경계가 이번에 열렸다** — 부지를 올리면 실내가 마당보다 어두워질 수 있다(`NEUTRAL_STYLE.floor` #131318 = 19.36 < 20.43): floor **#191920(25.5)** · wall #3a3a45(58.8)로 올리고, **"실내 바닥 > 부지"를 테스트가 잠갔다**(NEUTRAL + ROOM_STYLE 6종 전부). 격자선 알파도 부지에 딸려 간다 — 0.045로는 합성 대비 ΔL/L이 65%→41%로 떨어져 묻히므로 **0.065**(59% 복원). 좌표·시드·구조는 여전히 무수정
  - ✅ **12종 재검수 → 대로변(URBAN1) 상단 채움** (2026-07-30 · 브라우저 실측): 3차 이후 12종을 다시 훑어 층위가 읽히는 것을 확인했고, 그 과정에서 **대로변만 부지 북쪽 8줄(y 0~7)이 통째로 빈 포장면**인 것이 드러났다 — 색이 아니라 **배치** 문제라 톤 작업과 별개다. 부지 북쪽에 **대로 뒤 이면 블록**(상가 옥상 줄 → 인도 → 중앙선 없는 이면도로 → 인도)을 넣어 대로가 블록의 *가장자리*로 읽히게 했고, 좌우 필지 건물은 이 띠와 겹치지 않게 부지 높이(`LY`)에서 시작하도록 내렸다. 팔레트·좌표 규약 무변이라 색 테스트 32건은 그대로 통과(**1393 vitest**·tsc 0·eslint 0 error·build 통과), 상단 띠 실측 고유색 18종·비-바탕 픽셀 63%
  - ⏸ **HUD 대비는 미검증** — 부지가 화면 전체 배경이라 3차가 HUD 뒤 여백까지 밝혔다. 패널 면 대비는 61% → 12%로 떨어지고 테두리(88%)·`backdrop-blur`가 경계를 진다(계산값). 실사용에서 패널이 "위에 뜬 것"으로 읽히는지는 눈으로 볼 것
- ✅ **림월드식 축소 하한 — 배경 밖 공백을 화면에 들이지 않는다** (2026-07-30 · TDD · 사용자 신고 *"축소하면 배경 그림 바깥의 페이지 배경이 넓게 드러난다"*): 바로 아래 항목이 깐 배경(부지 + 좌우 12·상하 8타일)이 **카메라의 경계가 아니었다**. 하한이 상수 `ZOOM_MIN=0.5`라 창이 크거나 형상이 극단이면 배경이 화면보다 작아지는 배율까지 내려갔고, 그 밖은 페이지 공백이다. ① **여백 단일 출처** `BACKDROP_MARGIN`(타일)을 simHud로 올리고 Backdrop이 캔버스 크기·오프셋을 파생 — 그림 크기와 클램프 경계가 갈리면 배경을 넓힌 날 공백이 샌다 ② **커버리지 경성 제약**(`coverAxis`): 축마다 `x ≤ 여백px` · `x ≥ host − content − 여백px`로 기존 클램프 결과를 다시 자른다(뷰포트는 **바 밑까지 전부** — 바는 오버레이다) ③ **동적 하한** `zoomFloor` = 그 두 부등식이 동시에 성립하는 최소 배율 `max축(host / (base + 2·여백)) / fit`(ZOOM_MIN·ZOOM_MAX로 감쌈). 카메라 산술 4종이 낱개 인자 대신 **`CameraView` 한 벌**(host·safe·base·margin·fit)을 받는다 — 셋 다 `Size`라 순서를 바꿔 넘겨도 타입이 통과했다. **1361 vitest**(+12 · 성질/최소성 결정적 격자 스윕 5형상)·tsc 0·eslint 0 error·build 통과 · 돌연변이 4종 전원 사살
  - **커버리지가 안전 영역 중앙을 이긴다(계약 변경)** — 좌측 패널이 있는 창의 **축소 구간**에서는 둘이 양립하지 않는다: 안전 영역 중심이 뷰포트 중심보다 오른쪽인데 축소할수록 여백도 같이 줄어 중앙에 놓으면 왼쪽이 뚫린다. 가려진 타일은 팬으로 꺼낼 수 있지만 공백은 플레이어가 할 수 있는 일이 없어 커버리지를 골랐다. 기존 테스트 1건(좌측 인셋 중앙 정렬)을 그 계약으로 갱신했고, **줌인 구간(zoom ≥ 1.28)의 옛 동작은 회귀 테스트로 불변 확인**(T-102의 바 밑 미끄러짐 그대로)
  - ⏸ **배경 비율을 벗어난 극단 창**(예 300×3000)은 ZOOM_MAX에서도 다 못 덮는다 — 그때는 최대 줌에서 멈춘다(문서화된 천장 · 올리려면 `BACKDROP_MARGIN`을 키운다)
- ✅ **지역 선택 + 배경(backdrop)** (2026-07-30 · TDD · 사용자 컨셉 *"림월드가 지형을 고르듯 대한민국의 어느 지역에 병원을 지을지 고른다"* · 설계 [spec](../docs/superpowers/specs/2026-07-30-region-backdrop-design.md) · Fable 설계 → implementer 위임): 새 판 첫 화면에서 **지역 4타입**(도심/신도시/지방소도시/농어촌) 중 하나를 고르면, 그 지역의 배경 후보 3종 중 **하나가 무작위로** 뽑혀 부지 둘레에 정적 캔버스 한 겹으로 깔린다(12종 전부 게임에 들어간다 — 타입별 1안을 고르는 방식이 아니다). `SimRegionKey`·`SimWorld.region/backdrop` 신설, `createWorld(seed, start?)`는 **가산 인자**라 기존 호출부 전부 무변. `Backdrop.tsx`가 시안 HTML의 절차 생성 함수 19종 + 조립 12종을 이식(`T` 10 → `TILE` 16만 교체 — 좌표·색·시드 불변). **1349 vitest**(+3)·tsc 0·eslint 0·build 통과
  - **부지 안쪽은 `clearRect`로 뚫는다** — 캔버스가 transform div의 **첫 자식**이라 부모 배경(`OUTSIDE_FLOOR`+격자)보다 위에 깔린다. 설계는 "부지 안쪽은 안 그린다"로 적었지만 시안의 `grass(0,0,EW,EH)`가 캔버스 전면을 덮으므로 안 그리는 것만으로는 부족했다 — 마지막 한 줄이 그 구멍을 투명으로 되돌린다(브라우저 실측: 부지 44.4% = 1536/3456 타일이 정확히 alpha 0)
  - **이름을 기존 `RegionKey`와 공유하지 않는다** — `src/game/types.ts`의 CAPITAL/METRO/RURAL은 전국 세계 시뮬의 **권역**이고 이쪽은 **부지 입지**라 뜻이 다르다. `METRO`를 값에서도 뺐고, 규칙 매핑은 만들지 않았다(닿지 않는 해상도 금지)
  - ✅ **지역별 규칙 차이** — 바로 아래 항목에서 지불했다(고지문 삭제 포함). `src/game` 3계층과의 매핑은 **만들지 않았다**(그쪽은 classic 전용 · 닿지 않는 해상도 금지)
  - ✅ **선택 화면이 지도가 됐다** — 아래 「지역 선택이 지도가 된다」 항목
  - ⏸ **지역 재선택 UI·저장/불러오기·시드 선택** — "새 판 = 새로고침"인 현 구조에서 게이트 한 겹(`regionPicked`)으로 충분하다
- ✅ **지역이 규칙에 닿는다 — 파라미터 슬라이스** (2026-07-30 · TDD · 설계 [spec](../docs/superpowers/specs/2026-07-30-region-rules-design.md) · designer 설계 → implementer 위임): 배경 슬라이스가 남긴 **규칙 없는 약속**(카드 4장의 서사 8문장)을 지불했다. 지역 4타입이 **기존 훅포인트 6곳**의 파라미터만 쥔다 — 외래 도착 문턱 배율 · 희망 과 믹스 · 응급 도착 문턱 배율 · 주간 임대료 · 소송 비용 배율 · 시작 채용 풀. **새 시스템·새 salt·새 세계 필드 0개**: `SimRegionSpec`/`REGIONS`/`simRegion`/`regionHirePool`을 `world.ts`에 두고 규칙은 전부 `w.region`의 순수 파생이다(저장하면 카탈로그와 갈릴 값을 세계에 안 둔다). 그리고 `SimGame`의 고지문(*"지금은 지역이 부지 밖 풍경만 바꿉니다"*)을 **지웠다** — 안 지우면 화면이 반대 방향으로 거짓말한다. **1430 vitest**(+41)·tsc 0·eslint 0 error·build 통과 · 돌연변이 **30종 사살**
  - **URBAN은 스트림 중립 지역이다**(배율 1 · 믹스 null · 풀 델타 0) — `createWorld`의 기본 지역이라 이 중립이 곧 결정론 회귀 잠금이다. 실제로 `regression.test.ts`를 비롯한 스트림 테스트 전체가 **한 줄도 안 바뀌고** 초록이었다. 갱신된 것은 **결산군 4파일 + 소송 배율 1건**(URBAN 임대료 1,200이 기본 세계 결산에 들어오고, URBAN `lawsuitMul` 0.5가 `events.test.ts`의 소송 차감을 800→400으로 바꾼다 — 설계 §9-8이 명시적으로 요구한 변경이다)
  - **배율은 시드가 아니라 문턱에 곱한다** — 그래야 도착 분 집합이 부분집합(RURAL ⊆ URBAN ⊆ NEWTOWN · 응급은 NEWTOWN ⊆ URBAN ⊆ RURAL)으로 남아 "지역 간 몇 배"가 관측된다. ⚠️ 1차 돌연변이 `seededUnit(seed)/mul >= threshold`는 **문턱 곱하기와 대수적으로 동일**해 살아남았다 — 진짜 시드 변조(배율을 시드 **입력**에 섞기)로 다시 심어 사살했다
  - ⚠️ **`pawn.hiredEver` 동반 수정이 필수였다** — 의사 이름 서수가 `Σ(전국 풀 − 남은 풀)`이라 "초기 풀 = 전국 풀" 전제가 깔려 있었다. 지역이 초기 풀을 깎자 RURAL이 시작부터 **서수 10**을 받았다(에러 0 — 이름만 조용히 밀린다). 기준을 `regionHirePool(w.region)`으로 옮기고 traits.test 2건으로 잠갔다
  - **믹스 폴백 사슬은 이벤트 > 지역 > 전국** — 전염병 날은 어느 지역이든 내과로 쏠린다(NEWTOWN 미용 표가 그날 하루 꺼지는 것이 의도)
  - **카드가 환자 구성도 말한다**(리뷰 후 추가) — 여섯 축 중 `deptMix`만 빠져 있었는데, 하필 설계 §5가 "미용 5%가 이 지역의 정체"라고 못박은 축이었다. 전국 표 대비 **구간 폭 편차 상위 2과**만 그 지역의 실제 비중으로 적는다(NEWTOWN `미용·피부 35%·순환기내과 7%` · PROVINCIAL `미용·피부 5%·내과 55%` · RURAL `미용·피부 5%·내과 50%` · URBAN은 `deptMix: null`이라 침묵). 편차 비교 전에 정수 %로 반올림하는 것이 계약이다 — 실수인 채로 비교하면 눈으로는 동률인 RURAL의 세 과가 이진수 잔차로 순서가 갈린다(동률은 `HIRABLE_DEPTS` 순서 + `Array.sort` 안정성이 깬다)
  - **외래 도착 배율에 절대값 밴드**(리뷰 후 추가) — 부분집합·단조만 재던 탓에 `NEWTOWN 1.2 → 1.05` 돌연변이가 **살아남았다**(1.05도 부분집합이고 단조다). 응급 축에 이미 있던 밴드(±0.08)의 대칭 자리를 채웠다. ⚠️ 두 가지가 이 테스트의 조건이다: ⓐ 배율을 **손으로 적는다**(카탈로그에서 파생하면 기대값과 실측이 같은 표를 봐서 어떤 튜닝도 초록 — 실측으로 확인) ⓑ 표본이 **40,320분 전수**여야 한다(tick 경로의 하루치 표본에서는 NEWTOWN 비율이 1.135로 밴드 가장자리에서 0.015 떨어져 있어 좁은 띠를 못 세운다). ⓑ 때문에 문턱 판정을 `patientFlow.arrivalAt`으로 **export** 했다 — 응급의 `emergencyArrivalAt`과 같은 자리다
  - **I-B1 ⓐ의 미용 쪽 부호는 「총액 + 임대료」로 잰다**(계약 변경 · 리뷰 후 완화 폭 축소) — 옛 단언은 병원 **총** 순익이 양수인지 물었는데, 총액에 지역세(임대료)가 섞이며 URBAN 표준 병원이 미용 단독이어도 음수가 됐다. 1차 대응은 `byDept` 줄로 내려간 것이었는데 그건 **과했다**: 임대료뿐 아니라 간호 주급(300)과 등급 가감산까지 부호 계약에서 빠져 단언이 "미용 수익 > 미용 의사 주급"으로 약해지고, **"미용만 굴리는 병원은 흑자다"라는 병원 층위의 주장이 스위트 어디에도 안 남았다**. 임대료 한 항목만 되더해 옛 질문을 복원했다 — 여유는 넉넉하다(시드 1/3/4/7에서 +730/+850/+940/+1,030). 순환기 쪽은 반대로 `byDept`가 **더 센** 단언이라(그 줄이 음수면 임대료·간호비를 더한 총액은 반드시 음수) 거기 그대로 둔다. 실측 검증: 간호 주급 300→1,500 돌연변이가 `byDept` 형태에서는 **살아남고**(54건 전원 초록) 복원 형태에서는 **죽는다**(2건 실패)
  - ⚠️ **설계 §5의 응급 천장 계산은 실측과 달랐다** — 종이 계산은 "×1.25부터 부호가 뒤집힌다"였지만, 실제로 묶는 것은 도착 수가 아니라 **용량**(침대 수 × 90분 처치)이라 RURAL 응급배율을 1.6으로 올려도 순환기는 여전히 적자다(3.0에서 비로소 뒤집힌다). I-R4는 그래서 종이 계산 대신 **실제 주를 돌려** 부호를 잰다 — 1.2는 유지하되(설계 확정값) 잠그는 것은 배율의 값이 아니라 **부호**다
  - **밸런스 프로브 실측**: 네 지역 **전부 CAMPAIGN_END 완주**(폐업·전멸 0)이고 어느 지역도 지배(유일 흑자)나 열세(파산)로 읽히지 않아 **절대액은 한 값도 조정하지 않았다**. ⚠️ **다만 순서는 빌드에 달렸다** — 1차 기록(NEWTOWN 26,717~32,990 > URBAN 16,946~25,562 > RURAL 16,811~20,529 > PROVINCIAL 10,544~18,276)은 **빌드를 안 적어 재현 불가능**했고, 리뷰어의 독립 프로브는 정반대 순서를 냈다. 빌드를 명시해 재측정한 결과가 아래다 — **지역 규칙은 대부분 도착 쪽에 붙는데(외래 ×1.2 · 응급 ×1.2) 좌석·의사가 얇으면 그 초과 도착을 흡수하지 못해 배율이 금고에 도달하지 않고, 지역차가 임대료 하나로 축소된다.** 프로브가 재는 것은 「지역」이 아니라 **「지역 × 빌드」**다(설계 §10에 그 경고와 기재 항목을 박았다)
    - **빌드**: 대기실 1(16×11 · 좌석 **35**) · 진료실 4(각 6×5 · 내과/외과/순환기/미용) · 병동 1(10×6 · 침대 **8**) · 접수처 1(6×6 · `withCashier`) · 의사 **4명**(과당 1) · 간호사 **1명** · 시드 **1·3·5** · **12주**
    - **최종 금고**(시드 1/3/5): RURAL 24,762·26,424·22,707 > PROVINCIAL 20,594·22,083·18,966 > NEWTOWN 15,467·15,990·5,366 ≈ URBAN 11,049·12,710·9,340. 12주 임대료를 되더하면 22,566~27,110으로 모인다(NEWTOWN 시드 5의 14,966만 예외) — 이 빌드에서 지역차는 **사실상 임대료뿐**이다
  - ⏸ **범위 밖 그대로**: 이송 거리·골든타임 · 지역별 수가(급여 수가는 전국 동일 — 사실관계 훼손) · 채용 계약금 · 소아과 신설 · 지역별 이벤트 카탈로그 차등(RURAL에 「인근 병원 폐업」이 뜨는 서사 부조화는 알려진 채로 보류)
  - ✅ **밸런스 재측정 — 1차 결론이 뒤집혔다** (2026-07-30 · 러너 [scripts/region-balance-probe.ts](../scripts/region-balance-probe.ts) · `npm run probe` · 실측 [research](../docs/research/region-balance-probe-2026-07-30.md)): 위 프로브가 러너를 안 남겨 재현 불가능했던 것을 고치고 다시 쟀다. **교란 변수는 「용량」이 아니라 「수납」이었다** — 창구 1개 병원은 진료비의 **50~81%를 못 걷는다**(`autoFurniture`가 RECEPTION 하나당 COUNTER를 딱 하나 놓는다 → 방을 키워도 소용없고 **방을 더 지어야** 한다). 창구만 3개로 늘리고 나머지를 완전히 동일하게 두면 순서가 **신도시 32,384 > 대도시 21,345 > 지방 16,711 > 농어촌 14,823** — 설계가 예고한 「신도시는 볼륨 게임」 그대로다. 과별 수익이 근거: 신도시 미용 27,660 vs 농어촌 4,800(5.8배)로 믹스가 장부에 도달한다. **절대액은 한 값도 안 바꿨다**(넷 다 완주·흑자 · 1위와 4위 차 2.2배 — 지배도 파산도 없다)
    - ⚠️ **정정(같은 날 2차 측정)** — 위 「수납이 교란 변수」는 맞지만 **원인 지목이 틀렸다**. 창구 수는 무관했다: 접수처 3개+간호사 1명 = **28%**, 접수처 1개+간호사 3명 = **100%**. `cashierAt`은 설치된 첫 창구를 쓰고 점유를 안 보므로 창구를 늘려도 아무것도 안 바뀐다 — 갈리는 것은 `hasCashier`의 **간호사 ≥ 1**이고, 간호등급 SHORT면 그 간호사가 **1주차에 떠나** 그 뒤 11주가 통째로 미수가 된다. 정정 수치: 간호사를 기준(⌈의사÷2⌉)까지 채우면 **신도시 36,458 > 대도시 25,405 > 지방 20,901 > 농어촌 18,909**. 그리고 의사 8명 빌드는 **신도시만 완주**(13,946)하고 셋은 7~9주차 파산 — 「신도시는 볼륨 게임」의 더 강한 형태다
  - **프로브를 커밋한 것 자체가 이 항목의 산출물이다** — 빌드를 주석이 아니라 코드로 박아야 재현된다. 본 스위트와는 include를 분리했다(⚠️ `mergeConfig`는 배열을 *이어붙여* 1,447건이 같이 돈다 · `disableConsoleIntercept: true`가 없으면 표 자체가 안 보인다)
    - ⚠️ **의사 사직이 지배 변수다** — 12주 동안 전 지역 **4→1**(농어촌만 4→2). 고정비 합이 정원 급여의 20%인 이유이고, 프로브 표를 읽을 땐 **「의사 4→N」 칸을 먼저** 봐야 한다(안 보면 지역 밸런스 표로 읽은 게 사실은 사직 속도 표다)
    - ⚠️ **의사 8명 빌드는 네 지역 전부 3~6주차 파산** — 순환기내과 주급 8,000이 단독 최대. 과잉 채용이 곧 파산이라는 부호는 살아 있다
    - ⚠️ **지방 소도시는 도착 배율이 대도시와 같은데 진료 수가 절반 이하**(592 vs 1,415) — 내과 55% 쏠림 + 의사 과당 1명이라 **한 과의 병목이 병원 전체를 막고** 나머지 세 의사가 논다. 그 지역의 정체가 작동하는 모습이지, 버그가 아니다. 다만 화면이 그걸 "내과를 더 뽑아라"로 읽히게 하는지는 미확인
    - **미수는 숨어 있지 않다** — `alertsOf`의 `unpaid` 경보 + 하루 결산(`unpaidText`)이 샌 뒤에 금액을 말한다. 고지되는 압력이지 조용한 함정이 아니다
    - ⏸ **조정 후보로만 남김**: 농어촌이 임대료 0인데도 최하위 — 다만 「농어촌은 박하다」와 어긋나지 않아 재미의 문제로 판단될 때 손댄다
- ✅ **지역 선택이 지도가 된다 — 카드 네 장 → 대한민국 위의 열일곱 점** (2026-07-30 · TDD · 사용자 지시 *"실제 대한민국 지도로 보여주자 · 조금만 리서치해도 분류는 금방 가능하다"* · 설계 [spec](../docs/superpowers/specs/2026-07-30-korea-map-picker-design.md)): 카드는 규칙을 정확히 말하지만 **어디인지는 말하지 않았다** — "지방 소도시"는 분류이지 장소가 아니다. 통계청 2018 시도 경계(`skorea-provinces-2018-topo-simple`)를 **23×41 격자로 래스터화**해(위경도 클립 → 한 칸이 실거리 정사각이 되도록 Δ위도 = Δ경도 × cos36° → 칸당 5×5 표집, 육지 18%↑ → 빈 행·열 트림) 지도를 깔고 그 위 **17곳**을 고르게 한다. 손으로 찍은 게 아니라 해안선·제주·최북단 고성이 제자리이고, 결과는 문자열 배열 상수라 **런타임 의존성·외부 에셋 0**. **1442 vitest**(+12)·tsc 0·eslint 0 error · 돌연변이 4종 사살
  - **규칙은 한 줄도 안 바뀐다** — `REGIONS` 4타입과 훅포인트 6곳 앞에 「실제 지역 → 4타입」 매핑만 얹는다. 서울을 고르든 부산을 고르든 세계는 똑같은 `URBAN`이다(도시마다 값을 따로 두면 값이 두 곳에 산다 — 이 문서가 세 번 경고한 이중 기재)
  - **시군별 지표는 화면에 안 적는다** — 응급의료분야 의료취약지(복지부 고시 제2024-261호)·분만취약지·인구감소지역 89곳(행안부)이 실재하지만 **17곳 각각의 명단 포함 여부를 원문으로 확인하지 않았다.** 확인 안 한 수치를 박으면 「근거 있는 것만 말한다」가 깨진다 — 제도 비판은 이미 타입의 규칙이 지고 있고, 분류 자체(서울=대도시, 신안군=농어촌)는 근거가 필요 없을 만큼 자명한 선에서 멈춘다
  - **두 단계다**(점 클릭 → 확인 → [여기에 짓는다]) — 「고르고 나면 바꿀 수 없습니다」라고 써 놓고 오클릭 한 번에 12주가 결정되면 앞뒤가 안 맞는다. 카드 시절엔 목록이 곧 설명이라 한 번에 골라도 됐지만 지도의 점은 눌러 보기 전엔 무엇인지 모른다
  - **색을 안 쓴다 — 점 크기가 도시 규모다**(대도시 1.45칸 → 농어촌 0.62칸). 팔레트가 지면 위 단색 계열이라 타입별 색 4종을 들이면 지도만 다른 게임이 된다. 휘도 서열 패널 23.4 < 육지 46.1 < 점 115.8 < 선택점 206.6 — 「육지는 바탕이고 점이 내용」이 밝기로 보인다(배경 팔레트의 「부지가 배경보다 밝다」와 같은 계약). 육지에 `bg-frame`(38.4)을 쓰면 해안선이 뭉개진다
  - ⚠️ **클릭 판 1.5칸은 상한이다** — 반지름 0.75칸을 넘기면 **이웃 칸 지역의 중심까지 덮어** 그 지역이 영영 클릭 불가가 된다. 「두 지역이 같은 칸을 쓰지 않는다」(테스트)가 이 값의 안전을 받치는 불변식이라 **두 값이 서로를 전제한다**
  - **`SimGame`은 오히려 줄었다** — 1,200줄 파일이라 인라인 카드 마크업과 `REGION_CARDS`를 떼어 `RegionPicker.tsx` + `koreaMap.ts`로 옮기고 `<RegionPicker onPick={...} />` 한 줄만 남겼다. 남는 책임은 「고른 뒤에 무슨 일이 일어나는가」뿐
  - **지도는 회화 레이어라 "예쁜가"는 못 잠그지만 데이터로서 성립하는 조건은 잠근다** — 모든 점이 육지 위 · 칸 겹침 없음 · 4타입 전부 도달 가능 · 마스크 직사각형 · 육지 비율 3~7할 · `landRuns`가 육지를 정확히 덮고 **최대 길이**. 기대값을 마스크에서 파생하지 않는다([T-111](troubleshooting/T-111.md)); 타입 도달성만은 반대로 `REGIONS`에서 세어 **지도가 카탈로그를 따라가게** 한다
  - ⚠️ [T-114](troubleshooting/T-114.md) — 숨은 Browser pane에서는 `document.timeline`이 안 흘러 CSS 트랜지션이 **시작색에 고정**되고, 그게 「클래스가 안 먹었다」와 똑같이 보인다(선택된 점이 muted로 측정돼 30분 샜다). 색을 재기 전에 `getAnimations().finish()`
  - ✅ **시군별 지표 표시** — 아래 「고시 원문을 확인하고 딱지를 붙인다」에서 해소했다(원문 확인이 선행 조건이었다)
  - ⏸ **범위 밖**: 지역 재선택 UI · 지도 확대/이동 · 시군 단위 전체 개방 · 지역별 개별 수치
- ✅ **고시 원문을 확인하고 딱지를 붙인다 — 응급의료 취약지·인구감소지역** (2026-07-30 · TDD · 사용자 지시 *"고시 원문 확인부터 진행해"* · 대조 기록 [research](../docs/research/medical-vulnerable-area-designations.md)): 지도 슬라이스가 *"원문을 확인하지 않았으므로 시군별 지표를 안 적는다"*로 미뤄 둔 조건을 해소했다. **1447 vitest**(+5)·tsc 0·eslint 0 error · 돌연변이 5종 사살
  - **원문 확보 방법이 요점이다** — 응급의료 취약지는 [복지부 고시 제2024-261호](https://www.mohw.go.kr/board.es?mid=a10107010000&bid=0043&act=view&list_no=1484143) 첨부 **`.hwpx`(OWPML zip)를 풀어 `Contents/section0.xml`에서 제2조 전문**(시군구 100곳)을 얻었다. 같이 올라온 PDF는 CID 인코딩이라 텍스트 추출이 지저분한데 hwpx는 그냥 XML이다 — **정부 고시 원문이 필요할 땐 hwpx를 먼저 본다**. 인구감소지역 89곳은 행안부 페이지 HTML에서 표 텍스트를 뽑아 대조했다(요약 도구의 답을 원문으로 재확인 — 실제로 일치했다)
  - **지정된 곳에만 한 줄이 뜬다** — 없으면 침묵한다(「중립 축은 안 적는다」의 지도판 · 서울에 "취약지 아님"은 정보가 아니라 소음). 실재 고시에서 온 유일한 줄이라 서사·규칙과 층위가 안 섞이게 테두리 두른 작은 딱지로 분리
  - **대조가 드러낸 것**: 농어촌 4곳은 **전부 양쪽 지정**(게임 규칙과 방향 일치). 반면 **지방 소도시 다섯은 갈린다** — 정읍·삼척 양쪽 / 서귀포 응급만 / 안동 인구감소만 / **목포 어느 쪽도 아님**. 안동·목포가 응급 목록에서 빠지는 건 **권역 거점**이라서다(경북 응급 목록엔 안동시 대신 예천군). 4타입 분류가 틀린 게 아니라 한 타입 안에 섞여 있는 것이 실제 데이터이고, 딱지가 그걸 화면에 드러낸다
  - ⚠️ **기대값이 저장소 밖에서 온다** — 그래서 17곳 대조표를 테스트에 **명시적으로** 적었다(카탈로그 파생이면 무엇을 넣어도 통과 · [T-111](troubleshooting/T-111.md)). 이 테스트가 깨지는 정상 경로는 **재지정 고시뿐**이고, 깨졌는데 원문을 안 봤다면 고치는 게 아니라 덮는 것이다
  - ⏸ **분만취약지는 뺐다** — 고시 별표가 아니라 연간 「분만취약지 지원사업 안내 지침」으로 운영돼 성격이 다르고, 결정적으로 **이 게임에 산부인과가 없다**. 규칙에 안 닿는 사실은 안 올린다(소아과와 같은 자리 — 과가 생기면 다시 본다)
  - ⏸ **광역시 자치구는 적용 안 함** — 부산 동·서·영도구와 대구 남·서구는 인구감소지역이지만 지도의 점은 자치구가 아니라 광역시 전체를 가리킨다
- ✅ **가구를 실제로 쓴다 — 눕는 침대·앉는 의사·마주 보는 창구** (2026-07-30 · TDD · 사용자 플레이 보고 3건): 세 신고가 **한 뿌리**였다 — `world.blocksWalk`가 의자만 통행 허용이라, 자리를 계산하는 `occupySpot`이 나머지 가구에서 전부 "그 **앞** 칸"으로 접혔다. ① **침대가 통행 집합에서 빠진다**(의자가 걸었던 그 경로) → 응급 환자는 침대 **위**에 서고 TileMap이 `rotate(90deg) scale(0.8)`로 눕혀 그린다(줄이지 않으면 폰이 타일을 꽉 채워 무엇 위에 누웠는지가 사라진다 — 의자의 `SEAT_LIFT`와 같은 문제) · `BedSprite.occupied`가 드디어 `false` 리터럴을 벗어난다 ② **휴게실 침대 = 당직 침대**(사용자 요구 *"당직 서는 의사는 휴게실에서 잔다"*) — `BreakDef.furnitureKind` → `furnitureKinds` 배열이고 **순서가 곧 선호**다(휴식은 `['BED','CHAIR']` — 침대가 뒤면 의자가 다 찰 때까지 안 쓰여 300만원이 화면에서 침묵한다) ③ **진료실 의사가 앉는다** — `examSlots`가 **2패스**로 갈렸다: 환자 의자 짝짓기를 전부 끝낸 **뒤에** 남는 인접 의자만 의사에게 준다(한 패스면 첫 책상이 옆 책상의 환자 의자를 가져가 **진료실 정원이 조용히 줄어든다** — 「책상 수 = 정원」이 이 파일의 계약이고 연출이 규칙을 깎아선 안 된다) ④ **수납 창구가 마주 본다** — `cashierSpots`(한 칸) → `cashierSlots`(카운터 + `nurseSpot`/`patientSpot`). 직원 쪽 판정은 ⓐ 의자가 놓인 쪽 ⓑ 정문에서 **먼** 쪽 순이고, 마주 볼 짝이 없으면(벽에 붙은 카운터) **옛 계약으로 폴백**한다 — 없으면 그런 카운터가 창구로 성립하지 않아 지금까지 돈을 걷던 병원이 조용히 미수 병원이 된다. **1461 vitest**(+8)·tsc 0·eslint 0 error
  - **병동·휴게실 정원이 「가구 수」와 같아졌다** — 앞 타일을 쓰던 시절엔 침대 둘이 한 칸을 나눠 가지면 자리가 하나로 접혔다(dedupe). 이제 자리가 침대 그 자체라 **화면의 침대 수 = 응급 수용 상한**이고, `wardBeds`를 그대로 읽는 HUD 경고도 같은 수를 본다
  - **회귀 2건이 「침대로 막던」 픽스처였다** — `dept.test`의 정문 봉쇄(BED → DESK)와 `spots.test`의 앞 타일 dedupe(BED → COUNTER). 규칙이 바뀌었으니 픽스처가 다른 막는 가구로 옮겨 가는 것이 맞고, 테스트가 재던 계약(스폰 비켜서기·dedupe 안전망)은 그대로 살아 있다
  - ⏸ **응급 의사는 침대로 안 간다** — 사용자 확인(2026-07-30) *"응급실에서는 환자만 눕는 게 맞다"*. 의사는 자기 책상에서 처치한다(이동·복귀·경로 실패 처리가 통째로 붙는 변경이라 범위 밖)
  - ⏸ **환자 여럿이 한 창구로 몰리는 것은 그대로** — `cashierAt`이 여전히 첫 슬롯만 본다(점유를 안 본다). 창구 병목의 진짜 변수는 간호사라는 실측(바로 위 프로브 정정)이 있어 이번 범위에 안 넣었다
- ✅ **간호인력 리서치 + 엔티티 증보** (2026-07-29 · 문서 2장 · 코드 0줄): 카운터·접수처가 장식임을 확인한 뒤 사용자 제안(*"진짜로 붙이려면 간호사가 필요하다 + 처우·권한 쟁점"*)으로 [리서치](../docs/research/nursing-workforce-grounding.md)(간호등급제·잔류 실패·배치·PA 법제화 4축) + [엔티티 증보](../docs/concept/nursing-entities.md)(약 24개 · 문제 N1~N3 · 기존 기계 대응표). 적대 검증이 "감산 감수가 이득" 가설을 출처 부재로 격리. **다음: N1~N3 범위 결정(사용자) → 본 카탈로그 병합 → 구현**
- ✅ **화면이 곧 부지 — 맵 전면 확대 + HUD 얹기(림월드 배치)** (2026-07-29 · TDD · 사용자 지시 *"실제 게임 화면은 저 배치하는 화면인데 너무 작다 · 상단 시간·채용과 하단 벽·문 설치 UI를 배치 화면 안으로, 림월드처럼"*): 맵이 768×512 **고정 상자**로 화면 한가운데 놓이고 위아래에 바가 따로 서 있어, 정작 플레이하는 부지가 화면에서 제일 작았다. `main`을 `h-dvh` **3행 그리드**(auto·1fr·auto)로 바꿔 맵이 남은 공간 **전부**를 받고, TileMap이 자기 자리를 재서(`useFitScale`) CSS transform 한 번으로 통째로 확대한다 — 1600×900에서 타일 **16→21.4px**(맵 1026×684 · 면적 1.79배), 모바일 375폭에서도 넘침·스크롤 0. HUD 두 줄은 부지 바닥색(`OUTSIDE_FLOOR`) 위에 반투명으로 얹혀 **한 장의 화면**으로 읽힌다. **1171 vitest**(+5)·tsc 0·eslint 0
  - ⚠️ **가변 높이 UI가 배율을 흔든다**(사용자 보고 후 수정 · [T-101](troubleshooting/T-101.md)): 용도·과 줄을 조건부로 렌더하니 [용도]를 누를 때마다 footer가 86→173px로 자라 **화면 전체가 리스케일**됐다. 두 줄을 항상 렌더하되 `invisible`로 숨겨 자리를 예약한다 — 맵은 11% 작아지지만 **어느 상태에서도 배율이 같다**(6개 도구 상태 실측 불변)
  - **좌표 변환을 `simHud.tileFromPoint`로 내렸다** — 포인터를 화면 상수 `TILE`이 아니라 **그려진 rect에서 파생한 타일 크기**로 나눈다. 배율이 자동으로 반영되므로 확대·리스케일이 좌표를 어긋내는 경로가 **산술에서 사라진다**([T-099](troubleshooting/T-099.md)가 남긴 "맵 위 높이는 드래그 중 변하면 안 된다"의 구조적 폐기 — 다만 리스케일이 화면을 출렁이게 하므로 상태 문구는 계속 footer 예약 줄에 둔다). 실측으로 잠갔다: 배율 1.5 화면에서 타일 (10,5)→(14,8) 드래그가 정확히 그 칸들을 겨눈다(미리보기 14칸 = 5×4 테두리)
  - ⚠️ **HUD를 맵 위에 진짜로 겹치지는 않았다** — 부지가 고정 48×32라 카메라를 밀어 가려진 타일을 피할 수가 없다(림월드와 다른 조건). 겹쳐 띄우면 그 타일에 영영 건설을 못 한다. 그래서 3행 그리드로 자리를 나누고 **보기에만** 이어지게 했다. 부지 스크롤·줌이 생기면 그때 진짜 오버레이로 갈 수 있다 → **다음 항목에서 그렇게 했다**
- ✅ **부지 카메라(팬·줌) + HUD 진짜 오버레이** (2026-07-29 · TDD · 사용자 지시 *"부지 스크롤·줌 넣고 HUD 진짜로 겹치자"*): 바로 위 항목이 남긴 ⚠️ 두 개를 카메라 하나로 닫는다. **순수 카메라 산술 3종**을 `simHud`에 신설했다(`clampCamera`·`zoomedCamera`·`pannedCamera` — DOM을 모르고 vitest가 직접 겨눈다): 콘텐츠가 뷰포트보다 작으면 **중앙 정렬**(팬 무시), 크면 `[view−content, 0]`으로 잘라 **맵 가장자리가 뷰포트 안으로 못 들어온다**(빈틈 0), 줌은 `x′ = ax − (ax − x)·k`로 **앵커 아래 맵 좌표를 보존**한다(fit 대비 배수 `[1, 3]`). 조작은 휠 줌(트랙패드까지 `exp` 스케일 · `passive:false` 네이티브 리스너라야 `preventDefault`가 듣는다) · 중/우클릭 드래그 팬 · 도구 없을 때의 좌드래그 팬 · [+][⌂][−] 버튼(터치·트랙패드의 유일한 줌 수단). **`main`이 3행 그리드 → `relative`가 되고 맵이 `inset-0`으로 화면 전체를 깐다** — 가려진 타일은 이제 팬으로 끌어내 지을 수 있으므로 겹쳐도 도달 불가 타일이 안 생긴다. **1179 vitest**(+8)·tsc 0·eslint 0·build 통과
  - **T-101의 자리 예약을 되감았다** — footer가 오버레이가 되면서 `footer 높이 → 맵 크기` 결합 경로 자체가 사라져(맵은 뷰포트만 본다) 용도·과 줄을 **조건부 렌더로 복귀**시켰다. 6개 도구 상태 순회 실측: footer 87→130→173px로 자라는데 **맵 배율은 고유값 1개**(2.8826 · `SCALE_STABLE: true`), 예약분 11%도 회수(1026×684 → **1130×753**). T-101의 재발방지 원칙은 그대로 유효하다 — 없앤 것은 원칙이 아니라 그 원칙이 겨눴던 결합이다
  - **우클릭이 건설 드래그를 시작하던 버그를 함께 닫았다** — 포인터 핸들러에 버튼 구분이 없었다. 이제 **주버튼 + 지을 수 있음**만 건설이고 나머지는 전부 팬이다(브라우저 실측: 벽 도구를 든 채 우드래그 → 미리보기 0칸·카메라만 이동)
  - ⏸ **핀치 줌은 안 넣었다**(`ponytail:` 주석으로 한계 명시) — 포인터 두 개를 추적하는 상태가 통째로 필요하고 [+][⌂][−] 버튼이 같은 일을 한다. 리사이즈 시 **보던 중심 유지**도 안 한다(zoom은 유지하고 재클램프만) — 창 크기를 바꾸면 화면이 조금 밀린다
  - ⚠️ **zoom 1의 fit 기준이 뷰포트 전체라 부지 아래쪽이 영구히 가렸다** → 바로 아래 항목에서 안전 영역 기준으로 고쳤다([T-102](troubleshooting/T-102.md))
- ✅ **카메라 안전 영역 fit — HUD 밑에 깔리던 부지를 되찾다** (2026-07-29 · TDD · 배포본 사용자 보고 *"하단 메뉴에 의사 캐릭터가 처음 배치되는 모습이 가려져"*): 바로 위 항목이 zoom 1의 fit을 **뷰포트 전체**로 잡아, 기본 화면에서 부지 위·아래 끝이 정확히 두 바 밑에 놓였다. 게다가 그 배율은 콘텐츠 = 기준이라 **팬 슬랙이 0**이어서 끌어낼 수도 없다 — 의사가 처음 서는 입구 앞이 항상 안 보였다. fit·클램프의 기준을 **안전 영역**(뷰포트 − header − footer 높이)으로 바꾼다: 카메라 3종이 `Size` 대신 **`Rect`**를 받고 축 클램프가 `[start + safe − content, start]`로 일반화된다(콘텐츠 ≤ 안전 영역이면 그 **안에서** 중앙). 인셋 0이면 옛 계약과 동일해 **기존 8건이 그대로 회귀 가드**이고 인셋 3건을 새로 겨눴다. 바 높이는 `SimGame`이 재서(마운트 동기 측정 + ResizeObserver — [T-086](troubleshooting/T-086.md) 패턴) `insets`로 내려보낸다. **1182 vitest**(+3)·tsc 0·eslint 0 error·build 통과. 브라우저 실측(1280×800): 맵 top 43 = header bottom · bottom 627 = footer top(빈틈 0) · **채용한 의사 rect 608.75~627 = 안전 영역 안** · 줌인 후 팬 극단에서도 안쪽 빈틈 0 · 휠 앵커 드리프트 0
  - ⚠️ **T-101의 자리 예약이 부활했다** — fit이 footer 높이를 다시 읽으므로 결합이 **경로를 갈아타고** 돌아왔다(그리드 `1fr` → fit 기준). 용도·과 줄을 `invisible` 예약으로 **재복원**했다: 6상태 순회 재실측 `footerH` 고유값 1개(173) · `scale` 고유값 1개(1.1406). *"결합이 사라졌다"는 하루를 못 갔다* — T-101 원칙 ①이 옳았다
  - **부수로 1px 격자 어긋남도 닫았다** — 맵 상자의 `border`(1px)가 border-box에서 패딩 박스를 밀어 타일 격자 전체가 1px 어긋나 있었고, 상자가 안전 영역에 딱 맞는 지금은 그게 곧 마지막 줄이 footer 밑에 깔리는 양이었다(실측 1.14px). `border` → inset `box-shadow`(레이아웃 밖)
- ✅ **도구 팔레트를 하단 바 → 좌측 세로 패널로** (2026-07-29 · TDD · 사용자 지시 *"지금 메뉴가 하단에 있잖아. 이걸 사이드로 위치시키자. 사이드가 너무 비어보여"*): 부지가 가로로 넓은 48×32라 fit이 **세로에 걸려** zoom 1에서 좌우에 늘 빈 배경이 남는데, 하단 전폭 바는 그 가로를 못 쓰면서 **fit이 걸린 축(세로)만 깎았다** — 팔레트가 맵을 두 번 좁히고 있었고 남는 좌우가 곧 그 자리였다. `footer` → **`aside`(`w-40` 고정)**, 인셋 계약이 `{top, bottom}` → **`{top, left}`**. **카메라 산술은 한 줄도 안 바뀐다** — `clampAxis`가 원래 `start`를 받아 두 축 대칭이라(T-102의 `Size`→`Rect` 일반화) 좌 인셋은 `safe.x`를 채우면 성립하고, 그 대칭을 회귀 2건으로 못박았다. 안전 영역 산술은 순수 함수 **`safeArea`**로 훅 밖에 냈다(Red→Green). **1187 vitest**(+5)·tsc 0. 실측 1280×720 맵 `1016×677`·900×600 맵 `740×493`(둘 다 안전 영역에 정확히 fit) · 건설 드래그 6×6 = 20칸 정합
  - **T-101이 구조적으로 닫혔다** — 세 번 오간 결합(*가변 UI 높이 → 맵 배율*)이 **경로를 갈아탈 자리 없이** 사라졌다: 용도·과 줄이 바꾸는 건 패널의 **높이**인데 맵이 읽는 건 **폭**이다. 두 번 넣었다 두 번 뺐던 `invisible` 자리 예약도 함께 걷어냈다(예약은 수단이었지 목적이 아니었다). 원칙 ①은 유효하다 — 이번엔 그 옆을 **가변이 아닌 축으로** 마주 보게 했을 뿐
  - ⚠️ 검증 절차 함정 1건([T-103](troubleshooting/T-103.md)) — 표시 안 되는 브라우저 pane은 프레임을 안 그려 `ResizeObserver`가 안 돌고, "리사이즈했는데 안 바뀜"이 **코드 버그의 얼굴**로 나타난다([T-086](troubleshooting/T-086.md)이 아는 현상의 검증 절차판 · 하드 리로드가 답)
- ✅ **림월드 장르 해부 — 조작·UI·흐름 추출과 이식 판정** (2026-07-29 · 설계 문서 · 사용자 지시 *"림월드의 장르만 배껴온다는 생각으로"*): 림월드를 네 축(조작 문법 · UI 배치 · 게임 흐름 리듬 · 서사 기계)으로 해부하고 **전 요소를 채택/변형/기각으로 판정**한 카탈로그 — [docs/concept/rimworld-genre-extraction.md](../docs/concept/rimworld-genre-extraction.md). 판정 기준은 메시지 렌즈 하나: *사람을 사람으로 보게 하면 채택, 구조를 이길 수 있게 하면 기각*. 이미 흡수된 10개(배속·간접 통제·아코디언·욕구·스토리텔러·특성…)를 재확인하고 신규 판정: **P1 채택 3건**(① 인스펙트 — 폰 클릭 카드: 특성·사연이 코드에만 있고 노출 표면이 사직 편지뿐인 최대 미수확 지점 ② alerts 상시 경고 스택 — 상태줄 1줄은 동시 경고를 못 담는다 ③ 스페이스·ESC 키보드 최소셋), P2 3건(의사 바 · 현재 작업 문장 · 위기 후 휴지기 리듬), **기각 8건** — 특히 드래프트(직접 통제 = "내가 직접 뛰면 살렸다" → 메시지 붕괴 최단 경로)와 부 비례 위협(압력의 출처는 성장이 아니라 구조 상수여야 한다)
  - ✅ P1 3건을 구현 스펙으로 내리고([2026-07-29-rimworld-p1-surfaces-design.md](../docs/superpowers/specs/2026-07-29-rimworld-p1-surfaces-design.md)) 구현까지 완료 — 바로 아래 항목
- ✅ **스타팅 로스터 + 개원 준비 체크리스트 — 처음 여는 사람에게 순서를 준다** (2026-07-29 · TDD · 사용자 지시 *"맨 처음 게임을 시작할 때 의무적으로 의사를 몇 명 채용할 수 있도록"* + 범위 결정 *"튜토리얼을 필요로 하는 사용자에게는 건설도 안내해 줘야 한다 — **림월드가 한국에서는 그렇게 일반적인 장르는 아니거든**"*): 바로 위 「접수처 반려」가 이 필요를 만들었다 — 의사 0명은 이제 **도착 0명**이라 안 뽑으면 판이 문자 그대로 아무 일도 안 일어나고, 빈 화면과 고장이 구별되지 않는다. **1266 vitest**(+9)·tsc 0·eslint 0 error·`next build` 통과
  - **① 스타팅 로스터(게이트)** — 판이 열리자마자 채용 패널이 뜨고 **최소 3명**(`STARTING_ROSTER_MIN`)을 채워야 [개원 준비 시작]이 열린다. **새 화면을 안 만든 것**이 요점이다: `HirePanel`은 이미 과별 주급 + 주 고정비 합계를 나란히 놓아 *필수과를 채울수록 그 숫자가 커지는 것*을 뽑기 전에 보게 한다 — 스타팅 로스터가 딜레마여야 하는 이유가 그 패널에 이미 있었다. **하한이지 상한이 아니다**(셋을 다 미용으로 뽑는 것도 선택이고 그게 이 게임의 질문이다). 게이트는 **화면 상태이지 세계 상태가 아니다** — 코어에 「개원했는가」를 두면 tick·결산·회귀 픽스처가 그 국면을 알아야 하는데 이 게이트가 막는 것은 시뮬이 아니라 사람이다. 떠 있는 동안 **키보드는 통째로 잠긴다**(스페이스는 의사 없는 판의 시계를 켜려 들고, ESC는 못 닫으면서 아래 도구를 놓아 "먹통"으로 읽힌다)
  - **② 개원 준비 체크리스트** — **순서는 원래 `setupAlert` 체인에 있었다**(대기실 → 문 → 진료실 → 책상). 없던 건 그 순서를 한눈에 보여주는 형태뿐이라, 새 판정 대신 **`setupSteps(w)`를 단일 출처로 세우고 경고를 그 「첫 미완 단계」의 파생으로 뒤집었다** — 두 곳이 각자 판정하면 "경고는 진료실을 말하는데 체크리스트는 대기실에 체크가 없는" 화면이 나온다. `hint`가 함께 있는 이유는 **조작을 모르면 할 일을 알아도 못 해서**이고(드래그 건설·용도 지정은 이 장르 처음이면 추측 불가), **안내는 지금 할 일에만 편다**(다섯 줄이 다 펼쳐지면 목록이 아니라 설명서라 무엇부터 할지가 다시 묻힌다)
  - **`done`은 「문제가 없다」가 아니라 「실제로 갖췄다」** — 빈 판에서 `sealed === 0`만 보면 「문 내기 ✅」로 떠 처음 보는 사람을 속인다. 앞 조건을 붙여도 경고는 안 바뀐다(방 없는 판은 그 앞 `no-waiting`이 먼저 걸려 그 줄까지 오지 않는다)
  - 실측(새 dev 서버·새 탭): 게이트 `0/3 · 버튼 disabled` → 3명에서 활성 → 닫으니 체크리스트 `1/5` · 경고 「대기실이 없습니다」 → 대기실을 짓자 **`3/5`로 뛰고 안내가 다음 단계로 이동**, 경고도 「진료실 없는 의사 3명」으로 함께 이동 → 스페이스가 게이트 뒤에서 막힘 · **콘솔 에러 0**
  - ⚠️ 검증 중 `useEffect` deps 크기 경고가 떴는데 **HMR 잔재**였다([T-107](troubleshooting/T-107.md)) — 콘솔 버퍼가 리로드를 건너 살아남아 새 로드의 결함처럼 보였다. deps는 고정 7개 리터럴이라 런타임에 6개가 될 경로가 없다
- ✅ **접수처가 돌려보낸다 — 안 보는 과 환자는 대기실에 앉지 않는다** (2026-07-29 · TDD · 사용자 질문 *"내과도 내과 선생도 없는데 내과 환자가 대기실에서 대기해. 말이 안 되잖아. 게임 컨셉 때문에 어쩔 수 없는 부분인 거야?"* → **절반만 그렇다**): 컨셉이 요구하는 것은 *내과 환자가 온다는 사실과 **놓쳤다는 숫자***다(안 뽑은 대가가 사라지면 판이 "안 뽑는 게 이득"이라고 말한다). 요구하지 않는 것은 *그 사람이 의자에 90분 앉아 있는 것*이다. 근거는 게임 안의 비대칭 — **응급은 이미 문전 판정으로 돌려보내는데**(회차·사유까지) 외래만 안 그랬다. `servesDept(w, dept)` 신설 + `maybeArrive`가 희망 과를 먼저 뽑아 **좌석 판정보다 먼저** 반려하고, 사유는 `stats.leftNoDept`(`leftCount`의 **부분집합**)로 센다. 판정 기준이 **진료실이 아니라 의사**인 것이 계약이다(방이 없는 건 기다릴 만한 상태다). **1257 vitest**(+5)·tsc 0·eslint 0 error
  - **덤이 본편보다 컸다 — 좌석 증폭**: 안 보는 과 환자가 의자를 90분씩 물어 **볼 수 있었던 환자까지 문간에서 밀어냈다**(대기실이 차면 신규 도착은 폰조차 안 생긴다 — 한 명이 이탈을 두 건 만든 셈). 회귀 픽스처 실측이 크기다 — 내과만 있는 병원에서 **진료 4→7건(+75%) · 수입 48→84만원**, 이탈 36→33이면서 **그중 32건이 반려**로 정확히 귀속됐다
  - **밸런스 경계를 테스트로 못박았다** — 4과를 다 보는 병원은 반려 분기가 발동하지 않아 **코드 경로가 옛것과 완전히 같다**(`leftNoDept === 0`). 마감 12일 전 변경이라 "어디까지 흔드는가"를 계약으로 남겼다
  - **화면 둘**: 경고 스택 `no-dept`(*"…환자를 문 앞에서 돌려보내고 있습니다"* — **실제로 돌려보낸 뒤에만** 뜨고 과 목록은 **지금** 상태에서 뽑는다 · **warn이지 danger가 아니다**: 안 뽑은 것은 사고가 아니라 선택이다) · 하루 결산 `33 (그 과가 없어서 32)`(나머지 이탈은 더 지으면 줄지만 이건 **채용해야** 준다)
  - 깨진 옛 테스트 9건을 *낡은 기대값 / 진짜 회귀 / 표본 문제*로 먼저 분류([T-104](troubleshooting/T-104.md) 재발방지 ②) — **표본 3 · 스키마 4 · 의도된 규칙 변경 2 · 진짜 회귀 0**. 함정 2건: [T-105](troubleshooting/T-105.md)(유입을 줄이는 변경이 짧은 창의 계측기를 굶겨 "규칙 위반"의 얼굴로 깨진다) · [T-106](troubleshooting/T-106.md)(「내과」 ⊂ 「순환기내과」 부분문자열 단언 — 반대 방향으로는 **조용히 통과**한다)
  - ⚠️ 살아 있는 도착은 브라우저에서 못 봤다(rAF 0회 · 시계 09:00 고정 — [T-103](troubleshooting/T-103.md) 실측 재확인). 콘솔 에러 0만 확인했고 규칙·문구·귀속은 단위 테스트가 잠근다
- ✅ **영역 오버레이를 껐다 — 지도 위의 주석은 물어볼 때만 답한다** (2026-07-29 · TDD · 사용자 보고 *"평소에는 보이지 않다가 … 엑스레이 같은 모드를 on/off 하면 저렇게 보여져. **핵심은 지금 가시성이 너무 안 좋다는 거야**"*): 과 색 틴트와 방 이름표가 **늘 켜져 있어** 부지를 채울수록 정작 봐야 할 사람·가구·벽이 그 밑에 묻혔다 — 주석이 지도를 덮은 꼴이다. 실측이 그대로 나온다: **1칸(21px) 방에 「진료실 · 미용·피부」가 113px**(방 폭의 5.4배)로 서서 옆방까지 넘어간다. `regionOverlayOn({ toggled, tool })` 순수 함수 신설 — **기본 꺼짐**, 줌 버튼 아래 `▦`(「영역 보기」)로 켠다. 줌과 같은 묶음인 것이 자리의 근거다(둘 다 판을 안 바꾸고 **무엇을 보는지**만 바꾼다 — 판을 바꾸는 행동은 좌측 패널이라는 관통 규칙). **1249 vitest**(+5)·tsc 0·eslint 0 error·**시뮬 코어 0건 변경**
  - **감추는 것은 판독용 표시뿐이다** — **바닥색·벽색은 늘 그린다**: 방 바닥(`#16232a` 계열)은 바깥(`#0d0d11`)과 겨우 구별되는 농도라 "여기는 실내다"만 나르고 그건 주석이 아니라 **건물의 일부**다. 그래서 꺼도 병원은 병원으로 읽힌다
  - **용도 도구를 들면 토글과 무관하게 켜진다** — 안 그러면 어디가 이미 지정됐는지 안 보이는 채로 지정해야 해 **꺼진 기본값이 곧 함정**이 된다(이 변경이 건설 흐름을 망가뜨리는 유일한 갈래). 반대로 도구를 놓을 때 토글을 되돌리지 않는 것도 계약이다 — 도구는 잠깐 빌려 쓸 뿐 소유하지 않는다. 판정이 `SimGame` 쪽인 이유는 **맵이 도구를 모르기 때문**이다(`buildReady`와 같은 계약)
  - 실측(칸막이로 1칸짜리 진료실 셋을 만들어 재현): 용도 도구 → 버튼 `aria-pressed=true` 강제 켜짐 · 지정 직후 라벨 3개·틴트 30칸 · ESC로 도구를 놓자 **라벨 0·틴트 0**(방 3개는 그대로 — 세계는 안 건드린다) · 버튼으로 복귀 · 토글을 켠 채 도구를 놓으면 남는다 · 콘솔 에러 0. ⚠️ 스크린샷은 못 찍었다([T-103](troubleshooting/T-103.md)) — 위 수치는 전부 DOM 실측이다
  - ⏸ **이름표 겹침 자체는 안 없앴다** — 겹치는 양은 **방 폭**이 정하는 값이라 표시층에서 못 지운다. 바탕을 깔아 맨 위 하나는 읽히게만 했다. 없애려면 이름표를 맵 밖(인스펙트 카드·툴팁)으로 옮겨야 하는데 그건 다른 설계다
- ✅ **인사 명단에 필터 — 칩이 곧 인원수다** (2026-07-29 · TDD · 사용자 보고 *"리스트가 쭈우우욱 보여지는데 … 각 필터링에는 총 몇 명이 있는지 보기 쉽게"*): `rosterFilters(pawns, resigningIds)` 순수 함수 — **전체 → 과별(카탈로그 순서) → 상태별**(피로 위험·이번 주말 떠남·응급 끔), 인원 있는 것만. **`count` 필드가 없는 것이 설계의 핵심**이다: 칩의 숫자는 `doctors.length`이고 목록은 그 배열 그 자체라 인원수와 명단이 **갈릴 자리가 없다**(이중 기재의 가장 작은 판). 인원 0인 칩은 아예 안 뜬다 — 「떠남 0」이 뜨면 멀쩡한 병원이 사고 난 병원처럼 읽힌다(`turnAwayBreakdownText`와 같은 규칙). 덕분에 칩 줄의 길이가 곧 "지금 신경 쓸 축이 몇 개인가"가 된다. **1244 vitest**(+9)·tsc 0·eslint 0 error·시뮬 코어 0건 변경
  - **톤 — 「응급 끔」에는 경고색을 안 준다**: 피로·사직은 사람에게 일어난 일이지만 응급 토글은 플레이어의 선택이고, 붉게 칠하면 화면이 "그러면 안 된다"고 말하게 된다(패널이 카피에서 지키는 톤을 색에서도 지킨다 · 테스트가 잠근다)
  - 고른 칩이 사라지는 경우 하나(마지막 「응급 끔」을 되돌릴 때)는 `?? filters[0]`로 전체 복귀 — `useEffect` 정리를 안 두는 이유는 **그 폴백이 곧 정답**이라서다
  - 실측(6명 채용): 칩 `전체6 · 미용·피부1 · 내과1 · 외과2 · 순환기내과2`(카탈로그 순서 · 448px 카드에서 한 줄) · [외과2] → 2행 · 응급 0으로 내리자 「응급 끔1」이 중립 톤으로 등장 · 되돌리자 칩 소멸+전체 복귀 · 콘솔 에러 0
- ✅ **림월드 P1 표면 3종 — 경고 스택·키보드·인스펙트** (2026-07-29 · 스펙→Opus 위임 구현·TDD): ① **경고 스택** — `alertsOf(w)` 순수 함수. 기존 `setupWarningText` 체인을 setup 계열로 **무변 이관**(기존 테스트 0건 변경이 증거)하고 ops 3종(병상 0 danger · 피로 위험 danger · 굶주림 warn) 신설, 우측 상단 세로 스택 ② **키보드** — 스페이스(0 ↔ 직전 배속 · `toggledSpeed`) · ESC(모달 > 인스펙트 > 도구 체인 · `escTarget` — 결산 오버레이는 안 닫는다) ③ **인스펙트** — 팬 경로에서 클릭(이동 < 5px `CLICK_SLOP`) 분리 → 폰 클릭 카드. 내용은 `inspectCard(p, w)` 순수 함수 소유, 매 렌더 world에서 다시 찾아 그린다(움직이면 따라 살고 퇴장하면 저절로 닫힘). **특성·사연(traits.ts)의 첫 상시 노출 표면.** **1235 vitest**(+37)·tsc 0·eslint 0 error · **시뮬 코어 0건 변경**(`git diff -- src/sim src/game` 빈 출력 · 회귀 픽스처 무변) · 돌연변이 7종 사살(implementer 실측)
  - 구현이 스펙과 다르게 판단한 4건(전부 타당): 피로 판정을 `fatigueTone === 'RED'`로(막대와 경고가 한 함수) · 허기 판정을 공개 함수 `starvedSlowFactor > 1`로(`wantsMealNow`는 비공개 — export 추가는 시뮬 변경) · `STAGE_LABEL`을 `PatientStage` 전종(11종, Record라 tsc 강제) · 피로 수치 색 생략(머리 위 막대가 같은 신호를 이미 나른다)
  - 부모 세션 재검증: 1235 green·tsc·eslint 직접 재실행 + 브라우저 실측(채용 1명 → danger·warn 스택 동시 표시 · 의사 클릭 → 특성 2줄+사연+상태 문장+우선순위 카드 · ESC 닫힘 · 스페이스 0→1→0 · 벽 들고 클릭 → 건설되고 카드 안 뜸 · 콘솔 에러 0)
- ✅ **의자에 앉는다 — 의자만 통행 가능으로, 좌석이 의자 그 자체가 되다** (2026-07-29 · TDD · 사용자 보고 *"의자를 추가해도 캐릭터들이 의자를 사용 안 해. 의자에 앉도록 기능 구현해"*): 근인은 한 줄이었다 — `isWalkable`이 **모든 가구를 막았고**, 그래서 `standSpot`이 *"앉는다 = 의자에 인접한 첫 통행 타일에 선다"*로 각색돼 있었다(그 함수 주석이 명시하고 있었다). 폰이 설 자리가 타일뿐인데 의자 타일이 막히면 **앉음을 표현할 자리 자체가 없다**. ① **`world.blocksWalk`** 신설(`kind !== 'CHAIR'`) — `isWalkable`과 `buildBlockedSet`이 같은 한 줄을 본다 ② **`spots.occupySpot`** — 막힌 가구는 앞 타일, **안 막힌 가구는 그 위**(갈림의 기준이 종류가 아니라 **막힘**이다) ③ 대기실·휴게실·식당 좌석과 `examSlots.patientSpot`이 그것을 쓴다 ④ 화면은 앉은 폰을 **4px 들어 올린다** — 안 그러면 폰이 의자를 통째로 가려 *선 것과 구별되지 않는다*(기능만 들어가고 신고는 그대로인 상태). 앉음의 단일 출처는 **좌표**다(폰에 자세 필드를 두면 자리를 뜰 때 되돌리는 걸 잊는다). **1198 vitest**(+9)·tsc 0·eslint 0 error
  - ⚠️ **진료실이 통째로 죽을 뻔했다**([T-104](troubleshooting/T-104.md)) — 의자가 통행 가능해지자 `standSpot`이 의자를 "설 자리"로 고르기 시작했고, 자동 배치 진료실(책상이 내부 좌상단 = 위·왼쪽 벽 + 오른쪽 의자)에서 **슬롯이 아예 안 열렸다**(정원 0 = 진료 불가). 게임을 살린 것은 새 테스트가 아니라 **깨진 옛 테스트 두 건**이다. 해법은 `standSpot(blocked, at, seats?)` — *앉는 자리는 서는 곳이 아니다*. 서는 폰을 놓는 호출부 셋(`examSlots`·`day.freshMorning`·`needs.backToDesk`)이 전부 넘긴다(하나만 빠져도 그 의사는 하루 종일 슬롯 밖에 선다)
  - **회귀 픽스처를 다시 박았다** — `examsDone 3→4 · leftCount 37→36 · revenue 36→48`. 규칙이 바뀌었으니 결과도 바뀌는 것이 옳다(전환 무변 계약 위반이 아니라 **의도한 규칙 변경**). 새 `pawnTiles`가 그대로 증거다: 대기 환자 6명이 **의자 6칸에** 앉아 있다(옛 값은 전부 의자 **앞** 타일). 픽스처 머리말에 "언제 왜 갈렸는지" 각주를 남겼다
  - 실측: 입구 타일에 의자를 놓자 그 위 의사 스프라이트 `top`이 `496px → 492px`(= 31×16 − 4)로 즉시 갈렸다 · 콘솔 에러 0. ⚠️ 살아 있는 시뮬(걸어와 앉는 장면)은 이 창에서 못 봤다 — 표시 안 되는 pane은 프레임을 안 그려 `requestAnimationFrame` 클럭이 멈춘다(속도 `1×`인데 시계 09:00 정지 · [T-103](troubleshooting/T-103.md)). 그 장면은 300분 회귀 픽스처와 새 통합 테스트가 대신 잠근다
  - ⏸ **침대는 그대로 옆에 선다** — 눕기는 스프라이트가 함께 가야 읽힌다(의자는 앉은 자세와 선 자세가 화면에서 구별되지 않아 타일만으로 성립한다). `BedSprite`의 `occupied`가 아직 하드코딩 `false`인 것도 같은 자리의 미완이다
- ✅ **좌측 패널을 카테고리 아코디언으로 — [사람]·[건설]** (2026-07-29 · 사용자 지시 *"건설은 건설로 묶자… 건설말고도 다른 것도 묶을 수 있다고 판단되면 묶어"*): 평평하게 늘어선 10줄이 짧은 창에서 곧 스크롤이었다. **최상위는 이제 카테고리 둘**이고 누른 하나만 아래로 열린다 — **[사람]**(채용·인사) · **[건설]**(도구 8종 + 가격표). 기본은 전부 접힘. 도구 8종이 전부 `BUILD`인 것은 **가른 축이 "무엇을 바꾸는가"**이기 때문이다: 용도·철거도 결국 부지 편집이다. 「용도」를 최상위로 따로 세우면 그 버튼만 클릭 한 번에 **도구까지 무장하는 다른 종류**가 되어 *"카테고리는 열기만 한다"*는 규칙이 첫 줄부터 깨진다. **1189 vitest** 유지·tsc 0·eslint 0 error
  - **접거나 갈아탈 때 도구를 놓는다**(`toggleSection`) — 안 보이는 도구가 무장된 채 남으면 부지를 눌렀을 때 벽이 서는데 화면 어디에도 그 이유가 없다(팔레트가 접혀 있어 무엇을 들었는지 볼 자리가 없다). 실측으로 확인: [건설]→[벽] 선택 후 [사람]으로 갈아타면 `aria-pressed` 도구 0개 + 가격표가 사라지고 상태줄이 기본 안내로 복귀
  - **[사람]이 이탈 경고를 승계한다** — 안쪽 [인사]에만 두면 묶음이 접힌 동안 붉은 신호가 통째로 사라진다. *주중에 눈에 띄어야 대응할 시간이 생기는 것*이 그 경고의 존재 이유라, 접힌 채로 안 보이면 없는 것과 같다
  - 실측 1280×720: 기본 상태 버튼 **2개**(`사람▸`·`건설▸`) · 맵 `1017×678` @212,42 **불변**(패널 폭 160 고정) · 건설 드래그 (400,200)→(500,300) = `20칸 · 600만원`으로 이전 PR 실측값과 동일(좌표 정합 유지) · 900×600에서 기본·[건설] 열림 **둘 다 스크롤 없음**(558=558) · 콘솔 에러 0
  - ⚠️ **최심 상태는 여전히 스크롤된다** — [건설]>[용도]>[진료실]이면 버튼 20개라 `876 > 558`(900×600)·`876 > 678`(1280×720). 20줄은 **어느 배치로도** 600px에 안 들어간다. 묶음이 줄인 것은 *기본·얕은 상태*이지 최심이 아니다 — 최심까지 접으려면 용도·과를 별도 오버레이로 빼야 하는데 그건 다른 설계다
- ✅ **[채용]·[인사]도 좌측 패널로** (2026-07-29 · 사용자 지시 *"채용, 인사도 좌측 메뉴로 옮기자"*): 상단 바의 `ml-auto` 묶음에서 두 버튼을 떼어 좌측 패널 **맨 위**(도구 목록 위 · `border-b`로 구분)에 세웠다. 이로써 **판을 바꾸는 행동이 전부 한 패널에 모이고** 상단 바에는 *읽는 값(시각·금고·집계) + 시계*만 남는다 — 사람을 들이는 것은 벽을 세우는 것과 같은 행동이지 읽는 값이 아니다. 도구 **위**에 두는 이유는 순서가 곧 인과라서다(의사 없이 지은 진료실은 빈 방이고, 판을 여는 첫 동작이 채용이다) + 패널이 `overflow-y-auto`라 **맨 위가 언제나 기본 시야**다. 산술·상태·테스트 0건 변경(**1189 vitest** 유지·tsc 0·eslint 0 error)
  - **T-101 회귀 통과** — 6상태 순회(초기→도구→용도→과→과닫힘→해제)에서 패널 폭 고유값 **1개(160)** · 맵 rect 고유값 **1개(`212,42 1017x678`)**. 세로 배치라 늘어난 78px이 **높이**로 흡수되고 인셋(= 폭)은 안 흔들린다 — 하단 바 시절이라면 이 추가가 곧 배율 변화였다
  - 실측 1280×720: 헤더 `1280×42`(버튼 둘이 빠져도 줄바꿈이 없던 바라 높이 −1px) · 맵 `1017×678` @212,42(세로는 안전 영역에 정확히 fit, 가로는 중앙) · [채용]/[인사] 둘 다 패널에서 열리고 시계가 선다(`채용 중 — 일시정지`·`인사 중 — 일시정지`) · 900×600에서도 맵 `740×493` 그대로 · 콘솔 에러 0
  - ⚠️ **패널이 짧은 창에서 스크롤된다** — 600px 높이 + [용도]·[진료실]까지 펼치면 `scrollHeight 878 > client 558`이라 아래가 잘린다(사람 절 78px이 그만큼 보탠다). 두 버튼은 맨 위라 기본 시야에선 늘 보이지만, 스크롤을 내리면 상단 바 시절의 "항상 보임"은 아니다 — 실제로 불편하면 사람 절만 `sticky top-0`으로 올린다
- ✅ **줌 하한을 fit 아래로 — 시작 화면에서 죽어 있던 [−]** (2026-07-29 · TDD · 사용자 보고 *"화면이 확대만 되지 축소는 안 된다"*): 카메라의 줌 범위가 `[1, 3]`이고 **1 = fit = 게임이 시작하는 배율**이라, 하한과 기본값이 같아 첫 화면에서 [−]와 휠아웃이 **아무 일도 하지 않았다**. 확대만 되는 것으로 보인 이유가 그것이다. `ZOOM_MIN = 0.5`를 신설해 `zoomedCamera`의 하한을 `Math.max(1, …)` → `Math.max(ZOOM_MIN, …)`로 내렸다 — 부지 전체가 이미 보이는 배율에서도 둘레 여백까지 더 당겨진다. 클램프는 한 줄도 안 바뀐다: `clampAxis`가 *콘텐츠 ≤ 안전 영역이면 중앙*을 이미 계약으로 갖고 있어(T-102) 축소분은 저절로 중앙 정렬로 흡수된다. [⌂]는 여전히 정확히 fit(zoom 1)이다. **1189 vitest**(+2)·tsc 0. 실측 1280×720: 시작 `scale 1.32227` → [−]×4 → `0.661133`(= fit×0.5 하한에서 정지) · 맵 `508×339` 중앙 정렬 · [⌂]로 `1.32227` 정확 복귀 · 콘솔 에러 0
  - ⏸ **부지 확대(48×32 → 더 크게)는 안 했다** — 사용자가 *"맵을 더 크게 만들어도 돼"*로 허용했지만 축소 자체에는 필요 없고(fit 기준이라 부지가 커져도 zoom 1은 늘 전체를 보여준다), **공짜가 아님을 실측**했다: 96×64로 바꿔 돌리니 **테스트 9건이 깨진다**(events 2 · patientFlow 4 · regression 1 · week 1). 원인은 코드가 아니라 **밸런스**다 — `ENTRANCE`가 `y: GRID_H - 1`(아래 변)이라 부지를 키우면 정문이 멀어지고, 고정 좌표에 방을 세우는 픽스처들에서 환자 보행 거리가 늘어 이탈이 급증한다. 회귀 픽스처([regression.test.ts](../src/sim/regression.test.ts))는 *"손으로 맞추지 말 것"*이 계약이라 재핀은 그 픽스처의 의미를 지운다. 마감(2026-08-10) 12일 전 밸런스 변경이라 **사용자 결정 사항**으로 남긴다 — 하려면 픽스처 좌표를 `ENTRANCE` 상대로 옮기는 선행 작업이 필요하다
- ✅ **게임 이름 변경 — 「심스피탈」(SimSpital)** (2026-07-29 · 사용자 결정): 옛 이름 「수화기 너머의 벽」은 전화 협상이 핵심이던 시절의 것이라 병원 경영 시뮬로 피벗한 본편과 안 맞았다. Sim + hoSPITAL 합성. **살아 있는 표면만** 교체(게임 화면 3곳 · 제출 문서 7종 · PDF 파일명/문서 title 재생성 · 리서치 2건)하고 **역사 기록은 보존**(changeLog·plans·specs·game-concept 본문 — 대신 game-concept 이력 목록에 한 줄 추가). **1182 vitest**·tsc 0·eslint 0 error·build 통과
- ✅ **완전 림월드식 건설 재설계 — 자유 벽·영역 인식·용도 지정·가구 분리** (2026-07-28 **PR 1a·1b·2·3 완주** · 설계 확정 · 사용자 결정 *"림월드처럼 영역 선택과 벽·가구 배치를 분리하자"*): 촉발 문제는 **진료실을 아무리 크게 지어도 의사 1명만 들어간다**(가구 자동 배치 = 책상 1개 고정 + 방 단위 배정)는 것 — 크기가 비용만 늘리는 함정이다. 마감 리스크가 큰 재설계라 **PR 3개로 쪼개고 매 단계 플레이 가능**을 계약으로 둔다. 단일 출처: [spec](../docs/superpowers/specs/2026-07-28-freeform-build-design.md)
  - ✅ **PR 1a — 지형 모델·영역 인식·통행 전환**(2026-07-28 · TDD): `SimWorld`에 `walls`/`doors`/`designations` 신설 + `computeRegions`(벽·문 아닌 타일의 4방 성분 중 **격자 가장자리에 안 닿는 것** = 방, 문은 경계) + 통행 판정을 방 사각형에서 **벽 타일**로 전환. `placeRoom`은 **호환 어댑터**로 남아 두 모델을 동시에 낳는다. **게임 동작 무변이 계약**이라 기존 테스트 기대값 0건 변경. **1106 vitest**(+16)·tsc 0·eslint 0
  - ✅ **PR 1b — 소비자 영역 기반 전환**(2026-07-28 · TDD): 스팟·환자·응급·욕구·이벤트·HUD·TileMap이 `rooms` 대신 **영역**을 읽는다. `Furniture.roomId` 제거(소속은 좌표가 말한다) · `Pawn.roomId`는 영역 id 문자열로(PR 3에서 `deskAt`로 대체) · `insideRoom(rect)` → `region.tiles.has` · TileMap이 사각형 대신 **벽·문·영역 타일**을 그린다. 규칙 무변을 300분 회귀 픽스처(전환 **전** HEAD 실측값 하드코딩)로 잠갔고 기존 기대값 0건 변경. ⚠️ **성능 함정**: 틱마다 `computeRegions`를 부르니 주 종주가 103ms → 3.0초가 됐다(week.test 3건 타임아웃) — 입력 3필드 **identity 키** 한 칸 memo로 157ms 복귀([T-100](troubleshooting/T-100.md)). **1120 vitest**(+14)·tsc 0·eslint 0
    - ⚠️ **알려진 차이 2건**(둘 다 결정론 유지): ⓐ 같은 과 진료실이 여럿일 때 의사 배정 순회가 **건설 순서 → 좌표 순서**로 바뀐다(영역 배열이 id 오름차순 — 처리량은 같고 누가 어느 방에 앉느냐만 갈린다) ⓑ 벽 편집으로 영역이 갈라지면 저장된 `Pawn.roomId`가 끊길 수 있다 — `regionById`가 `undefined`를 내고 배정이 다음 분에 다시 붙는다(던지지 않는다)
  - ✅ **PR 2 — 건설 UI 분리**(2026-07-28 · TDD): 도구 팔레트 `[벽][문][책상][의자][침대][카운터][용도][철거]` + 비용표 단일 출처 `BUILD_COST`(벽 30·문 50·책상 100·의자 20·침대 300·카운터 100 만원 · 철거 환불 = 내림 절반) + **철거 신설**(1주차 이월 항목 해소). `placeRoom`·`autoFurniture`·`MIN_ROOM_W/H`·`COST_PER_TILE_MANWON`·`roomCostManwon`·`wallTiles`·`doorTile`·`Room` 타입·**`SimWorld.rooms` 필드**를 프로덕션에서 제거하고, 옛 사각 방은 **테스트 픽스처**(`testHelpers.placeRoom` — 새 프리미티브 조합)로만 남겼다. 상태줄·토스트·미리보기 문구는 전부 `simHud` 순수 함수라 테스트가 겨눈다. **1157 vitest**(+37)·tsc 0·eslint 0 error·build 통과
    - ⚠️ **회귀 픽스처의 축을 금고 → 수입 차액으로 바꿨다** — 비용표가 튜닝 대상이라 절대 금고를 박아 두면 밸런스를 만질 때마다 회귀가 운다(잡음). 시뮬이 낳는 값은 300분 동안의 **수입 36만원**뿐이고 그건 전환 전과 같다(examsDone·leftCount·폰 좌표도 전부 무변)
    - ⚠️ **벽 도구는 문·가구 위를 건너뛴다** — 그래서 "벽으로 문을 덮어 봉인"은 이제 못 한다(철거 후 벽 세우기로 대체). 기존 봉인 테스트들은 문 **바깥** 칸을 덮는 방식이라 그대로 산다
  - ✅ **PR 3 — 진료 슬롯 다의사**(2026-07-28 · TDD): **책상 수 = 의사 수**. `examSlots(w, region)`(spots.ts)가 (책상·환자의자·의사스팟·환자자리) 삼중을 열거하는 **단일 출처**이고 배정·라우팅·경고가 같은 표를 본다. `Pawn.roomId` → **`deskAt: Pt`**(점유의 단일 출처 — 같은 좌표 이중 배정이 구조적으로 불가능) · `regionById` 제거 · `furnitureSpot` → `standSpot`(좌표 하나만 받는다). **의자 없는 책상은 슬롯이 아니다** — "책상만 놓으면 안 되고 의자를 붙여야 한다"가 배치 인과로 드러나고, HUD가 「의자 없는 진료 책상 N개」로 말한다. **1166 vitest**(+9)·tsc 0·eslint 0 error·build 통과
    - ⚠️ **철거로 슬롯이 사라지면 배정이 풀린다**(신설): 안 풀면 없는 책상을 문 의사가 영원히 놀고 되돌릴 방법이 없다 — 철거는 PR 2에서 생긴 상시 도구라 이제 도달 가능한 상태다. 옛 `roomId` 시절의 "끊긴 배정" 함정(위 PR 1b ⓑ)이 여기서 닫힌다
    - ⚠️ **이 마일스톤은 여기서 끝난다** — 촉발 문제("크게 지어도 의사 1명")가 해소됐다
- ✅ **시뮬 1배속을 도보 속도로 — 페이싱 상수 분리** (2026-07-28 · TDD): 사용자 피드백(*"진짜 사람들이 걸어다니는 것처럼"*)에 따라 시뮬 전용 `SIM_MS_PER_GAME_MIN = 600`을 `useSimClock.ts`에 신설(50→600ms/게임분 · 1배속 초당 약 3.3타일 · 하루 약 6분). 클래식 판의 `MS_PER_GAME_MIN`(50)은 통화 재생 페이싱·`flowStepCount` 불변식이 걸려 있어 **분리**했고, 두 상수를 한 테스트에서 함께 잠갔다. 코어 시뮬 0줄. **1090 vitest**(+1)·tsc 0·eslint 0
- ✅ **온보딩 침묵 무반응 수정 — "캐릭터가 안 움직인다"의 진짜 원인** (2026-07-28 · TDD): 사용자 버그 신고를 조사하니 시뮬·이동·렌더는 전부 정상이었고(1062 green·배포본에서 진료 9건 실측) 무너진 자리는 **첫 5분의 침묵**이었다 — 과를 안 고른 채 부지를 끌면 드래그가 조용히 먹히고(미리보기·토스트 0), 하루가 1배속 30초라 파악하는 동안 하루가 끝나며, 채용한 의사가 전원 같은 칸에 겹쳐 서고, 방 없는 의사·대기실 없는 병원이 아무 표시 없이 멈춰 있었다. 수정 5종: 안내 토스트(`buildBlockReason`)·**첫날 일시정지 시작**·스폰 분산(`spawnSpotNear`가 폰 점유 타일을 비켜 선다)·정지 문구를 헤더에서 **footer 예약 줄로 이동**(드래그 시작 시 헤더 줄바꿈이 맵을 32px 내려 좌표가 2타일 어긋났다 — [T-099](troubleshooting/T-099.md))·사유 표시(`doctorRoomlessMark`·`setupWarningText`). 표시 판정은 전부 `simHud.ts` 순수 함수라 우선순위 체인까지 테스트가 겨눈다. **1089 vitest**(+27)·tsc 0·eslint 0. 함정 2건([T-098](troubleshooting/T-098.md)·[T-099](troubleshooting/T-099.md))
- ✅ **사직·이탈 — 사람이 떠나면 장부가 좋아진다** (2026-07-25 설계·구현 완료, brainstorming→writing-plans→TDD): 피로 포화로 마감한 날이 누적되면(`RESIGN_SATURATED_DAYS`) 주간 결산에서 사직하고 **전국 풀에서 영구히 사라진다**(다른 병원 이직이 아니라 필수의료를 떠남 — 재채용 불가). 완편 병원은 카운터가 영원히 0이라 구조적으로 망가진 배치에서만 돈다. 핵심 함정은 id 재번호(생존자가 사직자 포화를 상속) — `materializeRoster`의 CANDIDATES 순서 보존을 이용한 과별 위치 리맵으로 해결(불변식 R-4). **의도된 결과: 필수과가 떠나면 고정비·적자 콜이 줄어 다음 주 흑자가 오른다**(실측 W2 −55 → W3 +1,260만원, 해석 카피 0). **551 vitest**·tsc 0·lint 0·next build + 브라우저 실측(사직 줄 렌더·풀 4→3·순환기 0명에서 STEMI 하드락·콘솔 0). 설계 [spec](../docs/superpowers/specs/2026-07-25-attrition-resignation-design.md)·[플랜](../docs/superpowers/plans/2026-07-25-attrition-resignation.md) ⏸ 후속: 사직 신문 기사·전 배후과 0명 결말·소송 방아쇠 축
- ✅ **피로 승격 — 피로가 판정을 먹는다** (2026-07-25 설계·구현 완료, brainstorming→writing-plans→TDD·Fable/Opus 분업): 표시 전용이던 피로가 `occupiedUntilMin`에 합류해 진료 소요를 늘린다(34 이하 1.0 → 100에서 ×1.5, 연속·단조). 전제 수리 2건 — 피로 입력을 **표준강도분**(점유 분 × 강도: 미용 0.3/응급수술 2.0, 상대가치점수 업무량 근거)으로, 수용 로그에 `assigneeId`·`endMin`(피로 장부=판정 장부). 실측: 1인 순환기 주 내내 레드존·2주차 이월 82+(갈려나감), 2인 분담 감속 지대 밖("채용이 곧 회복"), 미용·검진 무풍. `adjudicate.ts` 0줄. **523 vitest**·tsc 0·lint 0. 설계 [spec](../docs/superpowers/specs/2026-07-25-fatigue-adjudication-design.md)·[플랜](../docs/superpowers/plans/2026-07-25-fatigue-adjudication.md)·[T-078](troubleshooting/T-078.md)·[T-079](troubleshooting/T-079.md) ⏸ 후속: ~~사직·이탈(피로 포화 × 인력 제로섬)~~ → ✅ 아래 「사직·이탈」로 실현 · 주간 결산 피로 요약
- ✅ **병원 맵 개편 — 안/밖 분리·의사는 진료실 안·대기실 동선** (2026-07-24 설계·구현 완료, TDD): 맵이 단순하고 의사·환자가 복도에 나란히 줄 서 구분이 안 되고 행인이 병원 안에 섞이던 문제(사용자 지적)를 공간 재구성으로 풀었다. 병원 밖 **거리 밴드**(외벽으로 안과 갈림 — 보행자 여기서만, 좌·우 양방향)·건물(진료실 가구+대기실/병동 분할+입구)·아바타 한 레이어. 의사는 **항상 자기 진료실**(복도 zone 제거, 소등되면 퇴근), 환자 동선은 대기실 의자→진료대→병동 침대→입석. `AvatarZone` 재편(`CORRIDOR`→`ROOM`/`BED`/`WAITING`), 가구·아바타 좌표 함수 공유. 판정·경제 0 침습. 503 vitest·tsc 0·DOM 기하 실측. 설계 [character-design.md](../docs/concept/character-design.md)
- ✅ **과별 손익 영수증 — 하루·주간 마감에 과별 내역** (2026-07-24 설계·구현 완료, brainstorming→TDD): 순이익 큰 숫자 하나뿐이던 수익/손해 표시를 과별로 쪼갰다(사용자: *"어느 과가 얼마나 손해 내는지 모르겠다"*). 하루/주간 마감에 `과·환자수 | 진료 | 고정비 | 순익` 4열 표 — 하루 마감에 과별 스냅샷(`DayRecord.deptStats`)을 굳히고 공유 순수 함수 `deptLedgerLines`로 하루(1일)·주간(7일)이 같은 표를 그린다. 고정비는 저장 안 하고 렌더 파생(economics.segments÷7). 판정·경제 0줄, 신규는 `deptLedger.ts`(deptDayStats·deptLedgerLines)·`DeptLedgerTable.tsx`뿐. 불변식 2개(과별 진료 합=netProfitDelta · 과별 순익 합=일별 순이익 합, 오차 0) TDD. 500 vitest·tsc·eslint 0 + 브라우저 실렌더(순환기 최대 적자·미용만 흑자, 모바일 375px 무넘침). 설계 [spec](../docs/superpowers/specs/2026-07-24-dept-ledger-receipt-design.md)
- ✅ **채용 데스크 v2 — 빈 병원 맵 허브(무스크롤 1화면)** (2026-07-24 설계 확정 → 2026-07-24 구현 완료, brainstorming): 사용자 요구 ①데스크톱 무스크롤 1화면 ②웹이 아니라 게임답게를 충족했다. v1의 세로 아코디언을 버리고 **개원 전 빈 병원 맵**을 화면 전체로 — 현판(이름 입력 통합·NAME 화면 삭제) + 맵(방 클릭→지원서 4장 중앙 오버레이·도장 즉시 그 방에 아바타) + 하단 바(장부·자격·개원). 신규는 `deriveSetupScene`(순수 파생)·`HospitalMap.onRoomClick`(옵셔널)뿐, 판정·경제·채용 로직 0줄. 설계 [spec](../docs/superpowers/specs/2026-07-24-hiring-desk-map-ui-design.md)
  - ✅ Task 1 `deriveSetupScene` → ✅ 2 `HospitalMap.onRoomClick` → ✅ 3 SetupWizard 맵 허브 재작성 → ✅ 4 문서·게이트·브라우저 실측(무스크롤)
- ✅ **채용 데스크 — SETUP 위저드 게임화(지원서+도장)** (2026-07-24 설계·계획 확정 → 2026-07-24 구현 완료, brainstorming→writing-plans→SDD): 과별 +/- 카운터를 지원자 32명(과별 4명·신입/중견/베테랑) 고정 풀의 지원서 더미로 바꿨다 — 픽셀 초상 변주·연차·경력·요구 계약금을 보고 도장으로 채용, 베테랑은 계약금 ×1.3에 진료 소요 ×0.8. `hiredIds`는 추가 필드고 `doctors` 카운트는 파생이라 성장·경제 소비처 무수정, 판정 침습은 `occupiedUntilMin` 한 곳. character-design §2·§6 부분 해제 포함(성격·사연 금지선 유지). 설계 [spec](../docs/superpowers/specs/2026-07-24-hiring-desk-setup-design.md)·[플랜](../docs/superpowers/plans/2026-07-24-hiring-desk-setup.md)
  - ✅ Task 1 지원자 풀 `candidates.ts` → ✅ 2 타입·명단 → ✅ 3 예산 → ✅ 4 진료 속도 → ✅ 5 초상 변주 → ✅ 6 채용 데스크 UI → ✅ 7 문서·게이트
- ✅ **재미 개선 3종 — 응급 결정화 · 대가 가시화 · 폐업** (2026-07-24 설계 확정 → 2026-07-24 3종 전부 완료, brainstorming): 플레이테스트+코드 전수 진단 — 실질 결정이 전부 개원 위저드에 몰려 있고 7일 루프는 그 결과의 결정론적 재생(관람)이다. 응급이라는 감정적 핵심에 플레이어 개입을 되돌린다. 독립 PR 3개 순차(앞의 것만 머지돼도 게임 성립). 설계 [spec](../docs/superpowers/specs/2026-07-24-emergency-agency-fun-design.md)
  - ✅ **PR 1 — 응급 결정화 + 카운트다운** (2026-07-24, 상세 changeLog): `decide(state, action: DecisionAction)` — 응급도 플레이어가 받기/돌려보내기를 직접 누른다. 하드락(배후과 없음·야간 당직 공백·과밀)은 액션과 무관하게 이긴다. 만료(`TIMEOUT`)는 신설 사유 `UNANSWERED`(응답 없음)로 기록, 하드락이 있으면 하드락 사유가 우선. `needsDecision`이 응급 6종을 포함하도록 넓어져 응급이 흐름을 멈추고 카드를 세운다(하드락이어도 카드는 뜨고 [받기]만 잠긴다). 응급 카드에만 실초 15초 카운트다운(UI 전용, 코어는 실시간 모름).
  - ✅ **PR 2 — 트레이드오프 + 대가 가시화** (2026-07-24, 상세 changeLog): 예약 카드에 수락 시 담당의 점유 시각·[받기] 비활성 사유를 미리 보여주고(대가 가시화), `busyWith`(점유 원인 추적)를 발판으로 `canBump`·`bumpTarget`가 하드락이 아니라 대기(인내 한계 이내)로 막힌 응급에서만 밀어낼 선택진료 예약을 찾는다. `BUMP_ACCEPT`가 그 예약을 중단(수익 회수)하고 대기를 건너뛰어 응급을 받는다 — 하드락(배후과 없음·야간 당직 공백·과밀·대기 한계 초과)은 이 버튼으로도 못 뚫는다.
  - ✅ **PR 3 — 폐업** (2026-07-24, 상세 changeLog): `insolvencyStreak`(금고 음수인 채 연속 주 수, `completeWeek`에서 `treasury < 0 ? +1 : 0`) · 두 주 연속이면 `nextWeek`이 throw로 봉쇄하고 `SessionEpilogue.closed=true` · WeekSummary가 streak 1(경고)/2(폐업, 「다음 주」 미렌더)로 은행 통지를 갈라 보여주고 Epilogue 머리글이 "병원이 문을 닫았다"로 바뀐다. 흑자 복귀 시 리셋, 주차는 무한 유지.
- ✅ **수익 구조 재설계 — 부문 손익을 '버는 돈'에서 '고정비'로** (2026-07-23 완료, 상세 changeLog): `fixedCostPerDoctorManwon`(항상 양수)로 뒤집어 부문 손익에서 수익을 없애고 `CALL_ECONOMICS`를 유일 수익원으로. 단위 억 → **만원**(보톡스 30만원 / 응급 PCI 850만원 / 개원 자본 5억 / 병상 증설 1.0억). 과별 흑자·적자가 입력이 아니라 계산에서 창발한다. 실측 7일 완주: 공범 전부수용 +2,430만원 · **전부거절 −2,940만원**(전엔 +303억) · 양심 −3,990만원. 가드: `[I8-a]` 전부 거절하면 반드시 적자 · `[I8-b]` 부문 손익은 어떤 빌드에서도 ≤ 0. [T-071](troubleshooting/T-071.md)
  <details><summary>착수 시점 기록(원문)</summary>

  🔜 **수익 구조 재설계 — 부문 손익을 '버는 돈'에서 '고정비'로** (콜 제한 폐지에서 파생, 2026-07-23 사용자 결정으로 분리): 지금 `DEPARTMENTS.profitPerDoctorBillions`는 *"미용 의사 1명이 주 70억 번다"*는 **입력값**이라 진료를 한 건도 안 봐도 흑자가 난다(실측: 전부 거절해도 주 303억). 이걸 **그 과의 고정비**(인건비·장비·당직)로 뒤집고 콜 델타를 유일한 수익원으로 두면, 과별 흑자·적자가 입력이 아니라 **계산에서 창발**한다 — [receiving.ts](../src/game/receiving.ts)의 `WORKUP_ECONOMICS` 주석이 이미 요구하는 그것. 동시에 **단위를 현실화**한다(*보톡스 한 건 1억*은 각색을 넘어 틀렸다 — 사용자 지적). ⚠️ 파급: `setup.ts`(과 카탈로그)·`world.ts`(재정중립 패키지가 그 값을 조정)·`ledger.ts`(에필로그)·`growth.ts`(**금고·병상 증설 60억을 함께 재조정해야 성장 루프가 유지된다**). 선행 완료: 워크인 델타 0으로 이중 계상 임시 봉합([T-069](troubleshooting/T-069.md)) + I8 회귀 테스트. 근거: [spec §8](../docs/superpowers/specs/2026-07-23-unbounded-arrival-flow-design.md)

  </details>
- ✅ **콜 제한 폐지 — 북적이는 병원**: 하루의 끝을 개수(`index >= queue.length`)에서 **시각**으로. 도착을 균등 슬롯에서 하루 전체 seed 난수로(뭉침), 못 받는 환자를 즉시 지우는 대신 **기다리게**(`LEFT_WAITING`), 대기 인원을 맵 복도에 파생 표시. 하루 17통 → **62~142명**. 결정이 없는 콜은 흐르는 동안 자동 처리돼 「계속」 연타가 사라졌다(멈추는 자리 = `needsDecision`). (2026-07-23, 상세 changeLog)
- ✅ **문서 부채(슬라이스 A/B 이월)**: README·docs/submission(ai-usage-doc·submission-plan)·docs/concept(ai-scenario-generation·game-concept) 5종의 '골든타임 뺑뺑이 간판'·삭제 모듈 거짓 현재서술을 받는 쪽 통합·응급 다양화로 정합(spec §8). 역사 문서는 전환 배너+단일 출처 포인터로 처리. (2026-07-20, 상세 changeLog)
- **F1~F3 — 필수의료 딜레마 장치**: 콜당 수가/원가("가격을 누가 정하나"가 부호를 가름) / 24시간 당직(F1b) / 검사+boarding(인과 사슬 닫힘) / 다음날 아침 신문. PR #38~#41. **📌 구현 후 설계 근거는 [spec §13](superpowers/specs/2026-07-17-essential-care-economics-devices-design.md)**
- **F4 — 가산금 배분(흉부외과)**: 🔴 **절단**(PR #44). 마감이 아니라 **사실이 세 번 거부** — 흉부외과는 1막에서 도달 불가 화면이었고, 역할을 주려 한 STEMI 게이트·응급 CABG·심장통합진료 삭감이 셋 다 거짓. 근거: [thoracic-surgery-role.md](../docs/research/thoracic-surgery-role.md) · [T-049](troubleshooting/T-049.md)
- **디자인 테마 Plan A — "의무기록·차트" 시스템**: 방향 확정(5안 심사)·토큰/폰트 3계층 기반(+Arial 버그 수정)·원형(TransferRound)/정점(Epilogue) 리스킨·안전한 카피. PR #49. 근거: [spec](superpowers/specs/2026-07-18-design-theme-dossier-design.md)·[Plan A](superpowers/plans/2026-07-18-design-theme-dossier-foundation.md)
  - ✅ **디자인 Plan B**: 나머지 전 화면(랜딩·세계이벤트·위저드·재투자·콜접수·하루마감·주간결산) 토큰 교체 + **장부 종이화(SegmentTree+LedgerPanel+CheerfulLedger 한 단위 — Plan A에서 이 셋 중 하나만 종이잉크로 바꿨다가 dark-on-dark로 되돌린 자리)**. zinc/emerald/red 원시 유틸 255곳 → 0곳. 종이는 §6대로 절제(화면당 1장), 판정은 도장+글자, 이모지 제거. 대비는 캔버스 합성으로 전수 실측(실패 0). (2026-07-23, 상세 changeLog)
    - ⏸ **원내 응급**: 화면 자체가 없다(응급 재설계 A에서 2막 미니게임 은퇴). 되살아나면 그때 같은 토큰으로.
  - ⬜ **카피/톤 세션(범위 밖 보존)**: 랜딩 태그라인 둘째 문장·Epilogue 헤드라인/부제·transfer placeholder 재작성 + 기관명 가공. 시각과 분리(게임 '내용' 변경).

---

## 🔑 선결 — 사용자 확인/결정 (코드 아님 · 내가 대신 못 함)

- ⬜ **제출 요강/폼 전문 정독** → **미확인 항목 목록은 [requirements.md 「❓ 아직 확인 못 한 것」](../docs/submission/requirements.md)이 단일 출처다**(여기 다시 적지 않는다). 확인한 항목은 그 문서에서 체크한다. — *최우선이던 「1번 비고 `GitHub Pages`가 예시인지 지정인지」는 **답을 기다리는 대신 Pages 배포를 추가해** 어느 쪽이든 충족하게 만들어 목록에서 내렸다(2026-07-22).*
- ✅ **Anthropic 콘솔 결제/크레딧 활성 확인** (2026-07-28 사용자 확인 — 실호출 포함으로 PR D 착수). ⬜ **남은 것은 Vercel 환경변수 `ANTHROPIC_API_KEY` 등록 + Firewall rate-limit 룰 + 콘솔 지출 상한**(위 PR D 「사용자 몫」 ⓐⓒⓓ)
- 🔴 **FAQ '유료 API 부담 주체' 문의 — 불필요해짐**: 키 없이도 폴백 문장으로 완주하게 만들어 **질문 자체를 무력화**했다(Pages/Vercel 이중 배포와 같은 수법). 근거는 [requirements.md](../docs/submission/requirements.md)의 해당 미확인 항목에 달아 뒀다

## 크리티컬 패스 — 남은 것 (상세: [submission-plan.md](../docs/submission/submission-plan.md))

- ⏸ **엔딩 누적 결산형 — 이번 주 신문 아카이브**(옛 판 `/classic` 대상이라 **크리티컬 패스에서 내림**): 에필로그에 7일 누적 돌려보낸 STEMI를 신문 헤드라인 아카이브로. 2026-07-28 랜딩 스왑으로 본편이 타일 병원이 됐고, 본편은 **엔딩 3종(`INSOLVENCY`/`NO_PEOPLE`/`CAMPAIGN_END`)의 결말문**이 이 자리를 대신한다. 북극성 「누적 결산형」(엔딩은 쟁취 아닌 필연)은 그대로 계승됐다. 근거: [game-concept.md §7 엔딩 구조](../docs/concept/game-concept.md)
- ✅ **주 반복 루프 — N주 확장 첫 삽** (2026-07-18, PR 예정): 게임이 한 주로 안 끝나고 `WEEK_SUMMARY`에서 **다음 주**(다음 세계 이벤트를 현재 세계에 누적·같은 병원으로 재개) / **종료**(`EPILOGUE`). 매주 재구성되는 세계를 여러 주에 걸쳐 노출 — "한 주면 끝나 변화가 안 보인다"(사용자) 해소. 응급 매주 반복(사용자 선택). 에필로그 장부는 **최종 주** 결산(N주 누적 장부는 구조 손익 ×N 스케일 + 일회성 채용 분리 필요 → ⏸ 후속); 주 간 누적은 WEEK_SUMMARY의 `cumulativeNetBillions`로 노출.
- ✅ **① GitHub Pages 배포 추가** (2026-07-22) — 요강 1번 비고(`GitHub Pages (링크)`)가 예시인지 지정인지 **확인을 기다리지 않고** 양쪽을 다 만족시켜 리스크를 없앴다. 이 게임은 API 라우트·서버 액션·동적 서버 API가 0개인 순수 클라이언트 앱이라 **코드 0줄 수정**으로 정적 export가 됐다 — `PAGES_BASE_PATH` 하나가 스위치([next.config.ts](../next.config.ts)), env 없으면 Vercel 빌드는 종전과 동일. → https://goospel.github.io/hospital-sim/
- 🔜 **③④ PDF 최종화** (P7, 8/3~8/7) — 2026-07-28 **본편 기준으로 전면 재작성 완료**(③ game-intro 전면 · ④ ai-usage-doc 물증 갱신 · `npm run pdf` 2종 재생성). **남은 것만**: ~~ⓐ 저장소 공개 URL 확정치 기입~~(완료 2026-07-22) ⓑ **스크린샷 7장 재캡처**(본편 `/` 기준 — 자리와 재캡처 표식은 이미 박혀 있다) ⓒ ④§6-4 **실호출 지연·usage 토큰**(키 등록 후 — 무키 강등·프록시 보안 4경우·재시도 상한은 이미 실측 기입됨) ⓓ **`ai-usage-directing.html` 갱신**(이번 재작성 미반영) ⓔ 제출 요강 재확인(마감 시각·타임존·빌드 제출 형식)
- ✅ **U2 실LLM — PR D가 삼켰다**(2026-07-28): 「거절 대사 1종만」의 극소판을 건너뛰고 **스토리텔러 프록시 전체**(director·letter·epilogue 3태스크 · structured output · 판당 상한 10 · 전 실패 폴백 강등)가 섰다. 남은 것은 코드가 아니라 **키 등록**(위 선결)이다. 왕복 지연 실측은 그 뒤 ⓒ에서
- 🔜 **R3.7 research 나머지 26건 재검증** (P7 직전, 8/1~8/3) — "이 문장을 문서가 **의도적으로 비판**한 것인가" 렌즈를 추가해 다시 돌리거나 건별로 사람이 판정. **지금 목록을 그대로 적용하면 리서치를 코드에 맞춰 파괴한다.** 살릴 후보: `essential-care-economics` 내부 모순 3건(:42·:93·:99) · `fee-schedule:248` 단위 표기 · 깨진 앵커 2건

### P6. 플레이 영상 (8/1–8/3) — ② · **남은 크리티컬 패스 둘 중 하나**
- ⬜ **본편(`/`) 배포본으로 30~60초 촬영** → 유튜브 업로드 (시드 고정 = 자동 성립). 랜딩 스왑이 머지된 뒤 Pages `/`에서 — 촬영 대상은 짓고·뽑고·아침 사건·결산/사직 편지 흐름
- ⬜ **스크린샷 7장 재캡처**(③④ 제출 문서용) — in-app 브라우저 `screenshot`이 이 환경서 스톨([T-034](troubleshooting/T-034.md))이고 pane 미표시면 프레임 자체가 안 온다([T-086](troubleshooting/T-086.md)·[T-095](troubleshooting/T-095.md)) → **사용자 로컬 화면에서**. 대상 목록은 게임 소개 문서의 재캡처 표식이 단일 출처다
- ⚠️ "라이브 LLM 구간"은 **Vercel 키 등록 후에만** 촬영 가능 — 미등록이면 폴백 문장으로 촬영한다(판정은 어차피 코드라 게임 동작은 동일하고 문장만 갈린다)

### P7. 두 PDF (8/3–8/7) — ③④ · **남은 크리티컬 패스 둘 중 하나**
- ✅ ③ 게임 소개 — 본편 기준 전면 재작성(2026-07-28). ⬜ **스크린샷 삽입만 남음**(P6)
- ✅ ④ AI 활용 기술 — 디렉팅 B축 스파인 유지 + 물증 갱신(2026-07-28 · 「같은 경계를 두 번째로 그었다」·§6-4 실측/미실측 분리·§3-1 트레일러 집계 재실행). ⬜ **실호출 지연·usage 기입 + `ai-usage-directing.html` 갱신**만 남음

### X8. LLM 2콜 풀버전 — ✅ **PR D로 실현**(2026-07-28)
- ✅ LLM 프록시(`/api/storyteller` — 서버 키·`pageExtensions` 스위치로 Pages 빌드에서 라우트 제외·CORS + 같은-오리진 허용) + 스토리텔러 3태스크(director·letter·epilogue)
- ✅ **정합성**: LLM이 상태 전이를 못 바꾸는 것이 **타입 수준**이다 — 돌려주는 것은 선택지 enum + 문자열뿐이고, 효과·전제·수치는 `events.ts`가 확정한다. `eligible` 대조·`applyEvent` 전제 throw가 그 위의 두 관문
- ✅ 판당 호출 상한 10 + 무키/타임아웃/refusal/파싱/잘림 **전 실패 한 경로 폴백 강등** + HUD 배지
- ⬜ **남은 것은 실호출 물증뿐** — 키 등록 후 프롬프트/응답 예시·지연을 ④에 기입(P7 ⓒ)

### Z9. 버퍼 + 최종 제출 (8/8–8/10) — 코드프리즈 · 의존: **P6·P7만**
- ⬜ 배포 빌드 최종 QA(완주·콜드스타트·모바일·**폴백 강등**) + 4종 링크 전수 점검 → 폼 제출
- ⬜ 신규 기능·영상 재편집 금지

---

## ⏸ 보류 — 지금 안 하는 이유를 보존

### 최소 제출선 이후 / v2
- ⏸ 거절 병원이 붉게 꺼지는 **간이 지도** — 구현비 대비 임팩트 낮음, 리스트로 충족
- ⏸ 지방 거리·이송시간 축 / 즉사→연속 경사 예후 / 형사우려·방어진료 — 리서치엔 있으나 최소 제출선 이후로 보류
- ✅ **지역 3계층 세계 시뮬 — 최소 슬라이스** (2026-07-27): 수도권/광역시/지방의 과별 의사 수가 매주 **결정론 드리프트**(lawsuitRisk 가중 RURAL→CAPITAL 이동)와 **이벤트 쇼크**(`RegionEffect`)로 굴러가고, 두 채널로만 플레이어에게 도달한다 — ① **전원 콜 구성**(`transferPressure` 0..1 → RURAL발 응급 비중·중증도) ② **채용 풀**(`pool ≡ hirablePool(world.regions)` 파생, `backgroundAttrition` 흡수). 주제 ③ 「지방 의료 공백」의 첫 메커니즘이고 판정 경로(`adjudicate.ts`)는 0줄. **570 vitest**. 설계 [spec](../docs/superpowers/specs/2026-07-26-region-world-sim-design.md)·[플랜](../docs/superpowers/plans/2026-07-26-region-world-sim.md)·[T-080](troubleshooting/T-080.md)
- ⏸ **지역 집계 장부** — 여러 병원 합산으로 "시스템 전체가 그렇게 굴러간다"를 형상화. 지역 **수치**는 위 슬라이스로 생겼지만 병원 개체는 여전히 플레이어 1개다 — 다병원 개체·합산 장부는 그대로 보류
- ⏸ **`region.ts` 분리 (리팩터 후보)** — `world.ts`(407줄)가 **세계 시뮬 · 이벤트 카탈로그 · 채용 차감 · 표시 데이터** 4관심사로 커졌다(이번 슬라이스의 품질 리뷰가 3회 반복 지적). 절단선: *지역 데이터+드리프트+`transferPressure`* ↔ *이벤트 카탈로그*. `receiving.ts`의 `originRegionFor`/`originLabelFor` 소재도 같이 판단한다 — `REGION_LABELS` 의존을 한쪽으로 모으는 문제다. 동작 무변경 리팩터라 마감 전 우선순위는 낮다
- ⏸ **지역 드리프트 페이스 (밸런스 후속)** — RURAL 13명이 드리프트 단독 기준 **10주차에 소진**되고(이벤트 쇼크가 겹치면 더 빠름) 그 뒤 세계가 정지한다(압력 1.0 포화 = "지방 소멸 완료" 상태). 2026-07-27 결정으로 **의도 수용** — 현 게임 길이에선 도달하지 않고, 포화 자체가 주제에 맞는 종점이다. 장기 플레이로 확장하면 **유입**(신규 배출·귀향)·배출 축이 필요하다
- ✅ **재투자·조작 루프 코어 실현** (2026-07-21, 위 완료 지도 「병원 성장 시스템」) — 금고 재투자·병상 증설·전국 풀 제로섬으로 "내가 만든 벽에 내가 부딪힘"을 실현. ⏸ **잔여**: 상급종합 지위(④+② 정점)·③ 지방 **공간화**(지도·거리·다병원 개체·플레이어 지역 선택 — **수치층만** 2026-07-27 슬라이스로 실현, 공간·개체는 미착수)·개원 채용의 풀 차감·리치한 소송 이벤트·다병원 지역 집계
- ⏸ **changeLog 4필드 스키마** — 효과는 실측됐지만(항목당 6.8배→1.6배) 남은 PR 5~10개면 95~114KB가 돼도 **역순이라 아무 일도 안 일어난다.** 마감 전엔 불필요, 본선 확장 카드

### F 딜레마 — 남은 갈래
- ✅ **순환기 상한 재검토 — 안 좁힘(상한 3 유지)** (2026-07-18): 3명째는 코드에선 강지배지만 현실에선 동시 진료 처리량·당직 로테이션을 산다 → 좁히면 [T-042](troubleshooting/T-042.md)("코드가 표현 못 하는 걸 현실에 없는 것으로 착각")를 반복. 축 부여(B안=동시 처리량 모델링)는 ⏸ 본선 확장. 근거는 코드 옆 단일 출처(`setup.ts` `MAX_DOCTORS_PER_DEPT` 주석)
- ⏸ 7일차 거절의 **결말 후속 기사**("3년 만에 송치" 구조 — 그 의사가 플레이어). F4 절단됐으니 독립 판단, 인과는 F1~F3로 이미 닫혀 급하지 않음
- ⏸ **워크인 자격 (경영확장 3단계)** — 미용·검진 의사 ≥1이라야 워크인 수용(`revenueCare` 신설). 딜레마엔 영향 없어 뒤로
- 🔴 **기각된 답 — "위저드 상한 1"** (이력 보존, T-042): 코드가 이진값이니 게임도 이진으로 맞추자는 것 → **코드의 한계를 현실의 진실로 착각**(T-039 단위 혼용과 같은 계열). 사용자가 기각 — *"현실에선 순환기 의사가 2명 배치될 수 있잖아. 한 명으로 강제하면 게임이 저렴해 보인다."* 답은 이미 레포 안 리서치에 있었다(24시간 중재 순환기 PCI 병원 · 당직 1명이 3명 동시진료 · 인력부족 사유 2.3배)

### 마감 후 확장 (보류 · 옛 이름 「본선 48시간 확장」)

> ⚠️ **전제 정정(2026-07-30 · 사용자 발견)**: 본선(9/4~6 판교)은 **주제가 현장에서 공개되는 48시간 신작 개발**이고 제출물도 다르다(게임 프로토타입 · 에이전트 설계서 · 디렉팅 명세서 — 언론 보도 교차 확인). 사전 과제 게임을 이어 만든다는 근거는 없다. 따라서 아래 항목들의 "(본선)" 표기는 전부 **"마감 후"로 읽는다** — 본선에 이어지는 것은 게임이 아니라 방법론(AI 권한 봉인·결정론 판정·TDD)이다. 단일 출처: [requirements.md](../docs/submission/requirements.md).
- 🔜 **F4 지정 티어 → 개원 등급 자격(파생 라벨) 슬라이스 = 스펙 확정** (2026-07-20): "권역응급의료센터" 등은 자칭 불가한 법적 지정 명칭이라([emergency-tier-designation-law.md](../docs/research/emergency-tier-designation-law.md)), 필수 배후과 수에서 등급을 **파생**해 위저드에 실시간 표시(A안 — 판정·경제 불변식 0 침습). 개원=경영자 창립 결정 자리로 두께 부여. **이 정체성 슬라이스는 마감 전 착수**. 설계: [2026-07-20-hospital-tier-credential-design.md](../docs/superpowers/specs/2026-07-20-hospital-tier-credential-design.md). ⏸ **본선 잔여**: 재지정/철회(연 단위 강등·다주차) · 등급이 실제 용량 변경(B/C) · 24h 가중 권역 예산벽 · 지역 신문 결과층. 재지정 재료(권역심뇌혈관센터 별표2·목포중앙병원 2022 철회, [thoracic-surgery-role.md](../docs/research/thoracic-surgery-role.md))는 보존.
- ✅ **세계 이벤트 공문 브리핑 + 수가·재정 덱 E1–E4** (2026-07-20): 각 이벤트에 실제 정책 도구로 채운 공문(`briefing`)을 붙여 병원장이 "무엇이 어떻게 바뀌나"를 고시 질감으로 읽게. 개원 상쇄를 산부→검진(검사 과보상 인하)으로 교체(2026 실제 개편 밀착), 덱을 E1(검사재분배)/E2·E3(순증)/E4(채용비)로 다양화해 반복 해소. 억 손익은 브리핑에 안 씀(거짓 정밀도 회피)·장부에서 겪음. 설계: [2026-07-20-world-event-briefing-design.md](../docs/superpowers/specs/2026-07-20-world-event-briefing-design.md). ⏸ **남은 것(본선)**: 규제·여론 축(capacity/lawsuit 노브) · 진짜 시행 시차 발동 · 양방향 로테이션(개선 뒤 D형 상쇄) · 매주 재구성 economics 실효(E2·E3·E4 효과 활성).
- ✅ **재정중립 제로섬 — 최소 슬라이스 착수** (2026-07-19): 개원 이벤트(`OPENING_EVENT`)를 순환기 +6 / 산부 −6(→ 2026-07-20 검진 오프셋으로 교체) 재정중립 정책수가 패키지로 — 급여 풀 profit 합=0, 미용(비급여) 무풍지대가 개원 위저드 숫자에서 드러남. "필수과 수가가 안 오르는 이유가 악의가 아니라 산수"를 값으로. 설계: [2026-07-19-fiscal-neutral-opening-event-design.md](../docs/superpowers/specs/2026-07-19-fiscal-neutral-opening-event-design.md), 근거: [fee-schedule-and-subsidies.md](../docs/research/fee-schedule-and-subsidies.md) §6. ⏸ **남은 것(본선)**: 산부 콜로 장부 체감 · 매주 재구성이 economics에 물리게(2주차+ 실효) · hireCost 손잡이 복원 — 정책 레이어 통째 신설은 여전히 마감 후 후보로 가장 강함
- ⏸ 음성 TTS(히어로 보이스) · 시나리오 추가(필수의료 붕괴·지방 의료 공백을 독립 시스템으로) · 방어진료 드리프트 누적 · 동적 디렉터(콜 밀도 에스컬레이션) · 소송·언론 인과 사슬 확장
