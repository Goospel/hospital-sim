# plan — 앞으로 할 일 (살아있는 계획)

> 범례: ✅완료 · 🔜다음 · ⬜예정 · `X`droppable upside · ⏸의도적 보류(본선/v2) · ⚠️리스크·전제
> 관련 문서: [changeLog.md](changeLog.md) · [troubleshooting.md](troubleshooting.md) · [docs/game-concept.md](../docs/game-concept.md) · **[docs/submission-plan.md](../docs/submission/submission-plan.md)** (25일 제출 로드맵 상세)

## 목표

NAN 2026 (NHN Game × AI 해커톤) **사전 과제 제출**. 마감: **2026-08-10**. 본선(9/4~9/6)은 이 프로토타입을 확장.

**필수 제출물 4종**: ① 플레이어블 빌드(공개 Vercel URL)+소스(GitHub public) · ② 플레이 영상(유튜브 30~60초) · ③ 게임 소개 PDF · ④ AI 활용 기술 PDF. (팀 소개·포트폴리오는 스킵)

전략: **최소 제출선 먼저** — LLM 없이 결정론 폴백으로도 한 판 플레이+배포를 먼저 확보하고, 실LLM은 그 위에 **비파괴 가산 레이어**로. 상세·리스크·영상 샷·PDF 목차는 → [docs/submission-plan.md](../docs/submission/submission-plan.md).

핵심 원칙: **판정 = 결정론적 코드 / 대사 = LLM** (2콜 분리). 게임 상태 전이는 절대 LLM이 정하지 않는다.

**AI 활용 심사축 결정 (2026-07-16)**: NHN 슬로건("AI를 활용하는 사람은 많다 — AI의 다음 단계를 설계할 디렉터를 찾는다")·본선 제출물(AI 에이전트 설계서·디렉팅 명세서) 리서치 → **심사 북극성 = '디렉팅'(AI를 어떻게 지휘해 게임을 만들었나, B축)**. 대화 LLM(게임 속 AI 기능, A축)은 B축의 **대체재가 아니라 최강 물증**이다 — 2콜 권한 경계 설계(`adjudicate`에 설득 파라미터 부재 = 타입 수준 하드월)는 "AI를 어디까지 믿고 어디서 끊는가"의 디렉팅 그 자체. 따라서 **극소판 라이브는 유지**(채우고 통제한 슬롯 > 설계만 한 빈 슬롯; Game×AI 제출물이 AI-제로로 안 읽히게), **풀 2콜(`X8`)은 발전 가능성으로 후순위**. ④ AI 활용 문서는 **B축으로 리드**하고 A축을 그 물증으로 배치 → `docs/submission/ai-usage-doc.md`.

---

## M0. 프로젝트 셋업

- ✅ Next.js + TS + Tailwind 스캐폴딩
- ✅ 작업추적 3종(plan/changeLog/troubleshooting) 생성 — `claude-docs/` 배치(PR #22). *learning-notes는 빈 골격으로 만들었다가 **22커밋 동안 0줄**이라 PR #22에서 삭제(`93f18ea`→`aba6d77`) — 이 프로젝트는 지식이 문서가 아니라 코드로 굳는다.*
- ✅ .gitignore에 `.commit-msg-tmp` 추가
- ✅ vitest 도입(TDD 러너) + 첫 테스트 실행 확인
- ✅ git init + 초기 임포트 커밋(main) → 이후 작업은 브랜치(`feat/sim-core`)
- ✅ env 관리 체계: `.env.example`(커밋되는 변수 계약) / `.env.local`(gitignore, 기기별 실제 값) + 기기 간 이어 작업 문서(README). 시크릿은 서버 전용(`NEXT_PUBLIC_` 금지).

## M1. 순수 시뮬 코어 (LLM 없이 · TDD) ✅ 완료 — "하드락 벽"의 신뢰성 확보

- ✅ 도메인 타입 정의: 병원(숨은 제약)·환자·전원 판정·전원 시도·게임 상태
- ✅ 전원 **판정 규칙**(결정론적): 병상 · 응급실 당직 · 과밀 · **배후진료 역량** 4게이트 → 수용/거절/거절사유 태그 (`adjudicate.ts`, 4 tests). *리서치 반영으로 2종→4종 재설계(PR #11) — 뭉뚱그린 '당직 전문의'를 `hasErOnCall`(접수)와 `backupCare`(최종치료)로 분리.*
- ✅ 병상 0 = 어떤 입력에도 **하드락 거절** 불변식 테스트 (설득 텍스트는 판정 함수 시그니처에 부재)
- ✅ 골든타임 타이머 (순수·불변, clamp, 음수 가드, 만료 판정) (`goldenTime.ts`, 5 tests)
- ✅ 한 판 진행 상태기계: 시작 → 전원 콜 반복 → 결과(수용/사망), 종료 후 가드 (`round.ts`, 6 tests)
- 전체 15 tests green · `tsc --noEmit` 통과

## 🔑 선결 — 오늘(7/16) **사용자** 확인/결정 (코드 아님 · 내가 대신 못 함)

- ⬜ **제출 요강/폼 전문 정독** → 필수항목·**마감 정확 시각/타임존**·빌드 제출 형식(라이브 URL만 vs 다운로드 웹빌드 아티팩트도?) 확정
- ⬜ **Anthropic 콘솔 결제/크레딧 활성 확인** (미설정이면 `X8` 스킵 전제 → ④PDF는 설계+로컬 시연 중심)
- ⬜ **FAQ '유료 API 부담 주체' 문의 발송 여부 결정** (초안은 내가 준비, 발송은 승인 후)

## 크리티컬 패스 — 최소 제출선 먼저 (상세: [submission-plan.md](../docs/submission/submission-plan.md))

> **U/P = 필수 최소 경로**(이것만으로 8/10 성립) · `X` = droppable upside · `Z` = 버퍼.
> 비평 반영: 패키징(P6→P7) **직렬화**, `Z9` 전제에서 `X8` 제외(버퍼 착시 제거), 실LLM 극소판을 **U2로 당김**(영상에 라이브 AI 확보), 간이 지도는 **v2 강등**.

### U1. 파이프라인 스파이크 (7/16) — 인프라 ✅ 배포 파이프라인 뚫림
- ✅ 로컬 **prod `next build`**(Turbopack) 통과 — Next 16 breaking change(`node_modules/next/dist/docs`) 정독 완료 (필수 게이트 해소)
- ✅ walking skeleton("수화기 너머의 벽") **Vercel 배포** → 공개 URL 렌더 확인 → https://hospital-sim-ashy.vercel.app/ (main 푸시 시 자동 재배포)
- ⬜ (U2와 겹쳐) 영상 20초 throwaway 유튜브 업로드 테스트 · LLM 단일 왕복 호출·지연 실측 — LLM 스파이크는 결제 활성 확인 후(선결②)
- ⏸ `ANTHROPIC_API_KEY` Vercel 환경변수 등록은 `X8`(실LLM) 착수 시

### U2. 한 판 UI 척추 + 실LLM 극소판 (7/16) — ① 🔜 진행중
- ✅ 결정론 코어를 React 배선(`GameClient.tsx`): 환자 도착 + **실시간 골든타임 카운트다운**(`tickTime`, TDD) / 병원 리스트 + '전원 콜' → `attemptTransfer` → 수용·거절 표시 / 종료 상태(ACCEPTED/DIED) UI + 입력 잠금 + '다시 한 판'
- ✅ 결정론 폴백 대사(`dialogue.ts`, TDD) + STEMI 시나리오(`scenarios.ts`, TDD·두 거절 사유 다 드러나되 풀림) 하드코딩 완주 — 브라우저 관찰 검증(NO_BED→NO_SPECIALIST→수용)
- ⬜ **실LLM 극소판**: 거절 대사 1종만 실제 Anthropic 왕복으로 렌더(무키면 폴백) — ⚠️ 결제 활성 확인 후(선결②)

### U3. 거절 누적 좌절 + 하드락 벽 (7/16) — ① ✅
- ✅ 거절 로그 리스트 + 사유 태그(병상없음/당직없음) 뱃지 + 병원별 재시도 횟수(`×N`)
- ✅ 자유 텍스트 설득 입력 — 매달릴수록 응답이 냉정해지되(`persuasionReply` 에스컬레이션, TDD) 병상0은 어떤 텍스트에도 `NO_BED` 유지. 불변식 테스트로 잠금(반복 재시도해도 절대 ACCEPTED 안 됨). 브라우저로 3회 매달림 관찰 검증
- ✅ 골든타임 임계 이하 화면 톤 경고(red vignette + 타이머 pulse, `lowTime` 로직 기반)
- ⏸ (v2 강등) 거절 병원이 붉게 꺼지는 간이 지도 — 구현비 대비 임팩트 낮음, 리스트로 충족

### R3.5. 리서치 + 전제 교정 (7/16) — ① ✅ (계획 외 삽입, 의도 정확도 확보)
- ✅ 실제 통계 리서치 + 적대적 검증(11에이전트 워크플로우) → [docs/research/](../docs/research/) 팩트체크·STEMI 팩트시트 (PR #10)
- ✅ 거절 사유 배후진료 중심 **4종 재설계**(NO_BED/NO_ER_ONCALL/ER_OVERCROWDED/**NO_BACKUP_CARE**) — Hospital 모델 분리(초기수용/최종치료/과밀), STEMI 시나리오 재균형(지배 병목=배후진료 불가), 각색 고지+디스클레이머. TDD 38 green (PR #11)
- ⏸ (선택·v2) 지방 거리·이송시간 축 / 즉사→연속 경사 예후 / 형사우려·방어진료 — 리서치엔 있으나 최소 제출선 이후로 보류
- ⚠️ **U4 디브리핑은 이 4종 구조 변수를 읽어야 한다** — 병상 중심이 아니라 배후진료 중심으로

### R3.6. 문서 lint 패스 — game-concept 동기화 (7/17) — ③④ ✅ (계획 외 삽입, P7 선행)
- ✅ **Karpathy LLM Wiki 패턴 대조**(46에이전트: 조사 5 → 매핑 → 3렌즈 적대검증 → 구멍분석) → **판정: 우리는 LLM Wiki가 아니다**(9요소 중 strong 0 / partial 1 / stretch 6 / 부재 2). 트리거가 source-push가 아니라 **feature-pull**이라 기존 페이지를 재검토할 계기가 원리적으로 없음 → 지식이 문서가 아니라 **코드로 굳는다**. 근거: learning-notes 22커밋 0줄→삭제, PR #18이 29에이전트 돌리고 기존 리서치 갱신 0건. [[not-llm-wiki-but-llm-run-workflow]]
- ✅ **lint 실행**(120에이전트: 5축 스윕 → 3렌즈 검증 → 38건 중 **16건 기각**) → `game-concept.md` 13건 교정. 부트스트랩(`93f18ea`) 이후 **35커밋 무갱신**이라 **게임 1막(설립 위저드·콜 트리아지) 전체를 문서가 모르고 있었음**. 숨은 제약 2종→4종, 코어 루프→실제 **7페이즈**(DAY_END 포함·한 주 7일), 미구현(반사실·지도)에 ⏸ 표기, 톤 가드레일은 본문 복제 대신 리서치 포인터(사본은 확정적으로 drift), 깨진 앵커 2건. 앵커 14/14·상대링크 56/56 실측 0 broken(`github-slugger` 정본).
- ⚠️ **이 패스 자신이 stale base에 걸렸다 (T-038)** — 워크트리를 #33 시점 main에서 뗀 채 lint를 돌려 120에이전트가 낡은 코드를 읽었고, #35가 폐기한 "분기"를 문서에 새로 써넣었다(13건 중 3건 오염). `origin/main` 머지 후 재검증·수정. **"문서가 코드를 못 따라간다"를 고치는 패스가 같은 이유로 실패한 것** — feature-pull 진단의 자기 증명.
- ✅ 손대지 않은 것: **병상 0 하드락**(리서치가 "현실적"이라 명시 확증 — 교정 대상은 하드락이 아니라 *지배 병목의 비중*이고 그건 이미 PR #11로 코드 반영됨) · 3대 문제 절의 퍼센트 수치(단일 출처는 리서치, 이중 기재는 새 drift 소스)
- ⚠️ **P7(③게임소개 PDF)은 이 문서에서 쓴다** — lint 전이었다면 존재하지 않는 게임을 설명하는 PDF가 나올 뻔했다.

### R3.7. 규칙 감사 + 훅 승격 + research P7 검문 (7/17) — ③④ ✅ (계획 외 삽입, P7 선행)
- ✅ **AI 활용 추적 규칙을 도입 15커밋 만에 처음 감사** — 준수 **15/15(100%)** · `--grep` 집계 **15/15** · `interpret-trailers` 파싱 **2/15(13%)** · **집계 실행 횟수 0**. **규칙은 100% 지켜졌는데 목적은 0이었다.** → ④문서 §3-1에 실측 절 신설 + CLAUDE.md에 작동하는 집계 명령·**"언제 집계하는가"**(④ 갱신 시·P7 최종화 시) 못박음. 소비자를 지정하지 않은 규약은 **지켜지면서도 죽는다**. [[query-decides-document-life]]
- ✅ **훅 2개로 승격 — 소프트 규칙이 안 듣는다는 게 데이터로 확정됨**: **T-041**은 스스로 *"재발률 100%"*를 예측하고 그 뒤 4커밋 전부 재발 / **T-038**은 사다리 두 칸(프로젝트→글로벌)을 쓰고도 2회 재발. → `.githooks/commit-msg`(파싱 안 되는 트레일러면 커밋 거부, TDD 9/9) + `~/.claude/hooks/block-stale-base.ps1`(글로벌 PreToolUse, stale base 첫 편집 거부, TDD 7/7). **⭐ 후자가 설치 10분 만에 3회차를 막았다**(#41 머지로 base가 1커밋 뒤처진 상태에서 deny).
- ✅ **미사용 스킬 첫 집계**(목적 b): 프로세스 스킬 14종 중 **9종 미사용**(`using-git-worktrees`·`systematic-debugging`·`verification-before-completion` 등). ⚠️ **인과로 읽지 않는다** — `using-git-worktrees`는 base 최신화를 안 하므로 **썼어도 T-038을 못 막는다**. 상관이지 원인이 아니다.
- ✅ `docs/research/` **코드 실증 4건** 교정: 거절 사유 **4종→5종**(`NO_NIGHT_BACKUP`) · 각색 고지의 **"+287억"이 코드에 없음**(현재는 `ledger.ts`가 플레이 결과에서 산출) · **"병원 6곳" 기준 미표기**(PCI 후보 기관 수 vs 접촉·거절 병원 수 — 상호 링크 신설) · **§2 각색 고지가 1막 전체를 모름**(콜 35통·수가/원가·boarding·24시간 당직 4행 추가)
- ⚠️ **lint 자체는 절반쯤 실패했다** — 100에이전트, 발견 35건, 기각 **2건(6%)**(PR #36은 42%). 원인은 **렌즈 설계 결함**: 세 렌즈가 전부 "이 관찰이 사실인가"만 물었고 **"이 문장을 문서가 의도적으로 썼는가"**를 묻는 렌즈가 없어, 리서치가 게임을 **비판한 기록**을 "낡은 서술"로 읽었다(T-037이 이미 기각한 "병상 0 형상화"가 P7-blocker로 재부상). [[wrong-question-scales-with-agents]]의 재연 — 렌즈는 안(案)을 심사하지 **프레임**을 심사하지 않는다.
- 🔜 **research 나머지 26건 재검증 (P7 직전, 8/1~8/3)** — "의도된 비판인가" 렌즈를 추가해 다시 돌리거나, 건별로 사람이 판정. **지금 목록을 그대로 적용하면 리서치를 코드에 맞춰 파괴한다.** 살릴 후보: `essential-care-economics` 내부 모순 3건(:42·:93·:99) · `fee-schedule:248` 단위 표기 · 깨진 앵커 2건.
- ✅ troubleshooting **목차 복구** — T-039~T-046 8건이 목차에서 누락돼 있었다(앵커 12/12 규칙 검증 후 생성). ⚠️ **이 복구는 절반이었다 → R3.8**(검사기가 `^## T-`만 봐서 「전제」 섹션을 못 봤다).

### R3.8. troubleshooting 분할 — 목차를 손글씨에서 `ls`로 (7/17) — ③④ ✅ (계획 외 삽입, 사용자 요청)
- ✅ **항목 1건 = 파일 1개**(`claude-docs/troubleshooting/T-027..T-048.md`, 22건) + 허브 목차 **자동생성**(`scripts/rebuild-troubleshooting-index.ps1` — 재생성 겸 검사기, TDD **27건**) + `.githooks/pre-commit`(stale이면 커밋 거부). 무손실 검증 **52건** 통과(원본을 `git HEAD`에서 꺼내 전 줄 대조).
- ⭐ **왜 지금** — 통증이 실측이다: `changeLog.md`는 **이미 Read 캡을 넘겼고**(36,602 > 25,000 토큰 → 116/287줄에서 잘림, 파일의 **59.6%가 기본 경로에서 안 읽힘**), `troubleshooting.md`는 **다음 T항목 하나에서** 넘길 상태였다(49,959B ≈ 캡의 **95.4%**, 여유 ≈1,140토큰 vs 항목 중앙값 ≈1,290토큰). 게다가 **오름차순이라 잘리는 게 가장 최신 함정**(T-044·T-045·T-046)이었다. changeLog가 더 큰데 안 아픈 이유는 **역순**이라 잘리는 게 7/16의 372B짜리 PR #1이어서다 — 정렬이 자기 크기 문제를 이미 풀어놨다.
- ✅ **왜 `ls`인가**: #42의 목차 복구 뒤에도 「이 프로젝트에서 이미 알고 있는 전제」가 **aba6d77에서 태어날 때부터 20커밋째 목차 밖**이었다(레포 전체를 지배하는 Next 16·T-026 전제가 216줄 한복판에 색인 없이). 검사기가 `^## T-`만 봤기 때문이다. T-041("검사기 없는 규약은 썩는다")의 **한 겹 위** — **검사기가 있어도 검사기가 고른 축 바깥은 100% 준수로 보이면서 썩는다.** 파일시스템은 축을 고를 수 없다. 새 처방도 아니다: 같은 병(인덱스↔본문 이중 기재)을 `~/.claude/hooks/rebuild-memory-index.ps1`(7/10)이 이미 고쳤고 글로벌 CLAUDE.md에 이유가 적혀 있다 — **그 처방을 이 레포에 겨눴을 뿐이다.**
- ⚠️ **분할은 뚱뚱함을 안 고친다** — 총 바이트 49,959 → 51,378(**+3%**, frontmatter 오버헤드). 바뀐 건 **접근 입도**뿐이다(한 번에 읽는 단위 캡의 95.4% → 허브 9.4% / 최대 항목 7.8%). 진짜 동력은 항목 수가 아니라 **항목당 길이**이고(changeLog 항목 중앙값 555B → 3,784B, **6.8배 / 31시간 만에**) 거기엔 분할이 무력하다. 그 자리를 **4필드 스키마**가 잡는다 — 스키마가 있는 troubleshooting은 T-027~T-046이 전부 **9줄 근처**로 잠겼다(9,9,9,9,9,9,9,9,9,9,9,10,9,9,11,9,9,9,9,6). ⚠️ 스키마도 완전하진 않다: 줄 수는 잠겨도 **바이트는 1.6배** 늘었다(필드 개수는 잠그고 필드 길이는 안 잠근다).
- ⚠️ **훅이 실제 커밋에서 3연속 실패했다 → T-048** — 검사기는 **22/22 green**이었는데 실제 `git commit`에선 셋 다 달랐다: ① **커밋이 그냥 통과**(워크트리 `config.worktree`의 `hooksPath`가 절대경로라 워크트리에서 만든 훅이 git 눈에 없음 — `commit-msg`는 메인에 있어 정상 작동해 착시까지) ② **사유 오진**(ExecutionPolicy로 로드 실패한 걸 "stale"로 보고 — 최신이어도 거부됐을 것) ③ **판정 실패**(PowerShell 한글 출력이 CP949라 bash에서 깨짐 → ASCII 마커 `INDEX-CHECK:`로 분리). **훅은 스크립트 직접 실행으로 검증되지 않는다** — negative(거부되는가 + **사유가 맞는가**) → positive(통과하는가) 순으로 실제 커밋 검증. T-045의 한 층 위.
- 📌 **changeLog·plan은 안 쪼갠다 — 기준은 건수가 아니라 읽기 패턴이다**: troubleshooting은 **T-### 조회**(그래서 목차가 필요했다) → 분할이 정확히 맞다. changeLog는 **최근 N개 통독** → 역순이 이미 풀었고 마감까지 안 아프다. plan은 **전체 통독**(42,611B ≈ 캡의 81%) → 쪼개면 손해.
- ⏸ **changeLog 4필드 스키마 — 본선 확장 카드**: 효과는 실측됐지만(6.8배 → 1.6배) 남은 PR 5~10개면 95~114KB가 되어도 **역순이라 아무 일도 안 일어난다.** 마감(8/10) 전엔 불필요.

### U4. 인과 디브리핑 → **차가운 사실 영수증** (7/16) — ① ✅
- ✅ 로그 → **팩트만** 결정론 산출(`debrief.ts` 순수 함수, TDD 8 tests): 전원 시도·거절·**거절 사유별 집계(첫 등장 순서)**·받을 수 있던 곳 N/전체·골든타임 소요. 해석 카피 0 — 함수가 순수 팩트 추출기.
- ✅ 결말 화면 = 라벨+숫자 '전원 기록' 영수증만. 반사실 분할·findings 카드·착지 카피 제거, 유도 헤더("무엇이 죽였나")→중립 "전원 기록", 각주는 허구 고지만(통계·출처는 팩트시트로). GameClient 배선, 브라우저 검증(DIED 12통·사유별 집계·1/6 · ACCEPTED 1/6), 46 green·next build 통과.
- ↺ **설계 전환("보여주지 말고 겪게")**: 초판은 "당신이 아니라 구조"를 글로 설명했으나, 플레이로 이미 손끝에 겪은 무력감을 자막으로 받아쓰면 여운이 강의로 납작해짐 → 해석을 삭제하고 사실(특히 반복된 `배후진료 불가 ×8`)이 스스로 고발하게. (사용자 지정)

### U4.5. 병원 장부 — 필수의료 경제(장부 에필로그) (7/16) — ① ✅
- ✅ **단일 병원 장부**(한바다대학병원) 결정론 산출(`ledger.ts` 순수 함수, TDD 8 tests) + 결말 패널: 미용·검진으로 흑자(순이익 +287억)인데 환자가 필요했던 순환기내과 채용은 0 → "왜 배후진료 불가였나"의 경제적 뿌리. **`0명`은 하드코딩 아니라 backupCare(=NO_BACKUP_CARE의 뿌리)에서 파생.** 조작 없음(패시브 리빌), 톤은 영수증과 동일(라벨+숫자, 해석 0). 화면 병치가 논지. 브라우저 검증, 54 green·next build 통과. [[game-show-dont-tell]]
- ✅ **전제 근거(리서치 먼저)**: 경량 리서치+적대 검증 워크플로우로 필수의료 저수가·적자 vs 피부·미용 고마진 방향 검증(방어 가능) → [docs/research/essential-care-economics.md](../docs/research/essential-care-economics.md). 부호만 근거 준수, ₩ 각색([[fact-grounding-before-mechanics]]·R3.5 선례).
- ⏸ (v2) **지역 집계 장부** — 여러 병원 합산으로 "시스템 전체가 그렇게 굴러간다"를 형상화. (사용자 지정: 지금은 단일 병원, 지역은 plan에만 보류)
- ⏸ (v2·B안) 플레이어가 수익을 배분·채용하는 **조작 루프**(내가 만든 벽에 내가 부딪힘) — 멀티라운드·경제 밸런싱, 마감 이후.

### U5. 실배포 v1 → **최소 제출선** (7/16, 예정 앞당김) — ① ✅
- ✅ prod 빌드(`tsc --noEmit`·`next build`) 통과 → Vercel 공개 URL 라이브 + 모바일(375px 가로 넘침 0)·데스크톱 스모크. **시드 고정은 자동 성립**(RNG 전무·`goldenTime` 순수 → 같은 판 항상 동일).
- ✅ STEMI 팩트시트 갱신([stemi-factsheet.md](../docs/research/stemi-factsheet.md)): "초안" 뗌, 스테일 교정(거절 2종→4종·병원 4→6곳), 결말 영수증·장부 반영, 경제 근거([essential-care-economics.md](../docs/research/essential-care-economics.md)) 크로스링크. 출처 각주+각색 고지 유지, '감수' 문구 없음.
- ✅ README 정직화+보강: 태그라인 과장("라이브 AI") 제거→현재=무키 결정론 폴백 명시(LLM 대사콜은 예정 가산 레이어=`X8`), "무엇을 겪나"(두 축)·플레이법·팩트시트 링크 추가. **무키 완주=현재 기본 상태**(LLM 미통합). 스크린샷은 P6 때 일괄(플레이스홀더).

### P6. 플레이 영상 (8/1–8/3) — ②
- ⬜ U5 배포본으로 30~60초 촬영 → 유튜브 업로드 (시드 고정=자동 성립).
- ⚠️ "라이브 AI 협상 구간"은 `X8`(실LLM) 착수 후에만 가능 — 미착수면 결정론 폴백 버전으로 촬영.
- ⚠️ 관찰: 콜드 로드 직후 첫 '전원 시작' 탭이 한 번 씹힘(하이드레이션 타이밍/자동화 레이스 가능성). 촬영 시 페이지 완전 로드 후 클릭. 신경 쓰이면 hydration 조사(별건).

### P7. 두 PDF (8/3–8/7) — ③④ · **여기서 4종 완성**
- ⬜ ③ 게임 소개 PDF (개요·플레이법·실행법·스크린샷·팩트시트)
- 🔜 ④ AI 활용 기술 PDF — **작업 초안 집필됨**: [docs/submission/ai-usage-doc.md](../docs/submission/ai-usage-doc.md) (디렉팅 B축 스파인 · 지금 집필 가능분 완료 · 라이브 로그·스크린샷은 극소판/`X8` 후 채움). 남은 일: PDF 변환·스크린샷·프롬프트 로그

### `X8`. LLM 2콜 풀버전 (droppable upside · **발전 가능성**: 디렉팅 물증 강화용, 사전과제 필수 아님 · P6·P7 조기완료 시 ≤8/7)
- ⬜ LLM 프록시(Next API 라우트, 서버 키·타입드 로더 검증) + 대사콜 풀버전
- ⬜ **정합성 테스트 TDD**: 코드 판정 == 최종 게임 상태 (LLM이 못 뒤집음)
- ⬜ 디브리핑콜(자연어) + 판당 호출 상한(6~10) + 무키/타임아웃 시 폴백 자동 강등
- ⬜ 협상 장면 재녹화 + ④PDF에 실제 프롬프트/응답 예시 갱신 — **이 안에서 ≤8/7 완결**(버퍼로 안 넘김)

### Z9. 버퍼 + 최종 제출 (8/8–8/10) — 코드프리즈 · 의존: **P6·P7만**
- ⬜ 배포 빌드 최종 QA(완주·콜드스타트·모바일·폴백 강등) + 4종 링크 전수 점검 → 폼 제출
- ⬜ 신규 기능·영상 재편집 금지

---

## E. 경영 확장 — "벽의 양쪽" 2막 단막극 (결정론 · 7/16~ · 제출선과 독립)

> spec §0의 "빈 활주로(7/16~7/29)" 작업. 8/10 제출작(협상+디브리핑+장부)을 **비파괴로 감싸는** 상위 구조 — 전화를 *받는* 병원까지 플레이. 이번 주는 API 불필요(결정론), 다음 주 실LLM(X8)은 대사만 얹음. 설계: [docs/superpowers/specs/2026-07-16-management-expansion-design.md](superpowers/specs/2026-07-16-management-expansion-design.md).

- ✅ 설계 확정(브레인스토밍) → spec (PR #17)
- ✅ 근거 리서치 — 필수과 소송·방어진료 리스크의 **부호(결과의 중대성 축)** → [essential-care-litigation-risk.md](../docs/research/essential-care-litigation-risk.md) (PR #18)
- ✅ **로직 코어(Part 1)** — `setup`(위저드·과 카탈로그·예산) / `receiving`(콜 큐·하드락/선택·장부·소송 누적) / `session`(5페이즈·2막 분기) / `ledger` 재배선(플레이어 병원) / `dialogue`(1막 다크코미디). 전부 결정론·TDD, 92 green·tsc 0·비파괴 (PR #20). 계획: [docs/superpowers/plans/2026-07-16-management-expansion-core.md](superpowers/plans/2026-07-16-management-expansion-core.md)
- ✅ **UI 통합(Part 2)** — `SetupWizard`·`ReceivingPhase`·`SessionClient`·에필로그 배선(Task 2~6) + **통일 시각 최종 마감·막간 붕괴 시그니처·a11y/반응형·배포 게이트**(Task 7). 진입=세션이 THE 게임(위저드-first, 기존 STEMI는 2막 TRANSFER로 흡수)·통일 시각(단일 어두운 zinc 시스템 + 초록→빨강 액센트 아크, body/html 지면을 프리퍼런스 무관 다크로 고정해 밝은 화면·흰 플래시 0). 설계: [docs/superpowers/specs/2026-07-17-management-expansion-ui-design.md](superpowers/specs/2026-07-17-management-expansion-ui-design.md). (로직 코어 Minor 4건: 음수값 방어→Part2 해소 / essentialHires 다필수과·도달불가 분기 커버·STEMI_SPECIALTY export→v2·별건 보류). Task 7 롤업 결함(LedgerPanel 순이익 부호 하드코딩 `+-24억`)도 이때 수정 + 회귀 테스트.
  - ✅ **이연 Minor 3건 청소 (PR #27)** — `callerPlea` 순수 함수 추출(TDD·대사 선택 통일)·Interstitial 붕괴 타이머 `useEffect` cleanup·`firedRef` 중복 진입 가드(reduced-motion 경로 커버). tsc 0·vitest 309 green·next build·브라우저 공범 경로 검증. (`receivingLine` 미사용 seed warning은 pre-existing 별건.)
  - ✅ **양심 경로 완주 코드 검증 (2026-07-17)** — Preview로 양심대학병원(순환기 2명·60억) SETUP→RECEIVING(5콜·STEMI가 하드락 아닌 **선택**, 전부 수용)→INTERSTITIAL(−46억)→EMERGENCY(원내 PCI·**생존**)→EPILOGUE(환자 살림·순이익 **−96억**=순환기−24+진료−22+소송−50) 완주 확인. 콘솔 0. 공범(흑자)↔양심(−96억+소송) 낙차 정상. ⚠️ **이 수치는 자리 제한(2026-07-17 1단계) 이전 값이라 낡음** — 이제 워크인 2통이 자리를 먹어 c4·c5가 `NO_BED`로 막히므로 "전부 수용"으로는 −96억이 안 나온다(워크인을 거절해야 STEMI 2통을 다 받는다). 재검증 필요. **발견→해소(PR #29)**: 콜 c3 라벨 "검진 패키지 문의"에 보톡스 대사가 붙던 라벨↔대사 불일치(`COSMETIC_WALKIN` 풀에서 c1·c3 둘 다 `index%2==0`→pool[0]) → `callerPleaAt(queue, index)`로 **kind 내 등장 순번**을 seed로 넘겨 정합(TDD, c3→pool[1] '검진 …'). 브라우저 검증 완료.
  - ⬜ (컨트롤러 몫, 코드 아님) Step 5 — **스크린샷 이미지 캡처**(④ 제출 문서·P6/P7용). in-app 브라우저 `screenshot` 도구가 이 환경에서 재차 30초 스톨(T-034)이라 이미지 미확보 — 최종 캡처는 사용자 로컬 화면에서. 캡처 대상 체크리스트는 세션 요약 참조.
  - ✅ **랜딩 타이틀 카드 (2026-07-17, 플레이테스트 폴리시)** — URL 진입 시 바로 위저드가 뜨던 걸, 앞에 다크 지면 타이틀 카드("수화기 너머의 벽")를 세워 첫 화면으로. 원본(97a012c) 느낌을 되살리되 카피는 경영 전체 아크로 재프레이밍("세우고=채용/받고=콜큐·응급/대가를 치른다=결말"). `LANDING`을 세션 페이즈로 승격(`startSession→LANDING`, `beginSetup` LANDING→SETUP 가드) — 재시작도 startSession 단일 진입점이라 자동 랜딩 리셋. TDD(+3)·tsc 0·vitest 316·브라우저 검증(시작→위저드·콘솔 0). 설계: [superpowers/specs/2026-07-17-landing-title-card-design.md](superpowers/specs/2026-07-17-landing-title-card-design.md).
  - ✅ **위저드 2단계 + 힌트 제거 (2026-07-17, 플레이테스트 폴리시)** — SETUP을 두 패널로: 이름 하나만(NAME) → 과 채용(DEPTS). 과 카드의 "수익 예상/적자·소송" 해석 라벨·색을 없애 **과 이름 + 채용비**만 노출, 상단 tell("순환기내과는 비워둬도…")도 중립 문구로. show-don't-tell을 SETUP까지 확장 — 흑자/적자·소송은 플레이어가 장부·결말로 스스로 알게. essential/lawsuitRisk 데이터·판정은 불변(표시만 감춤). `SetupWizard.tsx`만·tsc 0·vitest 316·브라우저 전체 플로우 검증.
  - ✅ **부문 손익 진행률 누적 (2026-07-17, 플레이테스트 폴리시)** — RECEIVING 러닝 장부의 부문 손익이 콜 1/5(진료 0)부터 과별 손익을 전액 선반영하던 걸, **분기 진행률(처리 콜/전체)만큼 0에서 누적**으로 교정("진료를 볼수록 흑자/적자" 직관·show-don't-tell과 정합). `quarterProgress`·`accruedSegments` 순수 함수 추가(TDD +8), `runningNetProfit`·`CheerfulLedger` 배선. 금액·부호 불변(도달 타이밍만)·결말/Interstitial(진행률 1) 값 불변. `receiving.ts`/`ReceivingPhase.tsx`·tsc 0·vitest 324·브라우저 검증(콜1/5=0→분기말 전액).
  - ✅ **하루 진료 자리 제한 — 과부하 신설 (2026-07-17, 1단계)** — 병원에 **능력의 한계**를 물리적으로 심음. 자리 = 병상(`FIXED_BEDS` 2→**3**) < 하루 콜 5통 → **매일 2통은 못 받는다**. `bedsFree` 신설·`hardlockReason`(게이트 0순위=자리) 추가·로그에 사유 기록·헤더에 "남은 자리 N/M"(숫자만). `beds`/`NO_BED`가 타입에만 있고 안 쓰이던(병상 무감소) 걸 되살림 — 전부 받아도 대가가 0이라 플레이어가 **고르지 않던** 게 근본 원인. **결과: "전부 수용"이 더는 양심적 플레이가 아니다** — 순환기를 갖춘 병원도 워크인으로 자리를 채우면 STEMI가 `NO_BED`로 막혀 +42억 흑자가 된다. 함께 고침: `RECEIVE_HARDLOCK`("**자리는 있는데**…")이 자리 소진에 나가 거짓말이 되던 버그 → 사유별 대사. TDD(+32)·tsc 0·vitest **356**·브라우저 양쪽 경로 검증. 설계: [superpowers/specs/2026-07-17-daily-capacity-calendar-design.md](superpowers/specs/2026-07-17-daily-capacity-calendar-design.md).
  - ✅ **7일 루프 + 달력 (2026-07-17, 2단계)** — 한 판 = 한 주. `day(1..7)`·`ledgerDays`·`DAY_END` 페이즈·`advanceDay`/`isLastDay`/`weekTotals` 신설, 요일별 고정 큐 `createCallQueue(day)`(결정론·라벨은 등장 순번 파생), 달력 `DayEnd.tsx` 7칸(숫자+색·해석 0). **분기 폐기** — 과 손익 = 이번 주 7일치, 하루는 1/7씩 누적(`dayProgress`), "올해/분기/오늘" 3중 혼재 해소. 7일치 1/7 합 = 주간 전액이라 `composeLedger`(전액)와 자동 정합 → **계산 경로 이원화 닫힘**. 치명 버그 2건 함께 수정: `buildEpilogue`가 7일차 하루치만 계상(→`weekTotals` 합산, 6일치 증발 방지)·`Interstitial`도 주간 누계로. **재밸런싱**: 콜 5→35통이라 델타·소송비가 7배 누적 → `PROFIT_DELTA` +3/+1/−6·`LAWSUIT_COST_PER_EXPOSURE` 5로 하향(양심 −525억→**−90억**). ⚠️ 이 회귀는 vitest 372 전부 green으로 통과 — 브라우저 완주로만 잡힘. tsc 0·vitest 372·7일 완주 2회·콘솔 0.
  - ⏸ **워크인 자격 (3단계)** — 미용·검진 의사 ≥1이라야 워크인 수용(`Hospital.revenueCare` 신설 + `RejectionReason` 새 항목 + `dialogue.ts` Record 2개 + Hospital 리터럴 13곳). 딜레마엔 영향 없고 미용 의사 0명 설정만 다뤄서 1·2단계에서 뺐다. 설계 §2.1. **아래 F 슬라이스가 우선순위 위** — 딜레마에 직접 닿는 쪽부터.
- ⏸ (다음 주·`X8`) 실LLM 경영/협상 대사 — 폴백 대사 위에 얹음
- ⏸ (구조 2·본선 이후) 재투자 루프([U4.5](#u45-병원-장부--필수의료-경제장부-에필로그-716---)의 B안)·병동 증설·장비 업그레이드·다회차 캠페인·다양한 환자 축·리치한 소송 이벤트 시스템

---

## F. 필수의료 경제성 딜레마 장치 + 후일담 뉴스 (2026-07-17~ · 플레이테스트 폴리시)

> 플레이테스트 피드백 2건: ① "돌려보낸 환자가 어떻게 됐는지 알 수가 없다" ② "필수의료과의 딜레마가 명확하게 다가오지 않는다 — 환자를 받더라도 적자가 난다고 알고 있다".
> 설계: [superpowers/specs/2026-07-17-essential-care-economics-devices-design.md](superpowers/specs/2026-07-17-essential-care-economics-devices-design.md)
> 근거: [fee-schedule-and-subsidies.md](../docs/research/fee-schedule-and-subsidies.md) · [er-rejection-news-coverage.md](../docs/research/er-rejection-news-coverage.md) (둘 다 2026-07-17 신설)

- ✅ **선행 리서치 3갈래** — 수가 체계 메커니즘 / 과별 가산·정책수가 / 응급실 뺑뺑이 언론보도. ⚠️ **리서치가 전제 2개를 반박했다**: (a) "환자를 받을수록 적자"는 고정비 구조상 **틀림** → 정확히는 "안 받아도 적자, 받아도 원가 미달"(구조적 저보상). (b) "가산을 줘도 적자"도 **틀림** → 흉부외과는 100% 가산을 받고서야 원가 **100%**(= 가산 빼면 절반). 그리고 **게임 자체의 부호 위반**을 찾음 — 심장내과 원가보전율 **117%**(흑자 쪽)인데 게임은 `CARDIOLOGY: -12`.
- ✅ 설계 확정(브레인스토밍) → spec + 리서치 문서 2종 + 기존 `essential-care-economics.md` 정정
- ✅ **스펙 적대 검증** — 4개 렌즈(사실/정합성/불변식/코드 대조) × 반증 파이프라인(에이전트 46) → **반증 통과 8건 전부 반영**해 스펙 전면 개정. 초안의 critical 2건: **I8 "±3자리 억"이 자기 근거인 −525억을 통과시킴**(3자리라서) / **단위 혼용** — 일반 응급 부호는 행위 단위(45%), STEMI 부호는 과 단위(117%)에서 뽑아 정반대 부호를 만듦. 이력: 스펙 §12
- ✅ **F1. 순환기 고정비 재정의 + 콜당 수가/원가 내역** (PR #38) — 나머지 셋의 전제(절단 불가)
  - ✅ 순환기 부문 = "적자과" → **24시간 심장중재팀 대기 고정비**로 재정의(숫자 유지·의미 변경). `setup.ts` DEPARTMENTS 주석에 "콜당 원가 미달은 CALL_ECONOMICS가 따로 표현 — 층이 달라 이중 계상 아님" + ⛔"콜 수로 나눠 1인당 손실 만들지 마라" 명시
  - ✅ 콜 델타 부호를 **행위 유형 표 하나에서만** 뽑는다(단위 혼용 금지): 일반 응급 **+1 → −3**(3/6 ≈ 50% — 기본진료 50.5%) / **STEMI는 음수 유지**하되 −6 → **−2**(11/13 ≈ 85% — 수술·처치 84.9%). 미용 6/3 = 200%(가격 규제 없음). **과 단위 117%·103%는 입력이 아니라 F2에서 창발한다**
  - ✅ 콜당 두 줄 노출: **"진료비 (병원 책정)"**(비급여) vs **"수가 (정부 고시)"**(급여) + 원가. 해석 카피 0
  - ✅ **지뢰 4 결정 — 셋 다 기각하고 F1b 신설**(아래). 상한 1은 "코드 사정에 게임을 맞춘 것"이라 사용자가 기각("한 명으로 강제하면 게임이 저렴해 보인다")
  - ✅ **지뢰 5 동시 해소** — 과별 인원 상한 `MAX_DOCTORS_PER_DEPT = FIXED_BEDS`. **각색값이 아니라 자리에서 파생**(한 과가 하루에 앉힐 수 있는 환자가 3뿐이라 4명째는 볼 환자가 없다). 예산은 못 막는다(미용 10명 = 정확히 100억)
  - ✅ **브라우저 7일 완주 2회** — 양심(미용1·순환기2) **−1억** / 공범(미용3·검진2) **+308억**. 둘 다 I8(|순이익| ≤ 400억) 통과. `tsc` 0 · vitest **412 green**
  - 📌 **뜻밖의 정합**: 양심 루트가 ~본전으로 착지했는데, 이게 리서치의 "**100% 가산 줘서 겨우 본전**"(흉부외과)과 우연히 같은 자리다. 딜레마는 절대 손실이 아니라 **격차 309억**(공범 +308 vs 양심 −1)이 만든다
  - 📌 **콜 구성(DAY_PLANS)은 유지한다** — 초안의 STEMI 14→7 재조정은 **철회**. 검사를 급여 전체로 열면서 재조정의 필요가 사라졌고, 함께 부작용 3개도 소멸(STEMI 하루 1이면 `pool[1]`='재이송' 라벨·대사가 7일 내내 **도달 불가** / 배치 원칙 "뒤로 갈수록 STEMI가 는다"가 "모든 날에 STEMI"와 **양립 불가** / 7일이 완전 균일해져 `DAY_PLANS` 존재 이유 자체가 파괴)
- ✅ **F1b. 24시간 당직 커버리지 — 지뢰 4의 해답** (PR #39)
  - ✅ `IncomingCall.nightShift`(DAY_PLANS 마지막 2통 = 야간, **RNG 0**) · `Hospital.roundTheClockBackup`(선택 필드 — 당직을 모델링하는 건 1막 플레이어 병원뿐) · `ROUND_THE_CLOCK_MIN_DOCTORS = 2`
  - ✅ **거절 사유 4종 → 5종**: `NO_NIGHT_BACKUP` 신설. 야간 공백을 `NO_BACKUP_CARE`와 뭉치면 대사가 *"저희도 순환기 시술팀이 없습니다"*가 되어 **30억 주고 순환기를 뽑은 플레이어에게 게임이 거짓말을 한다.** 현실에서도 별개이고 수용곤란 고지 최대 증가분이 '인력부족'(2.3배)이라 오히려 이쪽이 지배적이다. 새 대사: *"순환기 당직이 오늘 밤은 없습니다. 낮이었으면 받았습니다."*
  - ✅ UI에 '야간' 배지 — 왜 밤에만 막히는지 플레이어가 스스로 잇게. 해석 카피 0
  - ✅ 검증: `tsc` 0 · vitest **427 green**(신설 15) · 브라우저 — 야간 STEMI가 순환기 1명이면 하드락·2명이면 수용, 야간 워크인은 영향 없음. 양심 2명 결말 **+5억**(I8 통과)
  - ⚠️ **브라우저가 잡은 한계 — 전량 수용 전략에선 이 장치가 안 보인다**: 자리 3이 **주간 콜 3통**(idx 0~2)으로 다 차서 야간 콜(idx 3~4)이 **항상 `NO_BED`에 먼저 걸린다**(게이트 우선순위 자리 → 당직 → 과밀 → 배후). 측정: 순환기 1명 **+55억** / 2명 **+48억**인데 **구한 환자 수는 동일** — 즉 탐욕 플레이엔 2명이 여전히 −7억 순손해다. **강지배가 완전히 죽진 않았다.**
  - 📌 **그래도 지뢰 4는 해소된다**(강지배 = *모든* 전략에서 열등인데, 이제 **자리를 밤에 남겨두는 전략에선 2명이 우월**하다 — 존재하면 강지배가 아니다). 그리고 그 전략이 곧 딜레마다: **미용 하나(+3)를 거절해야 야간 심근경색 하나(−2)를 받는다.** 순환기 1명이면 자리를 남겨도 못 받으니 탐욕이 정답 — **"2명째가 사는 건 밤에 받을 선택지"**가 정확히 구현됐다
  - ⚠️ **다만 초보 플레이어는 이걸 못 본다** → **F3(신문)이 완성한다**: 밤에 거절한 STEMI가 다음날 기사로 돌아와야 "자리를 비워둘걸"이 성립한다. F2(boarding)가 자리를 더 조이면 압력이 커진다. **F1b 단독으로는 미완이고 F3에서 닫힌다**
  - ⚠️ 순환기 상한은 여전히 `FIXED_BEDS`(3) 파생이라 **3명째는 강지배**다(24시간을 두 번 사지 못함). `ROUND_THE_CLOCK_MIN_DOCTORS`(2)로 좁힐지는 F3 뒤 재검토
  - 🔴 **기각된 답 — "위저드 상한 1"**(이력 보존, T-042): 코드가 이진값이니 게임도 이진으로 맞추자는 것이었다. **코드의 한계를 현실의 진실로 착각**한 것이고(T-039 단위 혼용과 같은 계열), 사용자가 정확히 그렇게 기각했다 — *"현실에선 순환기 의사가 2명 배치될 수 있잖아. 한 명으로 강제하면 게임이 저렴해 보이고 만들기 쉽게 하려고 타협한 느낌이 든다."* 답은 이미 **레포 안 리서치**에 있었다: STEMI는 **"24시간 중재 순환기내과"**를 갖춘 PCI 병원이라야 재관류가 된다([medical-system-grounding.md](../docs/research/medical-system-grounding.md):20) · 거절하는 당직의의 정당한 제약은 **"당직 1명이 이미 3명 동시 진료"**(:66) · 수용곤란 고지의 **인력부족 사유가 1년 새 2.3배**([stemi-factsheet.md](../docs/research/stemi-factsheet.md):19) · I4의 근거 자체가 **"24시간 대기 고정비"**
- ✅ **F2. 검사 유도 + boarding** (PR #40) — 스펙 §2.6(척추)의 구현체
  - ✅ **급여 환자 전체**(일반 응급 + STEMI) 수용 시 「검사를 추가한다 +4억 · 자리 내일까지」 → 부호가 **−에서 +로 뒤집힘**. `WORKUP_ECONOMICS` 10/6 ≈ **166.7%**(검체 160.5% 밴드). 미용은 비급여라 버튼 자체가 없다
  - 📌 **일반 응급 전용이 아닌 이유**: 초안의 한정 논거("STEMI는 시술 117%가 본체")가 **순환 논증**이었다 — 117%의 흑자 출처 자체가 검사다. 한정하면 117%가 영원히 창발 못 하고 부호 위반이 남는다
  - ✅ 대가 = **boarding**: `오늘 자리 = FIXED_BEDS − 어제 검사 수`. **날짜 간 결합 신설**. 이월을 **기본값 0 선택적 인자**로 넣어 `receiving.test.ts`의 기존 자리 테스트는 살아남았고 — **스펙 §9의 예측대로 `session.test.ts:91`도 안 깨졌다**(검사를 안 붙인 경우엔 여전히 참이라, 검사를 붙인 경우를 새 테스트로 잠갔다). **기존 테스트 0건 깨짐**
  - ✅ 결말 장부에 `이번 주 검사 수익` **별도 줄**. 해석 0
  - ✅ **인과 사슬이 브라우저에서 실제로 닫혔다**: 월요일 검사 3건 → 진료 −7·검사 +12·**순이익 +12 "오늘 흑자 🎉"** → **화요일 자리 0/3** → 5통 전부 하드락 → 수요일 자리 3/3 복귀(자기제한) → 토요일 자리 1에 미용 하나 받자 **심근경색 2통·응급 1통 하드락**. 결말: 진료 **−5** / 검사 **+36** / 소송 −35 / **순이익 +42**(I8 통과)
  - 🐛 **tsc가 못 잡은 무성 버그**: `SessionClient`의 `onDecide={(accept) => decide(s.receiving!, accept)}`가 **`withWorkup`을 조용히 버렸다** — 인자 적은 콜백은 할당 가능해 타입체커도 통과한다. 검사 버튼을 눌러도 아무 일이 안 일어날 뻔했고, **브라우저 아니면 못 잡았다**
  - 📌 **I5의 전제가 F1b 때문에 바뀌었다**: 스펙 I5는 "n=1 + 주간 STEMI **전량 14**"인데, F1b 이후 순환기 1명은 야간 6통을 못 받아 **최대 8통**이다. 새 전제로 `−12 + 8×(−2) + 8×(+4) = **+4**` — 오히려 I5가 요구한 "**근소하게 ≳0**"에 정확히 맞는다
- ✅ **F3. 다음날 아침 신문** (PR #41) — 사용자가 가장 원한 것. **사슬이 여기서 닫힌다**
  - ✅ `news.ts` 신설 — 다음 날 접수 화면 **상단**(직전 대사 위)에 어제 못 받은 STEMI 기사. 결과=**코드 결정**, 문장만 뱅크(**RNG 0**·콜 id 시드)
  - ✅ 헤드라인 문법: `[지역] '뺑뺑이'… 병원 [N]곳 거부, [T] 만에 [나이대] 숨져` · **U+2026** · **"죽" 금지** · '뺑뺑이' 작은따옴표 · 2회차부터 **"또"** · 거부 **8~15곳** / 지연 **80~300분**. **무주체** — 병원명·의사명·환자명 없음(실제 보도 38건이 거의 100% 그렇고, 그게 이 게임의 show-don't-tell과 같다)
  - ✅ 🔴 **윤리 가드를 코드에**: `FORBIDDEN_REAL_EVENT_TOKENS`(대구·용인·보은·부산·10대·70대·33개월·고교생)를 **테스트가 뱅크 전체를 훑어 검사**한다. 지역=**가공**(한내/서림/금하/백천) + 프로필=**40~60대**로 **두 축 모두** 실제와 어긋나게. 규칙 전문을 `news.ts` 상단 주석에 복사(스펙 §5 요구)
  - 🔴 **브라우저가 잡은 거짓말 2건**(테스트로 회귀 잠금):
    - **부제가 거짓말했다** — 자리가 있는데 **내가 그냥 거절**한 걸 신문이 "병상 부족"이라고 썼다(`reason: null` → `NO_BED` 폴백). 사유를 지어내면 안 된다 → **'수용 거부'**(사유 없음)로 분리. 해석 카피 0인데 더 아프다
    - **같은 날 두 사람이 정확히 같은 시간 만에 숨졌다**(둘 다 "2시간 50분") — 콜 id가 `d1c2`/`d1c5`처럼 한 글자만 달라 시드 차이가 `>>3`에 먹혔다. FNV+xorshift로 교체
  - 📌 **F1b가 회수됐다**: 순환기 1명으로 돌리면 부제에 **'야간 당직 공백'**이 찍힌다 — 밤에 거절한 STEMI가 다음날 기사로 돌아온다. F1b의 "초보는 이 장치를 못 본다"가 여기서 해소된다
  - ⏸ 7일차 거절의 결말 후속 기사("3년 만에 송치" 구조 — 그 의사가 플레이어)는 보류 — ~~F4와 함께 판단~~ → **F4가 절단됐으니 독립 판단**. 인과는 F1~F3로 이미 닫혀서 급하지 않다
- ⏸ **F4. 가산금 배분 — 대상은 흉부외과** → 🔴 **절단**(2026-07-17, PR #44). 스펙이 지정한 "마감 압박 시 1순위 절단" 대상이었고, **마감이 아니라 사실이 절단했다** — 근거: [thoracic-surgery-role.md](../docs/research/thoracic-surgery-role.md) · [T-049](troubleshooting/T-049.md)
  - **왜 막혔나 — 도달 불가**: 흉부외과는 1막에서 **아무 역할이 없다**(`hardlockReason`이 검사하는 건 순환기뿐, 콜 3종 중 `THORACIC_SURGERY`를 요구하는 게 없음). 채용하면 **32억 + 주당 −15**를 내고 사는 게 **0**이라 합리적 플레이어는 안 뽑고, 그러면 가산금 슬라이더를 **영영 못 본다.** 뽑아도 슬라이더의 합리적 답은 **항상 법정 최소 30%**(더 줘봐야 사는 게 없음) — **아무도 못 보는 화면에 답이 하나뿐인 선택지**였다. ⚠️ 적대 검증(§12-②)은 "존재하지 않는 제도"는 잡았지만 **도달 가능성은 안 봤다**
  - **흉부외과에 역할을 주려 세 번 시도했고 세 번 다 사실이 거부했다** (전량: 리서치 문서 §8)
    - ❌ **STEMI 수용 게이트** — 거짓. ESC는 원내 심장외과를 **애초에 요구한 적 없고**("do not demand cardiac surgery on-site"), RCT 2건(CPORT-E 18,867명 / MASS COMM 3,691명)이 **비열등 입증**, 심평원 PCI 급여기준에 **시설 요건 없음**. 게다가 **방향이 반대** — STEMI primary PCI는 선택적 PCI보다 **더 관대하게** 허용됐다
    - ❌ **응급 CABG 콜** — 빈도로 죽음. STEMI 24시간 내 CABG **0.95%** → 한 판(STEMI 14통)에 **0.19건**이라 사실상 안 뜬다. (⚠️ **한국 수치는 없다** — 심평원 AMI 평가와 CABG 평가가 별개 체계라 분자·분모를 잇는 공표치가 없음. "한국 STEMI의 X%가 CABG"는 **지어낸 숫자**가 된다)
    - ❌ **심장통합진료 삭감** — **2015년에 죽은 조항**. 현행 자656에 「심장통합진료」·「좌주간부」·「다혈관」 **0건**(1차 원문 PDF **다섯 판본 전수 확인**). 살아 있던 2015년조차 답은 "삭감"이 아니라 **"MOU를 맺어라"**였다
  - 🎯 **패턴**: 제도가 흉부외과 없는 기관에 요구하는 건 **일관되게 "협약"이지 "탈락"**이다 — 급여기준(MOU) · KSCVI(전원 협약) · 전원 네트워크 시범사업 모두. **벽이나 벌금을 세우려는 설계는 어느 층에서도 사실과 어긋난다**
  - ⏸ **남은 축 = 지정 티어 하나**(권역심뇌혈관센터 "심장 수술 전문의 확보" 별표 2 · 권역외상센터 **전담 1명 겸직 불가** · 목포중앙병원 2022년 **지정 철회**, 전남 4년 공백). 사실이지만 **콜이 아니라 병원 등급**이고 **시간 척도가 7일보다 크다**(지정·평가·철회는 연 단위) → 접수 화면이 아니라 **위저드나 결말**의 자리. 재량 예외도 있어(2023년 13개 권역센터 전원 재지정) **하드 게이트가 아니라 압력**. **본선 48시간 확장 후보**
  - ✅ **살아남은 자산**: §4 가산금 배분 규정 조사는 유효하다(60%/30% 3시점 일관 · 20억 완화 **2011년부터**(2018 아님) · **단일 출처** · 복지부 원문 미확보라 **"고시"라고 쓰면 안 됨** · 5% 정원감축 제재는 **2011년 9개 병원에 실제 발동**, 트리거는 "지원자 미달"이 아니라 **"가산금을 해당 과에 안 쓴 것"**). 지정 티어 축을 본선에서 하면 그 위에 얹을 수 있다
- ✅ **별건 (F와 독립 · 적대 검증 발견) — 과별 인원 상한이 없다** → **F1에서 해소**(PR #38). `withinBudget`이 `hiringCost ≤ 100`만 봐서 **미용 10명(정확히 100억)이 합법 셋업**이었고 부문 손익 `70 × 10 = **+700억/주**`(예산의 7배)였다. `MAX_DOCTORS_PER_DEPT = FIXED_BEDS` + `withinDeptCaps`로 차단, 브라우저에서 스테퍼가 3에서 멈추는 것 확인
- ⏸ **재정중립 제로섬** (사용자 지시로 분리 — "구현이 크니깐 이건 따로 진행하려고해")
  - 상대가치 개편은 **총점 고정**이 전제 → 한 과를 올리려면 다른 과를 내려야 한다. 언론은 "재정 돌려막기", "나눠먹기식 땜질"이라 부른다. **필수과 수가가 안 오르는 이유가 악의가 아니라 산수** — 이 게임 주제 그 자체다.
  - 왜 지금 안 하나: 정책 레이어를 통째로 신설해야 하고 마감(8/10) 대비 위험이 크다. **본선 48시간 확장 후보로 가장 강함.** 근거: [fee-schedule-and-subsidies.md](../docs/research/fee-schedule-and-subsidies.md) §6

## ⏸ 본선 48시간 확장 (신청 이후 보류)

- ⏸ 음성 TTS(히어로 보이스)
- ⏸ 시나리오 추가 — 필수의료 붕괴·지방 의료 공백을 독립 시스템으로
- ⏸ 방어진료 드리프트 누적(반복 플레이로 플레이어가 전원·회피로 기우는 체감)
- ⏸ 동적 디렉터(콜 밀도 에스컬레이션 — 슬로건 "디렉터"에 부응)
- ⏸ 소송·언론 인과 사슬 확장
